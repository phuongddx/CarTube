---
phase: 04-carplay-search-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CarTube/Search/DurationFormatter.swift
  - CarTube/Search/SearchCoordinator.swift
  - CarTube/CarPlay/SearchResultsViewController.swift
  - CarTube/CarPlay/CarPlayViewController.swift
  - CarTube/CarPlay/CarPlaySingleton.swift
  - CarTube/Views/KeyboardView.swift
  - CarTubeTests/DurationFormatterTests.swift
  - CarTubeTests/SearchCoordinatorTests.swift
  - CarTube.xcodeproj/project.pbxproj
autonomous: true
requirements: [UI-01, UI-02, UI-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Populated results render as a native UIKit list layered over the webview: min(results.count, 8) rows at a fixed 68.0pt row height (satisfying the ≥60pt floor), each row showing a 106×60pt thumbnail (radius 8), a 2-line 22pt semibold title, a 1-line 17pt channel, and a 15pt monospaced-digit duration badge — and ONE tap plays the video through the existing loadUrl(YT_EMBED + videoId) path and dismisses the overlay (UI-01; UI-SPEC populated + overflow rows)"
    - "A partial row keeps its full height when duration is nil (badge hidden) and shows the #1C1C1E thumbnail placeholder until the per-cell async image load replaces it; cell reuse cancels the in-flight image task (UI-SPEC partial row)"
    - "Long titles truncate at 2 lines and long channel names at 1 line with tail truncation; more than 8 API results are truncated to the first 8 with no pagination anywhere on this surface (UI-SPEC overflow + long-text rows)"
    - "One through eight results render with identical layout and copy; zero results render the dedicated single no-results row — no count-dependent copy exists (UI-SPEC zero-one-many + empty rows; empty-row action wiring completes in 04-02)"
    - "The header — Close button top-left at a ≥44×44pt hit target and the right-aligned 13pt systemGray 'Results from YouTube' attribution — is static chrome that persists unchanged in every state (UI-SPEC header loading/error/overflow rows; UI-03)"
    - "Showing, hiding, and tapping rows never resizes, reloads, or navigates the webview beneath: the overlay toggles visibility only, the webview keeps view.bounds for the entire overlay lifecycle, and the only navigation is a concrete row tap (UI-01 hard constraint — the keyboard's frame-shrink is the carved-out anti-pattern)"
    - "The typed keyboard input path funnels through the single SearchCoordinator — @MainActor final class with static let shared — via exactly three new CarPlaySingleton passthrough methods (showSearchResults, dismissSearchResults, submitSearchQuery) and no other new singleton surface (UI-02)"
    - "The overlay appears with the 'Searching…' spinner row the moment a query is submitted, before any network completion (UI-SPEC loading row / loading entry)"
    - "On any search failure the coordinator executes the fail-closed degrade edge — the existing webview search path via the existing searchVideo funnel — so the driver is never left with a dead screen; the fallback ROW presentation lands in 04-02 (SRCH-03 caller contract)"
    - "ISO-8601 PT durations render as mm:ss / h:mm:ss badge strings ('12:34', '1:02:03'); unparseable or nil durations leave the badge hidden (DurationFormatter, fixture-tested)"
    - "SearchCoordinator unit tests prove the funnel against MockURLProtocol fixtures — the presenter records the [loading, results] state sequence for a happy-path query — with zero live network and zero quota spend"
  artifacts:
    - CarTube/Search/DurationFormatter.swift — pure ISO-8601 PT→badge formatter, Foundation-only
    - CarTube/Search/SearchCoordinator.swift — @MainActor funnel singleton with injectable service/cache and presenter/degrade/nap seams defaulting to the singleton edges
    - CarTube/CarPlay/SearchResultsViewController.swift — UITableViewController overlay: header (Close + attribution), result cells, loading + no-results rows, async thumbnails
    - CarPlayViewController wiring — typed resultsController property, full-frame child-VC insert below screenOffLabel, show/dismiss without any webview mutation
    - CarPlaySingleton — exactly three one-line passthroughs after toggleKeyboard
    - KeyboardView — typed-query mirror buffer + magnifyingglass submit key on all three keyboard modes
    - CarTubeTests/DurationFormatterTests.swift + CarTubeTests/SearchCoordinatorTests.swift
  key_links:
    - "KeyboardView submit key → CarPlaySingleton.submitSearchQuery → SearchCoordinator.search → LastQueryCache.cachedResults(for:) → YouTubeSearchService.search → SearchFallback.decide → presenter hook → CarPlaySingleton.showSearchResults → CarPlayViewController.showSearchResults → overlay rows"
    - "Row tap → onSelect closure → CarPlaySingleton.loadUrl(YT_EMBED + videoId) + dismissSearchResults — the one-hop playback path, identical to the share-extension and phone TextField path"
    - "Degrade decision → degrade hook → CarPlaySingleton.searchVideo(query) → existing percent-encoded webview search load"
    - "DurationFormatter.display(result.duration) → cell duration badge text"
---

<objective>
Tracer-first end-to-end typed search: a driver types on the existing CarPlay keyboard, taps a search key, and sees real parsed YouTube results as a native tappable list layered over the webview; one tap plays the video and dismisses the overlay — the landing zone both voice inputs (Phase 5) will require.

Implements the populated/partial/overflow/long-text/zero-one-many/header UI-consideration rows and the loading entry from `04-UI-SPEC.md`, UI-01 (glanceable tappable list, no webview disturbance), UI-03 ("Results from YouTube" attribution), and the UI-02 funnel skeleton (single SearchCoordinator + exactly three passthroughs).

Purpose: This is the phase's only architectural slice — overlay wiring pattern, state enum, coordinator seam, and passthrough cap are all decided here and only expanded upon in 04-02.
Output: DurationFormatter + SearchCoordinator + SearchResultsViewController + wiring in three existing files + tests, all target-wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-carplay-search-surface/04-UI-SPEC.md
@.planning/phases/04-carplay-search-surface/04-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/03-search-core/03-02-PLAN.md
@.planning/phases/03-search-core/03-03-PLAN.md
@CarTube/CarPlay/CarPlayViewController.swift
@CarTube/CarPlay/CarPlaySingleton.swift
@CarTube/Views/KeyboardView.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>DurationFormatter — pure ISO-8601 PT→badge formatter, RED first</name>
  <files>CarTube/Search/DurationFormatter.swift, CarTubeTests/DurationFormatterTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>CarTubeTests target and committed CarTube scheme exist from Phase 3 (03-01); CarTubeTests/MockURLProtocol.swift exists (03-02) for the funnel test in Task 2.</precondition>
  <read_first>
  - .planning/phases/04-carplay-search-surface/04-UI-SPEC.md — Component Inventory row 2: duration badge "bottom-right of text block or trailing, monospaced digits, e.g. '12:34' / '1:02:03'"
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "Phase 3 Contracts Being Consumed" table: SearchResult.duration is String? carrying raw ISO-8601 from videos.list contentDetails
  - .planning/phases/03-search-core/03-02-PLAN.md — Task 1/Task 2: fixture-loading convention (Bundle(for:) + path(forResource:ofType:)) and the throwaway ruby gem pbxproj-wiring procedure (delete the script after)
  - CarTube/Search/SearchResult.swift — the duration field the formatter consumes
  </read_first>
  <reversibility rating="reversible">new standalone file; pure function, no callers until Task 2 wires it into the cell.</reversibility>
  <behavior>
  - Test: "PT12M34S" → "12:34"
  - Test: "PT4M13S" → "4:13" (minutes not zero-padded below an hour)
  - Test: "PT1H2M3S" → "1:02:03" (with hours, minutes and seconds zero-padded)
  - Test: "PT2H" → "2:00:00" and "PT58S" → "0:58" and "PT30S" → "0:30" (missing components are zero)
  - Test: nil → nil, "" → nil, "12:34" → nil, "garbage" → nil, "P" → nil (only strict P[nH][nM][nS] input parses)
  </behavior>
  <action>
  Write CarTubeTests/DurationFormatterTests.swift first (RED), then CarTube/Search/DurationFormatter.swift: an enum DurationFormatter with one static func display(_ iso8601: String?) -> String? that parses the strict pattern P, then optional H/M/S integer components, into hours/minutes/seconds and formats mm:ss when hours == 0 (minutes unpadded, seconds zero-padded) or h:mm:ss when hours > 0 (minutes and seconds zero-padded). A character-scanning or single-regex parse both fit; if regex is used, one brief why-comment is allowed under the repo comment policy. Nil, empty, and non-matching input return nil — never a crash, never a fabricated string. Import Foundation only; four-space indent; repo header comment with module name CarTube. Wire the file + test into the CarTube / CarTubeTests targets via the throwaway ruby gem script and delete the script after (re-grep pbxproj first — Phases 2–3 shifted line numbers). RED → GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'enum DurationFormatter' CarTube/Search/DurationFormatter.swift && [ "$(grep -c 'import ' CarTube/Search/DurationFormatter.swift)" = "1" ] && grep -q 'Foundation' CarTube/Search/DurationFormatter.swift</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED with the DurationFormatter tests included, 0 failures
  - CarTube/Search/DurationFormatter.swift declares enum DurationFormatter with a single static display function; imports Foundation only (exactly one import line)
  - Test file covers both formatting shapes (mm:ss, h:mm:ss), zero-component edges, and all nil/empty/malformed rejects listed in behavior
  - project.pbxproj lists DurationFormatter.swift in the CarTube target sources and DurationFormatterTests.swift in CarTubeTests (grep gates)
  </acceptance_criteria>
  <done>A pure, fixture-tested PT-duration formatter exists, Foundation-only and target-wired, producing the exact badge strings the UI-SPEC pins.</done>
</task>

<task type="tracer">
  <name>End-to-end typed search — one path: keyboard submit → coordinator funnel → overlay rows → tap plays → dismiss</name>
  <files>CarTube/CarPlay/SearchResultsViewController.swift, CarTube/Search/SearchCoordinator.swift, CarTube/CarPlay/CarPlayViewController.swift, CarTube/CarPlay/CarPlaySingleton.swift, CarTube/Views/KeyboardView.swift, CarTubeTests/SearchCoordinatorTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>Phase 3 artifacts exist and compile: CarTube/Search/SearchResult.swift, YouTubeSearchService.swift (async search(query) throws, SearchError), LastQueryCache.swift (actor, cachedResults/store/clear), SearchFallback.swift (decide → SearchOutcomeAction), and CarTubeTests/MockURLProtocol.swift with the four fixtures.</precondition>
  <read_first>
  - .planning/phases/04-carplay-search-surface/04-UI-SPEC.md — IN THIS ORDER: Design System precedent note; Spacing exceptions (row ≥60pt via 68pt, Close ≥44×44, full-screen view.bounds + autoresizing, thumbnail 106×60 radius 8); Typography table (22/17/15/13 sizes, exactly 2 weights); Color table (black dominant, #1C1C1E placeholder + selected fill, #333333 separators, white text, systemGray secondary, systemRed accent reserved for spinner + empty-state action); Copywriting Contract (exact strings "Results from YouTube", "Close", "Searching…", "No results"); Component Inventory rows 1–5; Interaction Contract (tap row, Close, dismissal mechanics, UI-01 hard constraint, loading entry); Z-order block; Phase 5 reservation (do NOT add mic-button space)
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "SearchResultsViewController" section: header-comment convention (module CarTube), child-VC wiring form, z-order stack, NoSleep-guard wiring-position hazard (place wiring downstream of the guard; prefer insertSubview belowSubview so order-independence holds), toggleKeyboard anti-pattern carve-out, KeyboardView direct-call convention vs closure variant (closure variant chosen for the phone-preview contingency — sanctioned), deviation ledger items 1–4, visual-language separator color, state model
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "SearchCoordinator" section: @MainActor final class + static let shared (do NOT retrofit @MainActor onto CarPlaySingleton), controller-nil edge (reuse loadUrl behavior by calling it, never re-implement), showSearchResults nil-controller behavior is optional-chaining no-op (ephemeral results, no results cache in the singleton), funnel shape, boundary rule (SearchCoordinator is the ONLY Search/ file that may name CarPlaySingleton)
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "CarPlaySingleton" section: the exact three-passthrough block, one-line bodies, /// doc comments, placement after toggleKeyboard, no stored properties
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "No Analog Found" rows: thumbnail per-cell async load guidance (Task in cellForRowAt, URLSession.shared.data(from:), cancel in prepareForReuse, guard URL?, no third-party image library, NSCache omitted) and UITableViewController hand-roll guidance
  - .planning/research/ARCHITECTURE.md — Pattern 1 (overlay-on-webview, the deliberate no-resize difference from the keyboard), Pattern 2 (singleton passthrough cap), State Management section, Internal Boundaries (Search→CarPlay one direction; thread hop to main before presentation)
  - CarTube/CarPlay/CarPlayViewController.swift — viewDidLoad wiring region (keyboard block ~L112–118, screenOffLabel creation ~L122–129, splash after), toggleKeyboard, the keyboard WKScriptMessageHandler
  - CarTube/CarPlay/CarPlaySingleton.swift — the sendInput/backspaceInput/goHome/goBack/toggleKeyboard passthrough block and loadUrl/searchVideo edges
  - CarTube/Views/KeyboardView.swift — sendKey/backspace/dismiss conventions, MakeKBBtn(label:affectedByShift:image:maxWidth:action:) helper, the bottom action row of all three modes
  </read_first>
  <reversibility rating="costly">the three-passthrough surface, the overlay state enum, and the coordinator seam are the contracts Phase 5's Siri intent and mic button build against — changing them later touches three phases.</reversibility>
  <behavior>
  - Test (funnel smoke, written RED before the coordinator exists): a SearchCoordinator built with the MockURLProtocol-backed YouTubeSearchService and a real LastQueryCache, serving the happy-path search + videos fixtures, records the presenter state sequence [loading, results] for the query "lofi beats" — the results array non-empty with filled durations — and issues exactly two stubbed requests (search.list then videos.list)
  - Test: a second identical search on the same coordinator instance records [loading, results] again but issues ZERO further requests (cache hit) — asserted via the MockURLProtocol request log
  </behavior>
  <action>
  Write the two funnel tests RED against a SearchCoordinatorTests.swift stub, then build the slice production-quality end to end:

  1. SearchResultsViewController.swift (CarTube/CarPlay/, new UITableViewController subclass): declare enum SearchResultsState { case loading; case results([SearchResult]); case fallback } at file scope (the coordinator's presenter vocabulary; the fallback case renders in 04-02, degrade in this plan dismisss instead). Init with three closures — onSelect: (String) -> Void, onClose: () -> Void, onRetry: () -> Void — following the PATTERNS-sanctioned closure variant so the phone-preview harness (04-02) can drive the same controller. Plain-style table, opaque pure-black background, fixed rowHeight 68.0. Custom UITableViewCell built in code: 106×60pt thumbnail UIImageView (contentMode aspectFill, clipsToBounds, corner radius 8, #1C1C1E placeholder fill — UIColor(red: 28/255.0, green: 28/255.0, blue: 30/255.0, alpha: 1)); per-cell async image load launched as a Task in cellForRowAt using URLSession.shared.data(from:) on the guarded URL?, image set only on successful UIImage(data:), task cancelled in prepareForReuse; title UILabel 22pt semibold, 2 lines, tail truncation; channel UILabel 17pt regular, 1 line, tail truncation; duration UILabel 15pt monospacedDigitSystemFont trailing/bottom of the text block, text from DurationFormatter.display(result.duration), label hidden when nil; row content insets 16pt leading/trailing, thumbnail inset 16pt from vertical edges, thumbnail-to-text gap 4pt, selectedBackgroundView filled #1C1C1E. numberOfRows is a total formula: loading, fallback, and .results with an empty array each return 1 (their single dedicated row — an empty array IS the results state, so the no-results row is served by the .results case itself); .results with a non-empty array returns min(results.count, 8). Implement the empty branch as an explicit results.isEmpty check returning 1 in BOTH numberOfRowsInSection and cellForRowAt (the only row is indexPath 0, the no-results cell), written with that exact predicate so the awk-scoped grep gate pins the no-results row at row 0 of the empty state. State methods: update(_ state: SearchResultsState) sets state + reloads. tableHeaderView built in code: "Close" UIButton type .system top-left with a ≥44×44pt hit target and white 17pt semibold title calling onClose; right-aligned single-line 13pt systemGray UILabel with the exact string "Results from YouTube". Single-row renderings: loading row = centered medium UIActivityIndicatorView tinted systemRed + "Searching…" label; empty results array renders the single no-results row ("No results" heading + "Nothing matched that search." body; the red "Try another search" action wiring lands in 04-02 — render the label now, wire onRetry in 04-02). Separator color UIColor(hue: 0, saturation: 0, brightness: 0.2, alpha: 1). didSelectRow calls onSelect(results[row].videoId). Header comment per repo convention, module CarTube. No pagination, no scroll delegates, no NSCache.

  2. SearchCoordinator.swift (CarTube/Search/, new): @MainActor final class SearchCoordinator with static let shared and an internal init taking (service: YouTubeSearchService, cache: LastQueryCache) plus internal injectable seams for testing, each defaulting to the real edge — presenter: (SearchResultsState) -> Void defaulting to CarPlaySingleton.shared.showSearchResults, degrade: (String) -> Void defaulting to CarPlaySingleton.shared.searchVideo, and dismissOverlay: () -> Void defaulting to CarPlaySingleton.shared.dismissSearchResults. This file is the documented single exception to the Search/ no-singleton boundary (re-scoped gate below). Func search(_ query: String): guard against a nil-or-whitespace-trimmed-empty query (return without presenting anything), then Task { await run(query) }. Func run(_ query: String) async: presenter(.loading); if let cached = await cache.cachedResults(for: query) { presenter(.results(cached)); return }; resolve via try await service.search(query) into Result and SearchFallback.decide; on .showResults(store the page with cache.store(query:results:) then presenter(.results(results))); on .showNoResults presenter(.results([])); on .degradeToWebviewSearch execute the caller contract — degrade(query) then dismissOverlay() — the fallback-row presentation and ≤2.0s auto-dismiss replace the dismiss in 04-02. The service awaits run off the main actor as designed (Phase 3) and every presenter call happens on MainActor structurally. Import Foundation only (same-module types need no import); repo header comment.

  3. CarPlaySingleton.swift: add EXACTLY the three passthroughs from the PATTERNS block, placement after toggleKeyboard, one-line bodies, /// doc comments, no stored properties, no guards, no alerts: showSearchResults(_ state: SearchResultsState) → controller?.showSearchResults(state); dismissSearchResults() → controller?.dismissSearchResults(); submitSearchQuery(_ query: String) → SearchCoordinator.shared.search(query). Note the deliberate parameter adaptation: the pinned Pattern 2 excerpt passed a bare array, but the SPEC's loading and fallback rows are STATES an array cannot express — the state enum keeps the method count at three (document this one-line deviation in the commit message, not in code comments).

  4. CarPlayViewController.swift: add a typed private property for the results controller beside keyboardView (deviation 3 — the controller reference is kept, unlike the keyboard's plain UIView). In viewDidLoad, AFTER the screenOffLabel is created and BEFORE the splash block (downstream of the fail-silent NoSleep guard — do not touch that guard; surgical changes only): construct SearchResultsViewController with [weak self] closures — onSelect: CarPlaySingleton.shared.loadUrl(YT_EMBED + videoId) then CarPlaySingleton.shared.dismissSearchResults(); onClose: CarPlaySingleton.shared.dismissSearchResults(); onRetry: CarPlaySingleton.shared.dismissSearchResults() then CarPlaySingleton.shared.toggleKeyboard() (full wiring of onRetry completes in 04-02) — then addChild, frame = view.bounds, autoresizingMask = [.flexibleWidth, .flexibleHeight], isHidden = true, insertSubview(belowSubview: screenOffLabel) so correctness does not depend on statement order. Add func showSearchResults(_ state: SearchResultsState) { resultsController.update(state); resultsController.view.isHidden = false } and func dismissSearchResults() { resultsController.view.isHidden = true } mirroring toggleKeyboard's mechanics with ZERO webview mutation of any kind — no frame change, no reload, no navigation (the keyboard's height adjustment is the carved-out anti-pattern; dismissal restores nothing because nothing was moved). Do not touch the keyboard WKScriptMessageHandler or the JS message path.

  5. KeyboardView.swift: add a private typed-query mirror buffer (@State String). sendKey appends the exact transformed character it already forwards to sendInput (shift-aware; the space key's " " appends naturally); backspace drops the last character if any. Add a search-submit key to the bottom action row of ALL THREE keyboard modes, before the dismiss key, via MakeKBBtn(image: Image(systemName: "magnifyingglass"), maxWidth: width / 9, action:) — the action calls dismiss() then CarPlaySingleton.shared.submitSearchQuery(typedBuffer). The buffer is the keyboard's only new state; no other keyboard behavior changes.

  6. Wire SearchResultsViewController.swift (CarPlay group) + SearchCoordinator.swift (Search group) + SearchCoordinatorTests.swift into targets via the throwaway gem script; delete the script; verify both targets build. GREEN → commit.

  The tracer's end-to-end runnable proof is the funnel tests plus the build; the visual end-to-end run (submit → overlay → tap → play → dismiss) happens on the simulator CarPlay scene if the entitlement is granted, otherwise via the phone-preview harness built in 04-02 — both documented in this plan's verification section.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q '@MainActor' CarTube/Search/SearchCoordinator.swift && grep -q 'final class SearchCoordinator' CarTube/Search/SearchCoordinator.swift && grep -q 'static let shared' CarTube/Search/SearchCoordinator.swift && [ "$(git diff HEAD~1 -- CarTube/CarPlay/CarPlaySingleton.swift | grep -cE '^\+.*func ')" = "3" ] && grep -q 'belowSubview: screenOffLabel' CarTube/CarPlay/CarPlayViewController.swift && grep -q 'autoresizingMask = \[.flexibleWidth, .flexibleHeight\]' CarTube/CarPlay/CarPlayViewController.swift && grep -q 'rowHeight = 68.0' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'min(results.count, 8)' CarTube/CarPlay/SearchResultsViewController.swift && awk '/numberOfRows/,/^    \}/' CarTube/CarPlay/SearchResultsViewController.swift | grep -q 'isEmpty' && awk '/cellForRowAt/,/^    \}/' CarTube/CarPlay/SearchResultsViewController.swift | grep -q 'isEmpty' && grep -q 'Results from YouTube' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'Searching…' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'magnifyingglass' CarTube/Views/KeyboardView.swift && grep -q 'submitSearchQuery' CarTube/Views/KeyboardView.swift && ! grep -rn 'CarPlaySingleton' CarTube/Search/ --include='*.swift' | grep -v 'SearchCoordinator.swift' | grep -q . ; awk '/func showSearchResults/,/^    \}/' CarTube/CarPlay/CarPlayViewController.swift | grep -c 'webView' | grep -qx 0 && awk '/func dismissSearchResults/,/^    \}/' CarTube/CarPlay/CarPlayViewController.swift | grep -c 'webView' | grep -qx 0 && ! grep -q 'evaluateJavaScript' CarTube/CarPlay/SearchResultsViewController.swift CarTube/Search/SearchCoordinator.swift</automated>
    <human-check>When the CarPlay entitlement is granted: on the simulator CarPlay scene open the keyboard, type a short query, tap the magnifyingglass key — overlay appears over uninterrupted playback with the header and ≤8 rows; tap a row — video plays and overlay dismisses; reopen and tap Close — overlay dismisses, playback unchanged. Until then, defer this visual pass to 04-02's phone-preview harness (same controller, same coordinator).</human-check>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED including the funnel smoke tests (state sequence [loading, results]; second identical query issues zero network requests), 0 failures
  - CarPlaySingleton.swift gains EXACTLY 3 new funcs (git diff added-func count == 3): showSearchResults, dismissSearchResults, submitSearchQuery — each a one-line body, no stored properties added
  - CarPlayViewController.swift: results wiring sits after screenOffLabel creation and before splash (downstream of the NoSleep guard); insertSubview(belowSubview: screenOffLabel) present; the show/dismiss method bodies contain no webview reference at all (awk-scoped greps return 0)
  - SearchResultsViewController.swift: fixed rowHeight 68.0; total row-count formula (1 row for loading/fallback/empty-results, min(results.count, 8) otherwise; no-results row pinned at indexPath 0 via the isEmpty awk gates); thumbnail 106×60 radius 8 with 28/255.0-based placeholder fill; title 22pt semibold 2-line tail truncation; channel 17pt 1-line; monospacedDigitSystemFont duration label hidden when nil; Close ≥44×44pt white 17pt semibold; attribution 13pt systemGray with the exact string "Results from YouTube"; exact strings "Searching…", "No results", "Nothing matched that search." present; spinner tinted systemRed; separator UIColor(hue: 0, saturation: 0, brightness: 0.2, alpha: 1); per-cell image Task cancelled in prepareForReuse; URL? guarded (no force-unwrap in new code)
  - SearchCoordinator.swift: @MainActor final class, static let shared, Foundation-only imports, presenter/degrade/dismiss seams defaulting to the singleton edges, blank-query guard before any presentation
  - KeyboardView.swift: magnifyingglass submit key present in all three keyboard-mode bottom rows; submit routes through CarPlaySingleton.shared.submitSearchQuery with the typed buffer; buffer maintained by sendKey/backspace only
  - Re-scoped boundary gate holds: no CarPlaySingleton reference under CarTube/Search/ outside SearchCoordinator.swift
  - No evaluateJavaScript in either new file — the overlay adds zero new JavaScript execution surface
  - project.pbxproj lists both new source files in the CarTube target (CarPlay and Search groups respectively) and SearchCoordinatorTests.swift in CarTubeTests
  </acceptance_criteria>
  <done>A driver can type a query on the CarPlay keyboard, submit it, see real parsed results as a ≤8-row 68pt native list over uninterrupted playback, tap once to play, and dismiss — with every search input funneling through the single SearchCoordinator behind exactly three passthroughs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YouTube CDN → thumbnail decoder | untrusted remote image bytes fetched per cell and decoded into UIImageView |
| Data API JSON → overlay text | untrusted API-derived strings (title/channel/duration) rendered as labels; videoId interpolated into a playback URL |
| keyboard buffer → Google | typed query string egresses to googleapis.com exactly like the existing webview search does |
| Search/ island → CarPlay core | one-directional: only SearchCoordinator may name CarPlaySingleton (re-scoped gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | thumbnail decode (SearchResultsViewController cell) | medium | mitigate | UIImage(data:) nil-guard — image set only on successful decode, malformed bytes leave the #1C1C1E placeholder; no force-unwrap in the new path |
| T-04-02 | Tampering | videoId → playback URL construction | medium | mitigate | videoId originates from API JSON; row tap forwards it into the existing YT_EMBED + videoId path — DurationFormatter-style strict parse is unnecessary, but the closure passes only the API-provided string and never renders it as a URL elsewhere; no URL(string:)! is added in new code (the legacy force-unwrap in loadUrl stays untouched per surgical-changes) |
| T-04-03 | Spoofing | API-derived title/channel text | low | accept | display-only UILabel content; no interpreter, no link handling, no JS execution — grep gate proves zero evaluateJavaScript in the new files |
| T-04-04 | Information Disclosure | typed query egress to Google | low | accept | identical egress to the existing webview search path (searchVideo); no new data category leaves the device |
| T-04-05 | Denial of Service | unbounded image fetches during fast scroll | low | mitigate | ≤8 rows by construction; prepareForReuse cancels in-flight tasks; oversized/unsupported responses fail the UIImage decode guard |
| T-04-SC | Tampering | package installs | low | accept | no package installs — system UIKit only, no third-party image library (Registry Safety) |
</threat_model>

<verification>
- Task 1 automated: formatter tests green; Foundation-only; target wiring gates
- Task 2 automated: full gate chain above — 3-func cap on the singleton diff, wiring/z-order greps, 68.0/min-8/copy-string greps, webview-untouched awk-scoped greps, re-scoped Search-boundary gate, no-JS-eval gate, funnel tests green
- Human visual pass on the simulator CarPlay scene when the entitlement is granted (deferred to the 04-02 harness otherwise)
</verification>

<success_criteria>
- UI-01 core delivered: glanceable ≤8-row tappable list, ≥60pt rows, thumbnail+title+channel+duration, one-tap playback, playback continues beneath with zero webview disturbance
- UI-02 skeleton delivered: single SearchCoordinator funnel for typed input with exactly three passthroughs (voice surfaces land on it in Phase 5 unchanged)
- UI-03 delivered: "Results from YouTube" attribution in the persistent header
- The funnel is unit-proven against fixtures: [loading, results] sequence, cache-hit zero-request repeat, quota-degrade executes the webview edge
</success_criteria>

## Artifacts this phase produces (plan 01)

- CarTube/CarPlay/SearchResultsViewController.swift — UITableViewController overlay: SearchResultsState enum (loading/results/fallback — fallback renders in 04-02), header (Close + attribution), 68.0pt result cells with async-cancelled thumbnails, loading + no-results rows, onSelect/onClose/onRetry closure init (consumed by 04-02's harness and reused as-is by Phase 5)
- CarTube/Search/SearchCoordinator.swift — @MainActor funnel singleton: presenter/degrade/dismiss seams defaulting to the three singleton edges, cache→service→SearchFallback.decide chain, blank-query guard (the Phase 5 Siri intent and mic button call SearchCoordinator.shared.search)
- CarTube/Search/DurationFormatter.swift — pure PT→"12:34"/"1:02:03" badge formatter
- CarPlaySingleton — exactly three passthroughs (the milestone cap is now spent; Phase 5 adds nothing)
- CarPlayViewController — full-frame overlay wiring below screenOffLabel + show/dismiss with zero webview mutation
- KeyboardView — typed-query buffer + magnifyingglass submit key (the typed-input funnel entry)
- CarTubeTests/DurationFormatterTests.swift + SearchCoordinatorTests.swift — formatter matrix + funnel state-sequence tests (04-02 extends the coordinator matrix)
- Re-scoped boundary gate: the Phase 3 "no CarPlaySingleton under CarTube/Search/" grep now excludes SearchCoordinator.swift — later phases and CI must use the re-scoped form

<output>
Create .planning/phases/04-carplay-search-surface/04-01-SUMMARY.md when done
</output>
