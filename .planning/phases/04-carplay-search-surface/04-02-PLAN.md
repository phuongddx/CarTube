---
phase: 04-carplay-search-surface
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - CarTube/CarPlay/SearchResultsViewController.swift
  - CarTube/Search/SearchCoordinator.swift
  - CarTubeTests/SearchCoordinatorTests.swift
  - CarTube/Views/Debug.swift
  - CarTube.xcodeproj/project.pbxproj
autonomous: false
requirements: [UI-01, UI-02, UI-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A quota/key/transport failure renders the fallback row — heading 'Search unavailable', body 'Showing YouTube web search instead.', red spinner — while the existing webview search loads, and the overlay auto-dismisses bounded at 2.0 seconds (timer-only this phase; navigation-commit early-dismiss deliberately uncoupled — rationale documented in the task action); never a dead screen (UI-SPEC error row; SRCH-03 caller contract)"
    - "The empty state is fully wired: the 'Try another search' action is tappable, dismisses the overlay, and reopens the keyboard so the driver can retype (UI-SPEC empty row / Interaction Contract)"
    - "One through eight results render with identical layout and copy at any count, zero results shows the dedicated empty row, and the fallback row shares the same header — no count-dependent or state-dependent copy anywhere (UI-SPEC zero-one-many + header rows, completing the coverage)"
    - "All search inputs funnel through the single SearchCoordinator — typed now, voice in Phase 5 — with no new CarPlaySingleton methods beyond the three from 04-01 (UI-02 cap intact)"
    - "SearchCoordinator state transitions are fixture-proven: every failure kind degrades with the fallback state presented BEFORE the webview edge runs, the degrade path presents fallback then auto-dismisses, and a new different query mid-flight replaces the previous result (stale-response guard)"
    - "The phone-side preview harness drives the SAME SearchResultsViewController and SearchCoordinator the CarPlay scene uses, rendering every state (loading, 1..8 results, empty, fallback) on the phone simulator — verification does not block on the pending entitlement (contingency documented)"
    - "'Results from YouTube' remains visible in ALL states including loading, empty, and fallback (UI-03, header persistence)"
  artifacts:
    - SearchResultsViewController — fallback row rendering + onRetry action wiring + 2.0s auto-dismiss trigger closure
    - SearchCoordinator — stale-response guard (query generation token), fallback presentation ordering, fallback auto-dismiss hop
    - CarTubeTests/SearchCoordinatorTests.swift — extended matrix: degrade-before-edge ordering, fallback sequence [loading, fallback, dismissed], stale-response discard, empty state
    - Phone-side preview harness (Debug-screen section or a DEBUG-gated view) driving the overlay through every state
  key_links:
    - "SearchFallback.decide(.degradeToWebviewSearch) → presenter(.fallback) THEN degrade(query) → webview searchVideo load → fallback auto-dismiss (2.0s timer, generation-guarded; navigation-commit early-dismiss deliberately uncoupled this phase) → dismissOverlay()"
    - "No-results row action → onRetry → dismissSearchResults + toggleKeyboard (existing paths, no new surface)"
    - "Preview harness, two deliberate arms: state previews construct the production SearchResultsViewController directly and call update(_) — they carry ALL visuals (loading, 1..8, empty, fallback render identically to the CarPlay scene); the real-funnel button drives SearchCoordinator.shared with the presenter seam UNMODIFIED — execution-proof only (Xcode console/request log shows the live funnel run; visually inert on the phone because the default presenter targets the CarPlay singleton), state previews carry the visuals"
---

<objective>
Complete the state contract: wire the fallback row + ≤2.0s auto-dismiss for every failure kind, wire the empty-state retry action, add the stale-response guard, and land the phone-side preview harness so the full overlay state matrix is verifiable on the simulator regardless of the pending CarPlay entitlement.

Implements the UI-consideration error/empty rows' final wiring (the last two of the 12 resolved rows), completes UI-01's never-a-dead-screen guarantee (SRCH-03 edge execution), and proves UI-02's funnel completeness with the full fixture matrix.

Purpose: 04-01 proved the architecture on the happy path; this plan closes every documented state and makes verification entitlement-independent.
Output: Fallback + retry wiring, stale-response guard, extended coordinator tests, phone preview harness, human visual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-carplay-search-surface/04-UI-SPEC.md
@.planning/phases/04-carplay-search-surface/04-PATTERNS.md
@.planning/phases/04-carplay-search-surface/04-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/03-search-core/03-03-PLAN.md
@CarTube/CarPlay/SearchResultsViewController.swift
@CarTube/Search/SearchCoordinator.swift
@CarTube/Views/Debug.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>State completion — fallback row, retry wiring, stale-response guard, extended coordinator matrix</name>
  <files>CarTube/CarPlay/SearchResultsViewController.swift, CarTube/Search/SearchCoordinator.swift, CarTubeTests/SearchCoordinatorTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>04-01's artifacts exist and its tests pass: SearchResultsViewController with SearchResultsState/onSelect/onClose/onRetry, SearchCoordinator with presenter/degrade/dismiss seams, DurationFormatter, and the three passthroughs wired.</precondition>
  <read_first>
  - .planning/phases/04-carplay-search-surface/04-UI-SPEC.md — Copywriting Contract rows for the error state ("Search unavailable" + "Showing YouTube web search instead."), the empty-state action ("Try another search" → dismisses overlay and shows keyboard), Interaction Contract fallback row (auto-dismiss after 2.0s or navigation-commit per the SPEC; this plan implements the timer arm only — navigation-commit deliberately uncoupled, rationale in action step 2) and the tap-"Try another search" row; Component Inventory rows 5–6; Color table accent reservation (spinner + "Try another search" only)
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — "SearchCoordinator" section: timer/auto-dismiss analogs (showWarningLabel Timer.scheduledTimer and Task.sleep(for: .seconds(2)) both sanctioned on @MainActor); funnel shape including the degrade branch; controller-nil optional-chaining no-op rule
  - .planning/phases/03-search-core/03-03-PLAN.md — Task 2's SearchFallback behavior table: EVERY failure kind (apiKeyMissing, apiKeyInvalid, quotaExceeded, other) → degradeToWebviewSearch; success([]) → showNoResults — the exact decision inputs this task's matrix pins
  - .planning/phases/04-carplay-search-surface/04-01-SUMMARY.md — the seams, state enum, and test conventions 04-01 established
  - .planning/research/ARCHITECTURE.md — Pattern 3 fail-closed chain and the State Management section
  - CarTube/CarPlay/CarPlayViewController.swift — showWarningLabel's Timer pattern and toggleKeyboard (the retry action's target), and where fallback navigation commit could be observed (WKNavigationDelegate didFinish — read-only reference for the deliberately uncoupled arm; do not add webview-coupling this phase, the 2.0s timer alone bounds the fallback lifetime)
  </read_first>
  <reversibility rating="reversible">additive state wiring inside files 04-01 created; the stale-response guard is a small generation counter; worst case is revert of this plan's diff.</reversibility>
  <behavior>
  - Test (degrade ordering): given a coordinator whose service serves the 403 quota fixture, searching records presenter states [loading, fallback] AND the presenter saw .fallback BEFORE the degrade edge ran (ordering asserted with a shared event log the presenter and degrade closures append to)
  - Test (fallback dismissal): after the degrade path runs, the recorded presenter/dismiss sequence terminates in a dismissal within the test's control — assert the dismiss seam fired exactly once per degrade (the 2.0s real time is not awaited; assert the auto-dismiss task is scheduled by injecting a nap seam or a short Task.sleep with the test's expectation timeout, following the existing async test style)
  - Test (every failure kind degrades): apiKeyMissing, apiKeyInvalid, quotaExceeded, and other each produce [loading, fallback] — parameterized over fixture-driven error injections or stub throws
  - Test (empty success): a search resolving to success([]) records [loading, results([])] — the overlay's zero-results row, never a fallback
  - Test (stale response): submit query A (stub slow), then query B (stub fast) on the same coordinator; when A's response finally arrives it is DISCARDED — the final presenter state reflects B's outcome only, asserted via a generation-token-aware presenter sequence
  - Test (cache miss then hit unchanged): 04-01's two funnel tests still pass unchanged (regression guard)
  </behavior>
  <action>
  1. Write the behavior tests RED in CarTubeTests/SearchCoordinatorTests.swift using the existing MockURLProtocol fixtures and the seam-injection style from 04-01 (shared event-log arrays for ordering assertions).
  2. SearchCoordinator: add a private query-generation counter; search(_:) increments it and captures the token; run(_:generation:) discards its outcome (presenter, cache store, degrade — all of it) whenever the token no longer matches the current generation. Rework the degrade branch to: presenter(.fallback), then degrade(query), then schedule the auto-dismiss — Task.sleep(for: .seconds(2)) on the MainActor followed by dismissOverlay(), generation-guarded so a newer query's overlay is never dismissed by an older timer. The auto-dismiss is deliberately timer-only this phase: implementing the navigation-commit arm would require observing WKNavigationDelegate didFinish, coupling the Search-layer coordinator to webview navigation state and violating the phase's zero-webview-coupling posture — the 2.0s timer alone bounds the fallback's lifetime (whichever-first is satisfied trivially by the timer), so the early-dismiss arm is intentionally left uncoupled and the must_haves truth documents this. Do not touch WKNavigationDelegate.
  3. SearchResultsViewController: render the .fallback state as the single fallback row — "Search unavailable" heading, "Showing YouTube web search instead." body, centered medium systemRed spinner animating; header (Close + "Results from YouTube") persists unchanged. Wire the no-results row's "Try another search" label as a tappable action calling onRetry (17pt+ systemRed action text per the accent reservation). The empty-results row itself is inherited from 04-01 unchanged: keep the total row-count formula (1 row for loading/fallback/.results([]) via the isEmpty predicate, min(results.count, 8) otherwise — the no-results row stays pinned at indexPath 0), 68.0pt rows, and every 04-01 visual token untouched.
  4. No CarPlaySingleton or CarPlayViewController changes this plan — the cap stays spent and onRetry was wired in 04-01 (dismissSearchResults + toggleKeyboard). Verify with a git-diff grep gate.
  5. GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'Search unavailable' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'Showing YouTube web search instead.' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'Try another search' CarTube/CarPlay/SearchResultsViewController.swift && grep -q 'Results from YouTube' CarTube/CarPlay/SearchResultsViewController.swift && [ "$(git diff HEAD~1 -- CarTube/CarPlay/CarPlaySingleton.swift 2>/dev/null | wc -l)" = "0" ] && grep -q 'generation' CarTube/Search/SearchCoordinator.swift && ! grep -rn 'CarPlaySingleton' CarTube/Search/ --include='*.swift' | grep -v 'SearchCoordinator.swift' | grep -q . && ! grep -q 'evaluateJavaScript\|WKNavigationDelegate' CarTube/Search/SearchCoordinator.swift CarTube/CarPlay/SearchResultsViewController.swift</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED with the extended matrix green: degrade-before-edge ordering, fallback auto-dismiss, all four failure kinds, empty success, stale-response discard, plus 04-01's funnel tests still passing, 0 failures
  - Exact copy strings present in source: "Search unavailable", "Showing YouTube web search instead.", "Try another search", "Results from YouTube" (grep gates)
  - CarPlaySingleton.swift and CarPlayViewController.swift have ZERO diff this plan (the 3-method cap and no-resize wiring stand untouched)
  - SearchCoordinator contains a generation/stale guard discarding outcomes from superseded queries; no retry/backoff/prefetch tokens added anywhere in CarTube/Search/
  - Re-scoped boundary gate holds (no CarPlaySingleton outside SearchCoordinator.swift under Search/); zero evaluateJavaScript / WKNavigationDelegate in the new-code files
  - The fallback auto-dismiss is bounded at 2.0 seconds and generation-guarded (grep: .seconds(2))
  </acceptance_criteria>
  <done>Every documented overlay state is real and fixture-proven — failures degrade with the fallback row then auto-dismiss, empty results offer a working retry path, stale responses are discarded, and the driver is never left with a dead screen.</done>
</task>

<task type="auto">
  <name>Phone-side preview harness — drive every overlay state on the simulator without the entitlement</name>
  <files>CarTube/Views/Debug.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>Task 1's states render: SearchResultsViewController supports loading, results(1..8), results([]), and fallback via update(_:).</precondition>
  <read_first>
  - .planning/phases/04-carplay-search-surface/04-UI-SPEC.md — Component Inventory (the exact states to reproduce) and the Interaction Contract rows being visually verified
  - .planning/phases/04-carplay-search-surface/04-PATTERNS.md — closure-variant rationale (sanctioned for exactly this harness) and the keyboard wiring precedent
  - CarTube/Views/Debug.swift — the existing debug-screen button pattern this harness extends (CarPlaySingleton goBack/goHome/toggleKeyboard buttons)
  - CarTube/CarPlay/SearchResultsViewController.swift — the update(_:) state entry point and closure init from 04-01
  - CarTube/Search/SearchCoordinator.swift — the presenter seam the harness overrides
  </read_first>
  <reversibility rating="reversible">a DEBUG-usable debug-screen section; ships behind the existing Debug navigation link — revertible in isolation.</reversibility>
  <action>
  Extend CarTube/Views/Debug.swift with a "Search Overlay Preview" section containing buttons that present a full-screen UIHostingController-free preview: construct SearchResultsViewController with logging closures and call update with each state — .loading; .results with a 1-result fixture array; .results with an 8-result fixture array (synthetic SearchResult values: realistic 11-char videoIds, a long title, a nil-duration row, one thumbnail URL and one nil thumbnail to show the placeholder); .results([]) for the empty row; .fallback. Fixture values are inline test data in the debug file, not network calls — the harness spends zero quota. Present over the current window (full-screen child of the key window's root, isHidden toggling not needed for a modal preview — a plain modal or window-level presentation both fit the existing debug style; pick the smaller one). Add one button that runs the REAL funnel through SearchCoordinator.shared with the presenter seam unmodified — this button is execution-proof, not visual: on the phone the default presenter routes to CarPlaySingleton.shared.showSearchResults, which no-ops with no CarPlay controller, so the proof is the Xcode console/request log (live requests, cache-hit behavior, funnel trace), never rendered rows; the state previews above carry the visuals. Run it only when a dev key is configured (Phase 3 runbook); otherwise show an alert that the funnel needs the key and skip the call. Keep it consistent with the debug screen's existing plain-button style; no new settings, no production-surface changes.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'Search Overlay Preview' CarTube/Views/Debug.swift && grep -c 'update(' CarTube/Views/Debug.swift | grep -qv '^0$'</automated>
  </verify>
  <acceptance_criteria>
  - App builds (BUILD SUCCEEDED) with the harness in place
  - Debug screen contains a "Search Overlay Preview" section with state buttons covering loading, 1 result, 8 results (including a nil-duration row and a nil-thumbnail row), empty, and fallback
  - The harness constructs the production SearchResultsViewController (same class the CarPlay scene hosts) — verified by grep for its type name in Debug.swift
  - The real-funnel button guards on key availability (no quota spend from a bare tap; alert or skip when unconfigured)
  - Zero production-file diffs outside Debug.swift (git diff gate)
  </acceptance_criteria>
  <done>The full overlay state matrix is visually drivable on the phone simulator today, using the same controller and coordinator the CarPlay scene will host when the entitlement lands.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Visual verification — every overlay state on the simulator (entitlement contingency applied)</name>
  <what-built>The full CarPlay search results overlay: populated rows (thumbnail/title/channel/duration), loading, empty with retry, fallback with auto-dismiss, persistent header with attribution, tap-to-play, and the phone-side preview harness driving every state.</what-built>
  <read_first>
  - .planning/phases/04-carplay-search-surface/04-UI-SPEC.md — the Copywriting Contract and Component Inventory being eyeballed
  - CarTube/Views/Debug.swift — where the harness buttons live
  </read_first>
  <action>
  Human verifies visually. PRIMARY PATH (entitlement granted — check STATE.md's Phase 1 blocker note or the provisioning profile state): run the app on the simulator with the CarPlay scene active; open the keyboard, type a query, submit, and walk: loading row → results (check ≤8 rows, thumbnail quality, title/channel/duration legibility) → tap a row (plays, overlay dismisses, playback never resized or reloaded) → Close during playback (overlay dismisses, playback unchanged) → force a fallback (dev key absent or quota spent: submit and observe "Search unavailable" + webview search + ≤2s auto-dismiss). CONTINGENCY PATH (entitlement still pending): use the Debug screen's "Search Overlay Preview" section — walk loading, 1 result, 8 results, empty (tap "Try another search"), fallback, and confirm "Results from YouTube" is visible in every state; then report which path was used so the on-device pass can be scheduled at the grant (tracked by Phase 1's dated blocker).
  </action>
  <how-to-verify>
  1. Build and run on the iPhone simulator (xcodebuild build then run, or Xcode Run).
  2. Primary: open the CarPlay simulator window; contingency: phone app → Debug → Search Overlay Preview.
  3. Loading: submit a query — "Searching…" spinner row appears immediately with the header visible.
  4. Populated: rows are large and glanceable (68pt), thumbnails load, one tap plays, overlay dismisses, video never restarts or resizes beneath.
  5. Close during active playback — playback continues unchanged.
  6. Empty: clear/odd query or harness empty state — "No results" row; "Try another search" reopens the keyboard (CarPlay path) or logs retry (harness).
  7. Fallback: unconfigured/invalid key — "Search unavailable" row with spinner, webview navigates to search, overlay auto-dismisses within ~2s.
  8. Attribution "Results from YouTube" visible top-right in every state.
  </how-to-verify>
  <resume-signal>Type "approved" (plus which path was used), or describe visual defects (state, copy string, what looked wrong)</resume-signal>
  <acceptance_criteria>
  - The chosen path is recorded in the SUMMARY (primary on-scene vs contingency harness + dated entitlement status)
  - Every state renders with its exact copy strings and the attribution visible in all of them
  - One tap plays and dismisses; playback beneath is never resized or reloaded (primary path) or the production wiring is otherwise proven by the unit matrix (contingency)
  - Any visual defects are filed as concrete state+string descriptions, not vague notes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| failure outcomes → overlay state | API error kinds map to a single fallback presentation; no error detail text crosses into UI |
| debug harness → production coordinator | the harness may run the real funnel with a dev key; a bare tap must never spend quota |
| stale async responses → presentation | superseded query outcomes must not overwrite a newer state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Tampering | stale-response presentation (coordinator generation guard) | medium | mitigate | generation-token discard proven by the stale-response fixture test — an old response can never overwrite a newer query's state |
| T-04-07 | Information Disclosure | error detail leakage into UI | low | mitigate | fallback copy is fixed strings; SearchError payloads are never interpolated into labels |
| T-04-08 | Denial of Service (quota) | debug harness funnel button auto-spending | medium | mitigate | key-availability guard before the real funnel call; state previews are synthetic in-memory values with zero network |
| T-04-09 | Denial of Service | fallback timer dismissing a newer overlay | low | mitigate | auto-dismiss task is generation-guarded; dismiss fires exactly once per degrade (test-asserted) |
| T-04-SC | Tampering | package installs | low | accept | no package installs |
</threat_model>

<verification>
- Task 1 automated: extended coordinator matrix green; copy-string greps; zero diff on CarPlaySingleton/CarPlayViewController; generation guard present; boundary and no-JS-eval gates hold
- Task 2 automated: build succeeds; harness section and state buttons present; zero production-file diffs outside Debug.swift
- Checkpoint: chosen verification path recorded; all states eyeballed with exact copy; single-tap play/dismiss confirmed (or unit-matrix proof under contingency)
- Phase gate: every UI-01..03 success criterion maps to a green test, a grep gate, or a recorded visual observation
</verification>

<success_criteria>
- UI-01 complete: never-a-dead-screen — every failure degrades to the webview search with the fallback row and bounded auto-dismiss; one tap plays; webview untouched
- UI-02 complete: the funnel handles typed input's full state space; zero new singleton surface (cap intact at three)
- UI-03 proven in all states: attribution persists through loading, empty, and fallback
- Verification is entitlement-independent via the harness, with the on-device pass scheduled at the entitlement grant (Phase 1 dated blocker)
</success_criteria>

## Artifacts this phase produces (plan 02)

- SearchResultsViewController — the complete state contract: loading, results(≤8), empty with wired retry, fallback with spinner; every UI-SPEC copy string in place
- SearchCoordinator — generation-guarded outcomes, fallback-before-degrade ordering, 2.0s generation-guarded auto-dismiss (Phase 5's voice inputs reuse search(_:) unchanged)
- CarTubeTests/SearchCoordinatorTests.swift — the full funnel matrix: happy, cache-hit, four failure kinds, empty success, stale discard, ordering, dismissal-once
- CarTube/Views/Debug.swift — "Search Overlay Preview" harness (every state drivable on the phone simulator; real-funnel button key-guarded)
- Verification record: the checkpoint's chosen path + dated entitlement status in the SUMMARY (feeds Phase 6's re-verification and Phase 5's landing checks)

<output>
Create .planning/phases/04-carplay-search-surface/04-02-SUMMARY.md when done
</output>
