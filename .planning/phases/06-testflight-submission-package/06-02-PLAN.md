---
phase: 06-testflight-submission-package
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/submission/app-review-notes.md
  - docs/submission/app-store-metadata.md
  - docs/submission/fallback-ladder.md
autonomous: true
requirements: [SHIP-01, SHIP-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - App Review notes describe the CarPlay webview mechanism specifically (scene → delegate → WKWebView loading m.youtube.com with injected scripts) and state the audio-category positioning honestly — enough that guideline 2.3.1 has nothing hidden to find
    - App Store metadata copy (name, subtitle, description, keyword field) contains zero ad-blocking, SponsorBlock, or age-restriction-bypass claims — the pairing an auditor notices first per PITFALLS Pitfall 8
    - The fallback ladder documents rung 0 (webview survives), rung 1 (parked-gate variant, with its core-value trade-off named), and rung 2 (audio-UI variant), each with trigger guideline, what changes, and what it costs
    - The reconciliation between the Phase 1 entitlement-request wording (which deliberately omitted video vocabulary) and the review notes (which must disclose it) is stated explicitly in the notes document — two documents, two contracts
    - In-app settings toggle names are documented as disclosed-not-hidden per 2.3.1, with the out-of-scope position (no in-app rename) recorded rather than implied
  artifacts:
    - docs/submission/app-review-notes.md with REVIEW-NOTES-BEGIN/REVIEW-NOTES-END paste block
    - docs/submission/app-store-metadata.md with METADATA-BEGIN/METADATA-END paste block
    - docs/submission/fallback-ladder.md with rungs 0–2 and the reusability guarantee
  key_links:
    - review notes disclosure ↔ actual mechanism in code (CarPlaySceneDelegate → CarPlayViewController → WKWebView → m.youtube.com + atDocumentEnd scripts) — notes must match the binary 06-03 uploads
    - fallback-ladder rung triggers ↔ PITFALLS Pitfall 10's four rejection vectors (template rules, 4.2, 5.2.2/5.2.3, 2.3.1)
    - metadata honesty gate ↔ Phase 1 runbook grep-verify convention (marker-scoped sed extraction)
---

<objective>
Produce the three submission deliverables (SHIP-01, SHIP-02): honest App Review notes under the audio category, ad-block-free App Store metadata, and a documented fallback ladder that gives a webview rejection a pre-decided next move.

Purposes: PITFALLS Pitfall 10 — a bare upload with generic notes burns reviewer goodwill; a coherent package makes the webview surface the only rejection reason and makes any rejection actionable. The Phase 1 entitlement request deliberately omitted video vocabulary (its wording gate); the review notes must disclose the video surface (2.3.1's requirement) — this plan reconciles those two contracts explicitly instead of copying request text into notes.
Output: Three committed docs under docs/submission/, each with machine-extractable paste blocks and grep-verifiable wording gates.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testflight-submission-package/06-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/phases/01-external-dependencies-project-setup/01-02-PLAN.md
@CarTube/Views/HowTo.swift
</context>

<tasks>

<task type="auto">
  <name>Author honest App Review notes with 2.3.1 disclosure + wording-contract reconciliation</name>
  <files>docs/submission/app-review-notes.md</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "docs/submission/app-review-notes.md (docs — SHIP-01)" section: content anchors (scene manifest → delegate → WKWebView → m.youtube.com → atDocumentEnd scripts), the audio-category honest framing, and the explicit tension to reconcile with Phase 1 request wording
  - .planning/research/PITFALLS.md — Pitfall 10 (the four rejection vectors and the review-notes strategy), Security Mistakes and "Looks Done But Isn't" (submission package row)
  - .planning/phases/01-external-dependencies-project-setup/01-02-PLAN.md — Task 1: the REQUEST-TEXT marker convention and its negative wording gate — the contract this document must NOT inherit blindly
  - CarTube/Info.plist — UIApplicationSceneManifest CarPlay scene declaration (the mechanism the notes describe)
  - CarTube/Views/HowTo.swift — the app's current user-facing voice (register reference for notes tone)
  </read_first>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <action>
  Create docs/submission/app-review-notes.md following the Phase 1 runbook convention: prerequisites section (team U67AKNW8PW, the confirmed bundle ID from STATE.md, the audio entitlement category as ground truth), then a paste-ready reviewer-notes block delimited by literal marker lines REVIEW-NOTES-BEGIN and REVIEW-NOTES-END.

  The block between the markers describes the mechanism specifically, per 2.3.1: the app registers a CarPlay window scene via the scene manifest; CarPlaySceneDelegate installs CarPlayViewController, which hosts a WKWebView loading the YouTube mobile site (m.youtube.com) with feature scripts injected at document end; playback reaches the vehicle through its audio system; the phone app is the launcher/settings editor and a share extension opens videos via the cartube:// scheme. The disclosure names the injected feature scripts honestly (layout adjustments, ad filtering, sponsor-segment skipping, age-gate handling — matching the Settings toggle names that ship visibly in-app) because disclosed in-app features described honestly are not hidden functionality under 2.3.1. State the positioning plainly: the app is submitted under the audio category for playback of YouTube content through the vehicle's system, and the video surface carries a known residual review risk the developer has accepted (PROJECT.md Key Decision).

  Outside the markers, include the reconciliation section required by the pattern map: name explicitly that the Phase 1 entitlement request text deliberately avoided on-screen-viewing and driver-context vocabulary (per its own negative wording gate), while these notes deliberately disclose the video surface — the request states intended purpose; the review notes disclose implementation; both are honest for their respective contracts and neither text was copied into the other. Include a second, separate note documenting the in-app toggles position: Settings ships visible toggles named for the feature scripts; they are disclosed functionality, not hidden; renaming them is out of this phase's scope (no roadmap criterion requires it).
  </action>
  <verify>
    <automated>grep -q 'REVIEW-NOTES-BEGIN' docs/submission/app-review-notes.md && grep -q 'REVIEW-NOTES-END' docs/submission/app-review-notes.md && sed -n '/REVIEW-NOTES-BEGIN/,/REVIEW-NOTES-END/p' docs/submission/app-review-notes.md | grep -q 'WKWebView' && sed -n '/REVIEW-NOTES-BEGIN/,/REVIEW-NOTES-END/p' docs/submission/app-review-notes.md | grep -q 'm.youtube.com' && sed -n '/REVIEW-NOTES-BEGIN/,/REVIEW-NOTES-END/p' docs/submission/app-review-notes.md | grep -qi 'audio' && grep -q '2.3.1' docs/submission/app-review-notes.md && grep -qi 'reconcil' docs/submission/app-review-notes.md && grep -q 'U67AKNW8PW' docs/submission/app-review-notes.md</automated>
  </verify>
  <acceptance_criteria>
  - Both marker lines present; the extracted block contains WKWebView, m.youtube.com, the audio category, and the injected-scripts disclosure (each greppable)
  - The block describes the scene → delegate → view controller → webview chain specifically (no generic webview-wrapper phrasing) and references guideline 2.3.1
  - The reconciliation section exists outside the markers and names the request-notes wording difference explicitly
  - Prerequisites section names team U67AKNW8PW and the confirmed bundle ID
  - No section instructs copying REQUEST-TEXT content into the review notes
  </acceptance_criteria>
  <done>Honest, specific, audio-positioned review notes committed with an extractable paste block and the wording-contract reconciliation on record.</done>
</task>

<task type="auto">
  <name>Author App Store metadata with negative wording gate on ad-block vocabulary</name>
  <files>docs/submission/app-store-metadata.md</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "docs/submission/app-store-metadata.md (docs — SHIP-01)" section: the METADATA marker gate command, the in-app-copy-is-not-metadata position to state, and the HowTo.swift register reference
  - .planning/research/PITFALLS.md — Pitfall 8 warning sign (ad-block marketing × registered Data API key pairing) and Pitfall 10 (metadata honesty; 2.3.7/5.2.1 name/icon branding constraints)
  - CarTube/Views/HowTo.swift — the existing app voice to write copy in
  - CarTube/Views/Settings.swift — the visible toggle names the doc's disclosure position references
  </read_first>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <action>
  Create docs/submission/app-store-metadata.md with a METADATA-BEGIN/METADATA-END marker block containing the paste-ready store copy: app name, subtitle, promotional description, description body, and keyword field. Write in the HowTo.swift register — describes opening and playing YouTube content on the car screen through the vehicle's system, voice and on-screen search, share-extension entry — with zero references to the enhancement scripts' effects anywhere in any store-facing field. Keep name/subtitle free of YouTube trademark branding claims (2.3.7/5.2.1: no naming that reads as an official YouTube product).

  Outside the markers, document the position: App Store metadata is a different surface from in-app copy; the Settings toggles remain named as they ship because disclosed features honestly described in review notes are not hidden functionality (2.3.1 concerns hidden/undocumented functionality); the metadata gate exists because marketing enhancement effects reads as ToS-violation advertising to reviewers and to YouTube brand protection (Pitfall 8's pairing risk with the registered Data API key). Note which guideline sections constrain the name/icon (2.3.7, 5.2.1) and that screenshots must follow the same honesty rule.
  </action>
  <verify>
    <automated>grep -q 'METADATA-BEGIN' docs/submission/app-store-metadata.md && grep -q 'METADATA-END' docs/submission/app-store-metadata.md && [ "$(sed -n '/METADATA-BEGIN/,/METADATA-END/p' docs/submission/app-store-metadata.md | grep -icE 'ad[- ]?block|sponsorblock|sponsor|age[- ]?restrict|bypass')" = "0" ] && grep -q '2.3.7' docs/submission/app-store-metadata.md</automated>
  </verify>
  <acceptance_criteria>
  - Marker block present and extractable; the sed-extracted block matches zero case-insensitive hits for the enhancement-effect vocabulary (ad-block, SponsorBlock, sponsor, age-restrict, bypass patterns)
  - Copy includes name, subtitle, description, and keyword fields, written in the app's existing voice
  - The disclosure position section exists (in-app toggles stay named; metadata is the gated surface) and cites 2.3.7 for name/icon constraints
  - The doc records that screenshots follow the same gate
  </acceptance_criteria>
  <done>Store copy committed that markets the experience without the ToS-collision vocabulary, with the position and gates documented.</done>
</task>

<task type="auto">
  <name>Author fallback ladder — rungs 0/1/2 with triggers, costs, and reusability guarantee</name>
  <files>docs/submission/fallback-ladder.md</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "docs/submission/fallback-ladder.md (docs — SHIP-02)" section: the four-rung structure (including documenting rung 1's core-value contradiction as an accepted trade-off), the trigger guidelines per rung, and the decoupling guarantee
  - .planning/research/PITFALLS.md — Recovery Strategies row "Rejected on 4.2/5.2.3" and Pitfall 10's four rejection vectors; Technical Debt Patterns (search kept decoupled)
  - .planning/ROADMAP.md — Phase 4 criterion 3 (SearchCoordinator funnel) and Phase 5 (voice lands on the same funnel) — the architectural basis of the reusability guarantee
  </read_first>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <action>
  Create docs/submission/fallback-ladder.md in runbook style. Structure as four rungs, each with: the specific rejection guideline that triggers it, what changes, what it costs, and estimated effort class:

  Rung 0 — webview survives: submission passes; no action; this is the primary outcome given the accepted-risk Key Decision.
  Rung 1 — parked-gate variant: triggered by CarPlay template-violation or video-while-driving rejections (CarPlay guideline 7, the 2026 video category's parked-only shape). What changes: gate the video surface on CarPlay driving/parked state. Cost: name the contradiction explicitly — gating video to parked contradicts the milestone's Core Value (a driver opens videos while driving); document it as the accepted trade-off of this rung, not a plan to implement now.
  Rung 2 — audio-UI variant: triggered by 4.2 (repackaged website) or 5.2.2/5.2.3 (YouTube content/ToS) rejections that survive rung 1. What changes: replace the raw webview scene with template-based CarPlay audio surface. Cost: the largest rework of the ladder.
  Under every rung: search and voice assets are reusable because Phases 3–5 kept them decoupled behind SearchCoordinator and the voice funnel — no rung discards them. Also include a general row for 2.3.1 (notes inadequate): response is to sharpen the notes, not change the app — pointing back at the review-notes deliverable.
  </action>
  <verify>
    <automated>grep -qi 'rung 0' docs/submission/fallback-ladder.md && grep -qi 'rung 1' docs/submission/fallback-ladder.md && grep -qi 'rung 2' docs/submission/fallback-ladder.md && grep -qi 'parked' docs/submission/fallback-ladder.md && grep -q '4.2' docs/submission/fallback-ladder.md && grep -q '5.2.3' docs/submission/fallback-ladder.md && grep -q 'SearchCoordinator' docs/submission/fallback-ladder.md && grep -qi 'core value' docs/submission/fallback-ladder.md</automated>
  </verify>
  <acceptance_criteria>
  - All three rungs present, each naming its trigger guideline, what changes, and what it costs
  - Rung 1 documents the parked-gating/core-value contradiction as an accepted trade-off
  - 4.2 and 5.2.3 trigger guidelines cited; the reusability guarantee references SearchCoordinator decoupling
  - The ladder prescribes no immediate implementation — every rung is contingency documentation
  </acceptance_criteria>
  <done>A webview rejection has a pre-decided, documented next move at every trigger guideline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submission docs ↔ App Review | reviewer-facing disclosure; under-disclosure risks 2.3.1, over-claiming risks 2.3.7/5.2.1 |
| metadata ↔ YouTube brand protection | store copy is public marketing; enhancement-effect claims create ToS-collision exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Repudiation | review-notes honesty (2.3.1) | high | mitigate | Notes describe the exact shipped mechanism (scene → WKWebView → m.youtube.com → injected scripts) with the audio positioning stated; grep gates pin WKWebView/m.youtube.com/audio presence |
| T-06-05 | Information Disclosure | metadata advertising ToS-violating features | high | mitigate | METADATA marker block negative-gated for enhancement-effect vocabulary (the Pitfall 8 pairing risk); position section documents why in-app toggles are exempt |
| T-06-06 | Repudiation | request-vs-notes wording drift | medium | mitigate | Reconciliation section states both contracts explicitly; acceptance criterion forbids copying REQUEST-TEXT into the notes |
| T-06-07 | Denial of Service (review) | rejection without a pre-decided response | medium | mitigate | Fallback ladder maps every Pitfall 10 vector to a rung with trigger/cost/trade-off; reusability guarantee documented |
| T-06-SC | Tampering | package installs | low | accept | documentation only; no installs |
</threat_model>

<verification>
- Task 1: marker block extractable; WKWebView + m.youtube.com + audio + 2.3.1 + reconciliation greps green
- Task 2: METADATA block zero-hits on enhancement-effect vocabulary; 2.3.7 cited
- Task 3: three rungs + triggers + SearchCoordinator + core-value contradiction greps green
</verification>

<success_criteria>
- Review notes disclose the webview mechanism specifically and honestly under the audio category
- Metadata carries no enhancement-effect marketing anywhere in store-facing fields
- Fallback ladder documents rungs 0–2 with triggers and costs; search/voice reusability guaranteed
- The Phase 1 request-wording vs Phase 6 notes-wording reconciliation is on record
</success_criteria>

## Artifacts this plan produces

- docs/submission/app-review-notes.md — prerequisites (team, bundle ID, category), REVIEW-NOTES-BEGIN/END paste block (mechanism disclosure), reconciliation section, in-app-toggles position
- docs/submission/app-store-metadata.md — METADATA-BEGIN/END paste block (name/subtitle/description/keywords, zero enhancement-effect vocabulary), disclosure position, 2.3.7/5.2.1 constraints note
- docs/submission/fallback-ladder.md — rungs 0–2 with trigger guidelines, changes, costs, trade-offs, SearchCoordinator reusability guarantee
- Wording-gate literals REVIEW-NOTES-BEGIN/REVIEW-NOTES-END and METADATA-BEGIN/METADATA-END (used by the grep gates; mirrors Phase 1's REQUEST-TEXT convention)

<output>
Create .planning/phases/06-testflight-submission-package/06-02-SUMMARY.md when done
</output>
