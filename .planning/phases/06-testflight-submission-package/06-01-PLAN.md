---
phase: 06-testflight-submission-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CarTube/CarTubeApp.swift
  - CarTube/Views/ContentView.swift
  - CarTube/Util/Constants.swift
  - CarTubeTests/GitHubReleaseTests.swift
  - CarTube.xcodeproj/project.pbxproj
autonomous: true
requirements: [SHIP-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - The GitHub update check decodes a typed GitHubRelease struct (Codable) only after an HTTPURLResponse status check, and the alert body contains only version information plus fixed local copy — never remote text
    - The update-check URLs point at this fork's releases (phuongddx/CarTube), not the upstream repo, and live in Constants.swift as SCREAMING_SNAKE_CASE constants beside the existing YT_ constants
    - The pasteboard read on scene activation no longer exists anywhere in the app target, and pasting happens only through an explicit user-tapped PasteButton that still validates with isYouTubeURL before filling the field
    - grep -r JSONSerialization across CarTube/ Swift sources returns zero hits after this phase (the last dictionary-cast decode site is gone)
    - All decode/validation behavior is covered by unit tests via the existing MockURLProtocol + fixture pattern, and the full test suite passes on the iPhone Air simulator
  artifacts:
    - CarTube/CarTubeApp.swift with hardened checkNewVersions
    - CarTube/Views/ContentView.swift with PasteButton replacing the onChange(of: scenePhase) activation read
    - CarTube/Util/Constants.swift with GITHUB_RELEASES_API_URL / GITHUB_RELEASES_PAGE_URL constants
    - CarTubeTests/GitHubReleaseTests.swift with fixture-driven decode + gating tests
  key_links:
    - Constants.swift GITHUB_RELEASES_API_URL → CarTubeApp.checkNewVersions request URL → typed decode → confirmAlert display (fixed copy only)
    - ContentView PasteButton payload callback → isYouTubeURL check → urlString state → existing playVideo() path (untouched downstream)
---

<objective>
Close the two long-standing hardening items (SHIP-03) as the phase's tracer — real code, building and testing green before any submission docs exist: (1) the GitHub update check becomes a typed, status-gated decode that renders no remote text, and (2) the pasteboard read on activation is replaced by an explicit PasteButton interaction.

This is the tracer for Phase 6: it touches every code layer Phase 6 modifies (Constants → CarTubeApp networking → ContentView UI → CarTubeTests) and proves the phase's code changes build and test green under the existing scan gate before the docs deliverables (06-02) and archive (06-03) build on top.

Purpose: PITFALLS Security Mistakes table names both items — the unvalidated GitHub response interpolated into an alert (mitM/injection surface) and the activation-time pasteboard read (privacy banner + reviewer scrutiny). Fixing them here means the upload in 06-03 carries no known non-webview rejection reasons.
Output: Hardened update check + explicit paste interaction, both under unit test, suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testflight-submission-package/06-PATTERNS.md
@.planning/phases/03-search-core/03-02-PLAN.md
@CarTube/CarTubeApp.swift
@CarTube/Views/ContentView.swift
@CarTube/Util/Constants.swift
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>RED/GREEN typed GitHubRelease decode + status gate + fixed-copy alert</name>
  <files>CarTubeTests/GitHubReleaseTests.swift, CarTubeTests/Fixtures/github-release.json, CarTube/CarTubeApp.swift, CarTube/Util/Constants.swift, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "CarTube/CarTubeApp.swift (service/entry...)" section: the four weaknesses to close (ignored response, try? swallow, dictionary-cast, remote body interpolation), the replacement shape, and the URL-constant decision points
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "Shared Patterns": typed network decode (Codable never dictionary-cast), URL constants (SCREAMING_SNAKE_CASE, no inline URL(string:) literals in Swift bodies), alert routing (confirmAlert already main-dispatched — no extra hop)
  - .planning/phases/03-search-core/03-02-PLAN.md — Task structure: fixture authoring first, Bundle(for:) fixture loading, MockURLProtocol injection, pbxproj mutation via throwaway ruby xcodeproj gem script (delete after), and the xcodebuild test verify command shape
  - CarTube/CarTubeApp.swift — current checkNewVersions (the only URLSession call site in the app; keep the dataTask/resume/confirmAlert shape)
  - CarTube/Util/Constants.swift — YT_HOME/YT_EMBED/YT_SEARCH naming block the GitHub constants join
  - CarTubeTests/MockURLProtocol.swift — the Phase 3 protocol this plan's tests compose with
  </read_first>
  <precondition>Phase 3 plan 03-02 has executed: CarTubeTests target exists with MockURLProtocol.swift and the Fixtures/ resource-loading convention. If the test target is missing, halt — this plan depends on that infra.</precondition>
  <reversibility rating="reversible">Pure code change in two Swift files + tests; git revert restores prior behavior.</reversibility>
  <behavior>
  - Test 1 (decode): valid GitHub releases/latest JSON with tag_name v9.9.9 and a body decodes to a GitHubRelease with tagName "v9.9.9"
  - Test 2 (status gate): an HTTPURLResponse with statusCode 404 paired with a body decodes to nil from the gating function — non-200 never produces a release even when the body is valid JSON
  - Test 3 (newer-version detection): tagName v9.9.9 compared against installed version 1.0.0 with .numeric options yields orderedDescending (the display predicate)
  - Test 4 (schema drift): JSON missing tag_name fails decode to nil — no crash, no partial dictionary fallback
  - Test 5 (alert copy): the fixed alert-body string for the update prompt is a local constant; the gating path never interpolates release.body into any user-facing string (assert the display-copy builder ignores the decoded body field)
  </behavior>
  <action>
  Resolve the PATTERNS decision points as follows (defaults, per pattern-mapper recommendations — record them in the SUMMARY):
  - Harden, do not remove: ROADMAP criterion 3 requires validation, so the check stays and becomes validated. Removal is the rejected alternative.
  - Point at this fork, not upstream: the repo's origin is the fork phuongddx/CarTube; upstream Avangelista releases never match App Store builds, so the check is re-targeted at the fork's releases API + releases page.
  - Drop remote body interpolation: the alert shows version info plus fixed local copy only; the decoded body field exists on the struct (schema completeness) but no user-facing string interpolates it.

  RED first: create CarTubeTests/Fixtures/github-release.json (minimal real-shaped releases/latest response: tag_name, name, body) and CarTubeTests/GitHubReleaseTests.swift with the five behavior tests above. The tests reference the to-be-created GitHubRelease type and gating function, so the build fails RED. Fixture loading follows the Phase 3 convention (Bundle(for:) + path(forResource:ofType:)); network tests inject MockURLProtocol — no live network in tests.

  GREEN: in CarTube/CarTubeApp.swift add the GitHubRelease Codable struct (tagName/body with tag_name CodingKey) and rewrite checkNewVersions to: read the URL from the new constant; in the dataTask completion, guard response is HTTPURLResponse with statusCode 200; decode via JSONDecoder to GitHubRelease (a failed decode returns silently — the check is best-effort); compare tagName against the bundle version with .numeric orderedDescending; on success show the existing confirmAlert with fixed local copy naming the new version, no remote text in any interpolated position. Keep the URLSession.shared.dataTask + task.resume() shape and the confirmAlert call unchanged in style. Add GITHUB_RELEASES_API_URL (api.github.com/releases/latest for the fork) and GITHUB_RELEASES_PAGE_URL (github.com releases/latest page for the fork) to Constants.swift beside the YT_ constants; remove the inline URL string literals from CarTubeApp.swift so no GitHub URL literal remains in a Swift body outside Constants.swift. Add the test file + fixture to the CarTubeTests target via the Phase 3 throwaway ruby xcodeproj gem recipe (delete the script after; re-grep pbxproj line numbers first — Phases 2–5 shifted them).
  <!-- planner-discipline-allow: Avangelista -->
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'struct GitHubRelease' CarTube/CarTubeApp.swift && grep -q 'GITHUB_RELEASES_API_URL' CarTube/Util/Constants.swift && ! grep -q 'Avangelista' CarTube/CarTubeApp.swift && ! grep -rq --include='*.swift' 'JSONSerialization' CarTube/</automated>
  </verify>
  <acceptance_criteria>
  - Full suite passes (TEST SUCCEEDED), including the five new GitHubReleaseTests cases
  - CarTube/CarTubeApp.swift declares struct GitHubRelease with tag_name CodingKey mapping; the completion path checks HTTPURLResponse statusCode before decoding
  - No user-facing string in CarTubeApp.swift interpolates the decoded body field (grep for body usage inside string interpolation returns zero in display-copy positions)
  - Both GitHub URLs live in Constants.swift as SCREAMING_SNAKE_CASE; grep Avangelista across CarTube/ Swift sources returns zero (fork retarget verified)
  - Zero JSONSerialization hits across all app-target Swift sources — the last dictionary-cast decode is gone
  - project.pbxproj lists GitHubReleaseTests.swift in the test target and github-release.json in test resources
  </acceptance_criteria>
  <done>Update check decodes typed, status-gated, fork-targeted, remote-text-free; suite green; committed.</done>
</task>

<task type="auto">
  <name>Replace activation pasteboard read with explicit PasteButton</name>
  <files>CarTube/Views/ContentView.swift</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "CarTube/Views/ContentView.swift (component...)" section: the exact onChange(of: scenePhase) block to delete, the scenePhase @Environment line dying with it, both replacement options, and the downstream-path guarantee (urlString → playVideo() → extractYouTubeVideoID → YT_EMBED untouched)
  - CarTube/Views/ContentView.swift — the URL-input Section placement template and the two scenePhase use sites
  </read_first>
  <reversibility rating="reversible">Single-file UI change; git revert restores the activation read.</reversibility>
  <action>
  Resolve the PATTERNS decision point: PasteButton (option a) over an explicit Button (option b) — the pattern-mapper recommendation, since PasteButton is the system paste affordance (no pasteboard code in app logic at all, no paste-permission banner) and deployment target is iOS 16 after Phase 2.

  Delete the onChange(of: scenePhase) modifier containing the UIPasteboard read, and delete the @Environment(\.scenePhase) declaration (its only two use sites are that modifier and the declaration itself). In the URL-input Section (beside the TextField and Play button), add a PasteButton with payloadType String.self whose payload callback takes the first pasted string and assigns it to urlString only when isYouTubeURL(pasted) is true. Note PasteButton supplies its own label and tint styling — do not fight it with Button styles; place it as a row in the Section. Nothing downstream changes: urlString, playVideo(), and the invalid-link alert path stay exactly as-is. The deleted block was the only such access site in the repo.
  <!-- planner-discipline-allow: UIPasteboard -->
  <!-- planner-discipline-allow: scenePhase -->
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'PasteButton' CarTube/Views/ContentView.swift && ! grep -rq 'UIPasteboard' CarTube/ && ! grep -q 'scenePhase' CarTube/Views/ContentView.swift && grep -q 'isYouTubeURL' CarTube/Views/ContentView.swift</automated>
  </verify>
  <acceptance_criteria>
  - Build succeeds; PasteButton present in ContentView with String payload type and isYouTubeURL-gated assignment to urlString
  - Zero UIPasteboard references across the repo (activation read fully removed, not relocated)
  - scenePhase gone from ContentView (both the modifier and the environment declaration deleted together)
  - playVideo(), urlString state, and the invalid-link alert are unchanged (diff shows no edits below the paste row)
  </acceptance_criteria>
  <done>Paste is an explicit, user-initiated interaction validated by the existing URL parser; the activation read no longer exists.</done>
</task>

<task type="auto">
  <name>Run scan gate + full test suite as the tracer's end-to-end proof</name>
  <files></files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "scripts/scan-private-apis.sh re-run" section: BUILT_PRODUCTS_DIR derivation and both-binary scan command shapes
  - scripts/scan-private-apis.sh — the gate this task re-runs (no modification; a hit is a Phase 2–5 regression, not a gate change)
  </read_first>
  <precondition>Plan 06-01 Tasks 1–2 committed; Phase 2's scan gate exists at scripts/scan-private-apis.sh with its 15-marker list.</precondition>
  <action>
  Build both targets with the Phase 1 recipe (xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air'), derive BUILT_PRODUCTS_DIR via -showBuildSettings, and run scripts/scan-private-apis.sh against both the app binary (CarTube.app/CarTube) and the share-extension binary (CarTube.app/PlugIns/PlayOnCarTube.appex/PlayOnCarTube). Both must exit 0 — this phase adds no private symbols and must not regress the gate. Then run the full test suite once more. Record the two scan exit codes and the suite result in the SUMMARY as the tracer's proof-of-clean.
  </action>
  <verify>
    <automated>BP=$(xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' -showBuildSettings 2>/dev/null | grep -m1 'BUILT_PRODUCTS_DIR = ' | awk '{ print $3 }') && scripts/scan-private-apis.sh "$BP/CarTube.app/CarTube" && scripts/scan-private-apis.sh "$BP/CarTube.app/PlugIns/PlayOnCarTube.appex/PlayOnCarTube"</automated>
  </verify>
  <acceptance_criteria>
  - scripts/scan-private-apis.sh exits 0 on the app binary and on the extension binary
  - Full xcodebuild test run reports TEST SUCCEEDED
  - SUMMARY records both scan exit codes and the suite result
  </acceptance_criteria>
  <done>The hardened code is scan-clean and test-green on both binaries — the tracer is proven before docs and archive build on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API response → app UI | remote content crossing into an alert; previously unvalidated and interpolated |
| user clipboard → app state | pasteboard read on app activation without explicit user initiation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | GitHub update-check response rendered in alert | high | mitigate | Typed Codable decode gated on HTTPURLResponse statusCode 200; fixed local alert copy with no remote-text interpolation; HTTPS-only constant URLs; five unit tests pin the gate via MockURLProtocol |
| T-06-02 | Information Disclosure | activation-time general pasteboard read | high | mitigate | Delete the onChange(of: scenePhase) UIPasteboard read entirely; replace with user-tapped PasteButton (system consent, no banner, no pasteboard code in app logic); isYouTubeURL check retained before state fill |
| T-06-03 | Tampering | schema-drift decode failure crash | medium | mitigate | Failed decode returns silently (best-effort check); Test 4 covers missing-tag_name malformed JSON |
| T-06-SC | Tampering | package installs | low | accept | no package installs; ruby xcodeproj gem is an existing local tool used per Phase 3 recipe |
</threat_model>

<verification>
- Task 1: TEST SUCCEEDED with five new tests; GitHubRelease struct + status gate + fork constants + zero JSONSerialization + zero Avangelista greps all green
- Task 2: BUILD SUCCEEDED; PasteButton present; zero UIPasteboard repo-wide; scenePhase removed
- Task 3: both scan-gate runs exit 0; suite green; SUMMARY records proof
</verification>

<success_criteria>
- Update check validates before display: status-gated, typed, fork-targeted, no remote text in any alert
- Paste is explicit user action; activation read gone repo-wide
- Scan gate clean on both binaries; full suite green
</success_criteria>

## Artifacts this plan produces

- CarTube/CarTubeApp.swift — hardened checkNewVersions: GitHubRelease Codable struct (tag_name CodingKey), HTTPURLResponse statusCode gate, JSONDecoder decode, .numeric version compare, fixed-copy confirmAlert
- CarTube/Views/ContentView.swift — PasteButton (String payload, isYouTubeURL-gated) in the URL-input Section; scenePhase environment + activation read deleted
- CarTube/Util/Constants.swift — GITHUB_RELEASES_API_URL / GITHUB_RELEASES_PAGE_URL (fork phuongddx/CarTube), SCREAMING_SNAKE_CASE beside YT_ constants
- CarTubeTests/GitHubReleaseTests.swift + Fixtures/github-release.json — five behavior tests via MockURLProtocol (decode, 404 gate, version predicate, schema drift, no-remote-text copy)
- Decision record (SUMMARY): harden-not-remove; fork-not-upstream URL; remote-body dropped — the three pattern-mapper decision points with their defaults

<output>
Create .planning/phases/06-testflight-submission-package/06-01-SUMMARY.md when done
</output>
