---
phase: 06-testflight-submission-package
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - ExportOptions.plist
  - scripts/release-archive.sh
  - .gitignore
  - docs/submission/upload-runbook.md
  - docs/submission/submission-notes.md
autonomous: false
requirements: [SHIP-01, SHIP-03]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - A Release archive of app + share extension is produced under standard signing with the real team identity — nothing stripped, no ldid, no TrollStore path anywhere in the pipeline
    - scripts/scan-private-apis.sh exits 0 on the archive's app binary AND the archive's .appex binary (the final scan-clean proof on Release artifacts, not Debug simulator products)
    - The exported .ipa uploads to App Store Connect via xcrun altool with an ASC API key whose material never touches the repo (gitignored + git grep negative gate, Phase 1 xcconfig hygiene pattern verbatim)
    - The entitlement branch is resolved explicitly: grant landed → grant-wiring runbook executed + CarPlay Simulator verification + profile switch; still pending → archive without the CarPlay capability, upload for TestFlight validation, pending state documented in submission notes
    - Submission notes reference the review-notes and metadata deliverables from 06-02 so the uploaded package is coherent end to end
  artifacts:
    - ExportOptions.plist (method app-store-connect, teamID U67AKNW8PW, signing configuration per the resolved entitlement branch)
    - scripts/release-archive.sh (set -e, archive → export → scan gate on both Release binaries)
    - docs/submission/upload-runbook.md (ASC key creation, env/xcconfig wiring, upload commands)
    - docs/submission/submission-notes.md (branch record, scan proof, deliverable cross-references)
  key_links:
    - xcodebuild archive → exportArchive via ExportOptions.plist → exported .ipa → altool upload → App Store Connect TestFlight
    - resolved entitlement branch → ExportOptions.plist signing style → pbxproj provisioning state (grant-wiring runbook's automatic-signing-off step)
    - ASC key material → environment/xcconfig outside git → altool --apiKey/--apiIssuer arguments (never inline key material)
---

<objective>
Produce the final TestFlight-ready archive and upload (SHIP-03 criterion 4): standard-signing Release archive of app + share extension, scan-gated on the Release binaries, exported and uploaded under an App Store Connect API key with Phase 1-grade secret hygiene, with the entitlement contingency branched explicitly at a human checkpoint.

Purpose: the milestone definition of done is a TestFlight-ready upload with the webview surface as the only remaining rejection reason — 06-01 removed the hardening debt, 06-02 produced the honest submission docs, and this plan packages and ships the result. The entitlement grant may or may not have landed by now; the plan resolves that branch as a checkpoint rather than assuming.
Output: Scan-clean signed .ipa in App Store Connect, committed ExportOptions/release script/runbook/submission notes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testflight-submission-package/06-PATTERNS.md
@.planning/phases/06-testflight-submission-package/06-01-SUMMARY.md
@.planning/phases/06-testflight-submission-package/06-02-SUMMARY.md
@.planning/phases/01-external-dependencies-project-setup/01-02-PLAN.md
@.planning/phases/02-severance-signing-modernization/02-02-PLAN.md
@CarTube/CarTube.entitlements
</context>

<tasks>

<task type="auto">
  <name>Resolve entitlement branch and wire signing state</name>
  <files>ExportOptions.plist, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "Entitlement-era provisioning switch (conditional...)" section: the grant-landed branch (execute the Phase 1 runbook verbatim — never re-derive), the pbxproj signing sites (CODE_SIGN_STYLE / DEVELOPMENT_TEAM across four target configs), and the manual-signing PROVISIONING_PROFILE_SPECIFIER addition via throwaway ruby xcodeproj gem script
  - .planning/phases/01-external-dependencies-project-setup/01-02-PLAN.md — Task 1: the grant-wiring runbook's prescribed sequence (portal capability → profile → import → com.apple.developer.carplay-audio → automatic signing OFF → CODE_SIGN_ENTITLEMENTS check → CarPlay Simulator verify)
  - .planning/STATE.md — Blockers/Concerns: the dated entitlement record (submission date, bundle ID, runbook pointer) whose status this task resolves
  - CarTube/CarTube.entitlements — post-Phase-2 state (only grantable keys; the runbook executes without its fallback branch)
  </read_first>
  <precondition>Phases 2–5 complete: entitlements file holds only grantable keys (INFRA-02), deployment target 16.0, scan gate present. Plan 06-01 committed (hardening in the archive).</precondition>
  <reversibility rating="costly">Provisioning/signing state changes pin the archive to one profile configuration; reverting requires re-running the wiring branch. The branch choice itself is checkpointed in Task 2.</reversibility>
  <action>
  Read STATE.md's dated entitlement record and determine the branch. Present the determination to the user in Task 2's checkpoint — do not silently pick.

  GRANT LANDED branch: execute docs/runbooks/carplay-entitlement-grant-wiring.md verbatim (portal capability enable → new provisioning profile → import → com.apple.developer.carplay-audio true in entitlements → automatic signing OFF via the throwaway ruby xcodeproj gem script adding PROVISIONING_PROFILE_SPECIFIER per config at the four CODE_SIGN_STYLE sites → confirm CODE_SIGN_ENTITLEMENTS unchanged). Then verify the CarPlay scene connects in the CarPlay Simulator (download from Additional Tools for Xcode if needed) and record the verification result in the SUMMARY. ExportOptions.plist: method app-store-connect, teamID U67AKNW8PW, signingStyle manual, provisioningProfiles mapping the app + extension bundle IDs to the imported profile names.

  STILL PENDING branch: no capability wiring; archive without the CarPlay capability under the existing standard signing (the entitlements file already contains only grantable-or-empty keys post-Phase-2). ExportOptions.plist: method app-store-connect, teamID U67AKNW8PW, signingStyle automatic. The pending state is documented in Task 4's submission notes, not silently absorbed.

  Validate the plist: plutil -lint ExportOptions.plist must pass.
  </action>
  <verify>
    <automated>plutil -lint ExportOptions.plist && grep -q 'app-store-connect' ExportOptions.plist && grep -q 'U67AKNW8PW' ExportOptions.plist</automated>
  </verify>
  <acceptance_criteria>
  - ExportOptions.plist lints clean; method app-store-connect; teamID U67AKNW8PW present
  - Grant branch (if taken): runbook executed end to end; pbxproj shows CODE_SIGN_STYLE Manual + PROVISIONING_PROFILE_SPECIFIER via grep; CarPlay Simulator verification result recorded in SUMMARY
  - Pending branch (if taken): signingStyle automatic; no capability wiring performed; STATE.md entitlement record unchanged
  - The branch taken is explicitly stated in the SUMMARY with the evidence (portal state or STATE.md record)
  </acceptance_criteria>
  <done>Signing state matches the real entitlement reality and ExportOptions.plist reflects it exactly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Confirm entitlement branch + App Store Connect API key created</name>
  <what-built>ExportOptions.plist and signing state wired for the branch the agent determined from STATE.md; before archiving, the user confirms the branch is factually correct and provides the upload credential.</what-built>
  <read_first>
  - docs/runbooks/carplay-entitlement-grant-wiring.md (only if the grant-landed branch was wired — confirm the steps completed correctly)
  - .planning/STATE.md Blockers/Concerns (the dated entitlement record)
  </read_first>
  <how-to-verify>
  1. Confirm the branch: has Apple granted the CarPlay entitlement for this App ID (check developer.apple.com/account → Certificates, IDs & Profiles → Identifiers → the App ID → CarPlay capability)? If granted, confirm the CarPlay Simulator verification from Task 1 actually showed the scene connecting. If still pending, confirm the pending branch is correct.
  2. Create the App Store Connect API key for upload: App Store Connect → Users and Access → Integrations → App Store Connect API → Team Keys → Generate API Key with Admin (or App Manager) role. Note the Key ID and Issuer ID shown; download the .p8 exactly once (it cannot be re-downloaded).
  3. Place the .p8 at ~/.private_keys/AuthKey_<KEYID>.p8 (altool's default search path) or set API_PRIVATE_KEYS_DIR to its folder — never inside this repo.
  4. Report back: branch confirmation (granted-with-simulator-proof or still-pending), the Key ID, and the Issuer ID (the .p8 stays on your machine; do not paste its contents).
  </how-to-verify>
  <resume-signal>Type "branch confirmed: granted" or "branch confirmed: pending", plus the Key ID and Issuer ID, or describe what blocked you</resume-signal>
  <acceptance_criteria>
  - User confirms the entitlement branch with portal evidence; mismatch → Task 1 re-runs the correct branch before any archive
  - Key ID + Issuer ID reported; .p8 confirmed to live outside the repo (default path or API_PRIVATE_KEYS_DIR)
  - No key material pasted into the session or any repo file
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Release archive script — archive, export, scan-gate the Release binaries</name>
  <files>scripts/release-archive.sh, .gitignore</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "Archive + TestFlight upload (build, batch — SHIP-04)" section: the standard command shapes (archive with -destination generic/platform/iOS, exportArchive with ExportOptions.plist, altool upload), the ipabuild.sh anti-analog (every signing step there is a do-not-copy), and the archive-binary scan paths
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "scripts/scan-private-apis.sh re-run" section: the Release-archive binary paths under build/CarTube.xcarchive/Products/Applications/
  - .planning/phases/02-severance-signing-modernization/02-02-PLAN.md — the scan-gate contract (any binary path argument; exit 0 = clean) and shell conventions
  - ipabuild.sh via git history (git log --all --oneline -- ipabuild.sh then show the pre-deletion revision) — shell style reference ONLY: set -e, dirname cd, stale-target rm before copy; its signing steps are the anti-analog
  </read_first>
  <precondition>Task 2 checkpoint passed: branch confirmed, ASC key material in place outside the repo.</precondition>
  <reversibility rating="reversible">Build artifacts under build/ are regenerated per run; script + gitignore are plain files.</reversibility>
  <behavior>
  - Behavior 1: script exits non-zero before any upload when the scan gate hits on either Release binary (gate order: archive → export → scan → upload; scan failure aborts)
  - Behavior 2: script exits zero after producing build/export/CarTube.ipa when both binaries scan clean, without performing the upload (upload is a separate documented command so the script stays safe to re-run)
  - Behavior 3: re-running the script replaces a stale build/CarTube.xcarchive instead of layering onto it
  </behavior>
  <action>
  Create scripts/release-archive.sh following the shell conventions (set -e; cd to repo root via dirname; rm -rf the stale archive path before archiving). Steps in order: (1) xcodebuild -project CarTube.xcodeproj -scheme CarTube -configuration Release -destination generic/platform=iOS -archivePath build/CarTube.xcarchive archive; (2) xcodebuild -exportArchive -archivePath build/CarTube.xcarchive -exportOptionsPlist ExportOptions.plist -exportPath build/export; (3) run scripts/scan-private-apis.sh on build/CarTube.xcarchive/Products/Applications/CarTube.app/CarTube and on build/CarTube.xcarchive/Products/Applications/CarTube.app/PlugIns/PlayOnCarTube.appex/PlayOnCarTube — both must exit 0 or the script aborts before printing the upload command; (4) print the path of build/export/CarTube.ipa and the exact altool command (from the runbook, referencing env-provided ASC_KEY_ID and ASC_ISSUER_ID) without executing it. chmod +x. Add build/ to .gitignore if absent (the archive, export, and any DerivedData must never be committed). Run the script for real; record the two scan exit codes and the produced ipa path in the SUMMARY.

  Then execute the upload per the Task 4 runbook using the checkpoint-provided Key ID and Issuer ID (env vars ASC_KEY_ID / ASC_ISSUER_ID; the .p8 already at altool's search path). Record the altool output (success message with build number, or the failure verbatim) in the SUMMARY. If altool reports a validation error, fix and re-run — do not proceed to Task 4 with an unuploaded ipa unless the error is the documented pending-entitlement case. Under no circumstances reproduce the deleted TrollStore packaging steps (third-party re-signing, signature stripping, or building with code signing disabled) — that pipeline is the anti-analog; the standard archive signs with the team identity and needs nothing after export.
  <!-- planner-discipline-allow: ldid -->
  </action>
  <verify>
    <automated>test -x scripts/release-archive.sh && grep -q 'set -e' scripts/release-archive.sh && grep -q 'scan-private-apis.sh' scripts/release-archive.sh && grep -q 'app-store-connect' ExportOptions.plist && grep -q 'build/' .gitignore && test -f build/export/CarTube.ipa && scripts/scan-private-apis.sh build/CarTube.xcarchive/Products/Applications/CarTube.app/CarTube && scripts/scan-private-apis.sh build/CarTube.xcarchive/Products/Applications/CarTube.app/PlugIns/PlayOnCarTube.appex/PlayOnCarTube && ! grep -qE 'ldid|CODE_SIGNING_ALLOWED=.NO.|codesign --remove' scripts/release-archive.sh && ! git grep -qE 'AuthKey_[A-Z0-9]+\.p8' -- . ':!*.md'</automated>
  </verify>
  <acceptance_criteria>
  - Script executable, set -e, invokes the scan gate on both Release binaries, and contains none of the TrollStore signing steps (ldid, codesign removal, signing-disabled build flags)
  - build/CarTube.ipa exists; both Release-binary scans exit 0 (the SHIP criterion-4 scan-clean proof)
  - .gitignore covers build/
  - Upload executed with env-sourced key identifiers; altool outcome recorded verbatim in SUMMARY
  - Script never embeds key material: git grep for the AuthKey pattern and issuer hex across the repo returns nothing
  </acceptance_criteria>
  <done>Scan-clean standard-signed ipa produced, uploaded to App Store Connect, with the gate order proven (scan before upload) and zero TrollStore residue.</done>
</task>

<task type="auto">
  <name>Upload runbook + submission notes — coherent package record</name>
  <files>docs/submission/upload-runbook.md, docs/submission/submission-notes.md</files>
  <read_first>
  - .planning/phases/06-testflight-submission-package/06-PATTERNS.md — "Archive + TestFlight upload" section notes: ExportOptions.plist field semantics, altool key requirements, and the Secrets hygiene pattern to apply verbatim
  - .planning/phases/01-external-dependencies-project-setup/01-01-PLAN.md — the xcconfig secret-hygiene pattern (committed example only, real value gitignored, git grep negative gate in acceptance criteria)
  - docs/submission/app-review-notes.md and docs/submission/app-store-metadata.md (the 06-02 deliverables this record cross-references)
  </read_first>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <action>
  Create docs/submission/upload-runbook.md in the Phase 1 runbook convention: numbered steps for ASC API key creation (Users and Access → Integrations → Team Keys; Admin or App Manager role; the one-time .p8 download), key placement at altool's search path or API_PRIVATE_KEYS_DIR, exporting ASC_KEY_ID and ASC_ISSUER_ID as environment variables, running scripts/release-archive.sh, then the exact xcrun altool --upload-app command with -f build/export/CarTube.ipa and the key/issuer flags. Include the secrets-hygiene note verbatim in spirit: key material never enters the repo; the git grep gate is the enforcement. Document TestFlight processing expectations (build processing minutes-to-hours; TestFlight tab appearance).

  Create docs/submission/submission-notes.md: state the resolved entitlement branch and its evidence; record the scan-gate exit codes on both Release binaries and the uploaded build's number/date; cross-reference docs/submission/app-review-notes.md (paste into App Review notes field at submission), docs/submission/app-store-metadata.md (store copy source), and docs/submission/fallback-ladder.md (the response plan on rejection); if the pending branch was taken, document that the upload lacks the CarPlay capability pending Apple's grant, per the entitlement runbook's pending-state expectations. This note is the single page the user reads before pressing Submit in App Store Connect.
  </action>
  <verify>
    <automated>grep -q 'altool' docs/submission/upload-runbook.md && grep -q 'ASC_KEY_ID' docs/submission/upload-runbook.md && grep -q 'ASC_ISSUER_ID' docs/submission/upload-runbook.md && grep -q 'Integrations' docs/submission/upload-runbook.md && grep -q 'app-review-notes.md' docs/submission/submission-notes.md && grep -q 'app-store-metadata.md' docs/submission/submission-notes.md && grep -q 'fallback-ladder.md' docs/submission/submission-notes.md && ! git grep -qE 'AuthKey_[A-Z0-9]+\.p8|Issuer:[a-f0-9-]{20,}' -- ':!*.md'</automated>
  </verify>
  <acceptance_criteria>
  - upload-runbook.md contains the altool command, both env var names, the Integrations click path, and the one-time .p8 warning
  - submission-notes.md names the resolved branch with evidence, both scan exit codes, the build number/date, and cross-references all three 06-02 deliverables
  - Pending branch (if taken): the missing CarPlay capability is documented as the known upload state with the runbook pointer
  - git grep across non-md files returns zero hits for the .p8 filename pattern and long issuer hex — no key material anywhere
  </acceptance_criteria>
  <done>The uploaded package is documented end to end: how it was built, what it contains, what branch it reflects, and what the user does at Submit time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ASC API key material → repo/git history | upload credential; any commit is a permanent leak on a public fork |
| Release binaries → App Store Connect | the shipped artifact; private-API regression here is a 2.5.1 rejection |
| resolved signing state → archive validity | wrong branch produces an archive that cannot upload or cannot use CarPlay |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Information Disclosure | ASC API key committed to public fork | critical | mitigate | .p8 lives only at altool's search path outside the repo; Key ID/Issuer ID via env vars; git grep negative gate on .p8 filename and issuer hex patterns in acceptance criteria; checkpoint forbids pasting key material |
| T-06-09 | Tampering | private-API regression in the Release archive | high | mitigate | Scan gate runs on both Release archive binaries inside release-archive.sh BEFORE upload; script aborts on any hit (behavior 1); exit codes recorded in SUMMARY |
| T-06-10 | Tampering | TrollStore signing residue in the release path | high | mitigate | Script acceptance negative-gates the legacy re-sign/stripping steps; standard archive signs with team identity — nothing stripped or re-signed after export |
| T-06-11 | Elevation of Privilege | entitlement branch mis-resolved (archive without granted capability) | medium | mitigate | Blocking human checkpoint confirms branch with portal evidence before archiving; mismatch re-runs Task 1's correct branch |
| T-06-SC | Tampering | package installs | low | accept | no registry installs; ruby xcodeproj gem is the established local tool from prior phases |
</threat_model>

<verification>
- Task 1: ExportOptions.plist lints; branch evidence in SUMMARY; (grant branch) Manual signing + simulator proof recorded
- Task 2: checkpoint returns branch confirmation + Key ID/Issuer ID, no key material in session
- Task 3: script executable and TrollStore-free by grep; both Release scans exit 0; ipa exists; upload outcome verbatim in SUMMARY
- Task 4: runbook + submission notes greps green; git grep key-material gate passes
</verification>

<success_criteria>
- Scan-clean standard-signed Release archive of app + share extension uploaded to App Store Connect (TestFlight-ready)
- Entitlement branch resolved explicitly with evidence, never assumed
- Zero key material in the repo; zero TrollStore signing steps in the release path
- Submission notes make the package coherent: branch, proof, deliverables, and the user's Submit-time steps in one place
</success_criteria>

## Artifacts this plan produces

- ExportOptions.plist — app-store-connect method, teamID U67AKNW8PW, signing style per the resolved entitlement branch
- scripts/release-archive.sh — executable release pipeline: archive → export → dual-binary scan gate (abort on hit) → print upload command; standard xcodebuild commands only
- .gitignore — build/ entry (archive/export/DerivedData never committed)
- docs/submission/upload-runbook.md — ASC key creation click path, env-var wiring, altool command, TestFlight processing notes, secrets-hygiene enforcement
- docs/submission/submission-notes.md — branch record with evidence, scan exit codes, build number, cross-references to all three 06-02 deliverables, pending-branch documentation
- The uploaded TestFlight build itself (evidence recorded in SUMMARY, not committed)

<output>
Create .planning/phases/06-testflight-submission-package/06-03-SUMMARY.md when done
</output>
