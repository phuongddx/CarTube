---
phase: 02-severance-signing-modernization
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - CarTube/CarPlay/CarPlayViewController.swift
  - CarTube/CarPlay/CarPlaySingleton.swift
  - CarTube/Views/Settings.swift
  - CarTube/Views/ContentView.swift
  - CarTube/CarTubeApp.swift
autonomous: true
requirements: [INFRA-03, INFRA-06]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A simulator build of scheme CarTube succeeds after deleting every MediaRemote and BackBoard/SpringBoard brightness path, proving the caller-first deletion left no dangling reference
    - The strings output of the built CarTube binary contains none of: MRMediaRemote, kMRMediaRemoteNowPlayingInfo, BKSDisplayBrightness, SBGetScreenLockStatus, SBSSpringBoardServerPort, SBBacklightLevel, PrivateFrameworks/ (scan script passes on the app binary)
    - Saving Settings no longer terminates the process: the Save button applies the new webview configuration in place (recreated WKWebView with re-selected user scripts) and the app remains running; grep for exit(0) and exitGracefully across Swift sources returns nothing
    - The LockScreenDimmingOn key, its Settings toggle row, and its registration are fully gone: grep -r 'LockScreenDimmingOn' over Swift sources returns 0
    - Each surviving settings key (SponsorBlockOn, AgeRestrictBypassOn, AdBlockerOn, Zoom, ScreenPersistenceOn) still resolves in exactly 3 files: CarTubeApp.swift, Settings.swift, CarPlaySingleton.swift or CarPlayViewController.swift
    - The screen-off warning label path survives untouched: showScreenOffWarning, showWarningLabel, registerForScreenOffNotification, registerForUnlockNotification all still exist with callers
    - The now-playing takeover feature (checkIfYouTubePlaying, dontAskAboutLastPlaying, getNowPlaying) is removed — no alert about "You were watching this video" can appear
  artifacts:
    - CarPlayViewController.applyConfiguration() — extracted reusable configuration method
    - CarPlayViewController.applyConfigurationInPlace() — safe re-creation of the WKWebView without process exit
    - CarPlaySingleton.applyConfiguration() — facade passthrough (phone Settings → singleton → controller)
    - Deleted symbols: exitGracefully, getNowPlaying, checkIfYouTubePlaying, dontAskAboutLastPlaying, askAboutLastPlaying, saveInitialBrightness, setLowBrightness, restoreBrightness, setScreenBrightness, getScreenBrightness, isAutoBrightnessEnabled, getSettingsBrightness, setAutoBrightness, isScreenLocked, initialBrightness, initialAutoBrightness (callers first, definitions last)
    - Settings.swift without the Lock Screen Dimming section and with the reworded apply-in-place footer
  key_links:
    - Settings.saveSettings() → CarPlaySingleton.shared.applyConfiguration() → controller.applyConfigurationInPlace() → new WKWebView with re-read UserDefaults script selection → reload of current video/homepage
    - CarTubeApp.init notify registrations → CarPlaySingleton.showScreenOffWarning → controller.showWarningLabel (surviving, uncut)
---

<objective>
Execute the severance (INFRA-03) and the settings behavioral-contract change (INFRA-06) in one atomic cut of the same files: delete MediaRemote now-playing takeover and BackBoard/SpringBoard brightness code caller-first, remove the LockScreenDimmingOn toggle across its 3-file contract, replace NoSleep's hidden second webview with the public idle timer, and make Settings apply the webview configuration in place instead of exit(0).

These two requirements share every file they touch (Utilities callers, Settings, the controller's viewDidLoad) — splitting them would mean editing the same lines twice. The INFRA-06 exit-to-apply contract change is user pre-approved in REQUIREMENTS (INFRA-06) and PROJECT constraints ("Settings exit-to-apply and single-cached-video model retained unless a phase explicitly changes them" — this phase explicitly changes it). Now-playing takeover removal is likewise user-approved (REQUIREMENTS INFRA-03, "feature removed").

Severance order (PATTERNS caller map, delete caller first): ContentView/App callers → singleton methods → scene-delegate callers → Utilities definitions. The screen-off warning label path SURVIVES (ROADMAP success criterion 2): notify registrations, showScreenOffWarning, showWarningLabel stay.

Note: plan 02-04 raises the deployment target AFTER this cut, so all code here must still compile at iOS 14 availability — the public UIApplication idle timer and WKWebView re-creation APIs used here are iOS 2.0/8.0-era public APIs, so no availability guards are needed.

Purpose: The riskiest private-API surface leaves the binary in one atomic, caller-mapped cut, and the last Apple-forbidden exit() disappears with it.
Output: Severed Swift layer, in-place settings application, idle-timer NoSleep replacement.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-severance-signing-modernization/02-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@CarTube/CarPlay/CarPlayViewController.swift
@CarTube/CarPlay/CarPlaySingleton.swift
</context>

<tasks>

<task type="auto">
  <name>Caller-first deletion — now-playing takeover, brightness trio, LockScreenDimmingOn contract</name>
  <files>CarTube/Views/ContentView.swift, CarTube/CarTubeApp.swift, CarTube/CarPlay/CarPlaySingleton.swift, CarTube/CarPlay/CarPlaySceneDelegate.swift, CarTube/Util/Utilities.swift</files>
  <read_first>
  - .planning/phases/02-severance-signing-modernization/02-PATTERNS.md — "Verified Caller Map" (every symbol with definition + caller lines) and the SURVIVES note under it
  - .planning/research/PITFALLS.md — Pitfall 4 (removal breaking launch/features: the viewDidLoad → checkIfYouTubePlaying chain, the unsafeBitCast-of-dlsym crash risk, dead-toggle cleanup in same phase)
  - .planning/research/ARCHITECTURE.md — "What Must Be Severed (and how degradation behaves)" table
  - CarTube/CarPlay/CarPlayViewController.swift lines 20-32 (the two viewDidLoad calls being removed here land in Task 2's file — coordinate: remove checkIfYouTubePlaying call in Task 2, everything else in this task)
  </read_first>
  <reversibility rating="costly">Feature removals (now-playing takeover, lock-screen dimming) are user-approved one-way cuts; code recoverable from git history but the features do not come back this milestone.</reversibility>
  <action>
  Delete bottom-up in dependency order — each step's target must have zero remaining references after the step:
  1. ContentView.swift line 24: delete the CarPlaySingleton.shared.dontAskAboutLastPlaying() call inside playVideo(). Leave the rest of playVideo() (extractYouTubeVideoID → YT_EMBED → loadUrl chain, invalid-link alert) untouched.
  2. CarTubeApp.swift: delete the dontAskAboutLastPlaying() call at line 35 (inside onOpenURL); rewrite the init block (lines 15-23) to register the screen-off notification UNCONDITIONALLY with a callback containing only CarPlaySingleton.shared.showScreenOffWarning() — delete the LockScreenDimmingOn UserDefaults gate, the saveInitialBrightness call, the setLowBrightness call, and the entire registerForUnlockNotification registration (its only callback body was restoreBrightness; the warning label self-hides via its own 3-second timer, so nothing needs the unlock event); delete the "LockScreenDimmingOn": true line from registerDefaults() (line 51).
  3. CarPlaySingleton.swift: delete checkIfYouTubePlaying (lines 129-143), dontAskAboutLastPlaying (31-33), askAboutLastPlaying (13), saveInitialBrightness (46-49), setLowBrightness (51-57), restoreBrightness (59-66), initialBrightness and initialAutoBrightness (11-12). KEEP showScreenOffWarning (81-83), disablePersistence/enablePersistence (68-78), loadUrl/searchVideo caching, and all keyboard/nav passthroughs.
  4. CarPlaySceneDelegate.swift: sceneDidBecomeActive keeps only setCPWindowActive(true) and enablePersistence() — delete the isScreenLocked() block (lines 28-34). sceneWillResignActive keeps only disablePersistence() and setCPWindowActive(false) — delete the isScreenLocked/restoreBrightness block (lines 37-40).
  5. Utilities.swift: delete getNowPlaying (149-179), isScreenLocked (61-75), getScreenBrightness (79-90), setScreenBrightness (94-103), isAutoBrightnessEnabled (108-118), getSettingsBrightness (122-134), setAutoBrightness (137-146), registerForUnlockNotification (48-58, its only caller died in step 2), and the import Dynamic line (8). KEEP isYouTubeURL, extractYouTubeVideoID, exitGracefully (deleted in Task 3), registerForScreenOffNotification (35-44) — the notify key com.apple.springboard.hasBlankedScreen is public notify API and survives per the gate-scope decision.
  Do not touch the Dynamic package references in project.pbxproj — that is plan 02-04's ruby-xcodeproj job. The import Dynamic deletion here leaves the package unused but referenced; the project still builds.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -1 | grep -q 'BUILD SUCCEEDED' && ! grep -rn 'getNowPlaying\|checkIfYouTubePlaying\|dontAskAboutLastPlaying\|askAboutLastPlaying\|saveInitialBrightness\|setLowBrightness\|restoreBrightness\|setScreenBrightness\|getScreenBrightness\|isAutoBrightnessEnabled\|getSettingsBrightness\|setAutoBrightness\|isScreenLocked\|registerForUnlockNotification\|LockScreenDimmingOn\|import Dynamic' --include='*.swift' CarTube/ PlayOnCarTube/ && grep -q 'showScreenOffWarning' CarTube/CarPlay/CarPlaySingleton.swift && grep -q 'registerForScreenOffNotification' CarTube/CarTubeApp.swift && grep -q 'showWarningLabel' CarTube/CarPlay/CarPlayViewController.swift</automated>
  </verify>
  <acceptance_criteria>
  - BUILD SUCCEEDED after the cut (no dangling reference to any deleted symbol)
  - grep -rn over the deleted-symbol list across all Swift sources in CarTube/ and PlayOnCarTube/ returns zero matches
  - grep -r 'LockScreenDimmingOn' --include='*.swift' . returns 0 (all three contract sites dead: CarTubeApp registration, Settings state/write, singleton consumption)
  - Survivors intact: showScreenOffWarning exists in CarPlaySingleton, registerForScreenOffNotification called in CarTubeApp init, showWarningLabel exists in CarPlayViewController
  - CarTubeApp.init registers the screen-off notification unconditionally (not behind a UserDefaults gate)
  - git diff shows CarPlaySceneDelegate.swift reduced to the 4-call target shape from PATTERNS
  </acceptance_criteria>
  <done>Every MediaRemote/brightness/private-prefs symbol and its callers are gone, the settings key contract for LockScreenDimmingOn is dead in all three files, and the warning-label path survives.</done>
</task>

<task type="auto" tdd="true">
  <name>In-place configuration — extract applyConfiguration, replace NoSleep webview with idle timer</name>
  <files>CarTube/CarPlay/CarPlayViewController.swift</files>
  <read_first>
  - CarTube/CarPlay/CarPlayViewController.swift lines 28-73 (the config-construction block being extracted — exact boundaries) and lines 96-110 (the noSleepView construction being replaced) and lines 213-234 (the persistence timer that references noSleepView)
  - .planning/phases/02-severance-signing-modernization/02-PATTERNS.md — "CarPlay/CarPlay/CarPlayViewController.swift" section (extraction recipe, script-loading convention to preserve, containment/frame patterns to follow)
  - .planning/research/PITFALLS.md — "Performance Traps" row 2 (second hidden WKWebView for NoSleep — replace with public idle-timer strategy; matches the resolved entitlements decision)
  </read_first>
  <behavior>
  - Behavior 1: applyConfiguration() produces a WKWebViewConfiguration whose userContentController contains exactly the enabled scripts (SponsorBlock / AgeRestrictBypass / AdBlock per UserDefaults booleans) plus always-CustomLayout plus the zoom viewport script with the current Zoom value, plus the keyboard message handler, with the five media flags set (allowsInlineMediaPlayback true, allowsPictureInPictureMediaPlayback false, allowsAirPlayForMediaPlayback false, requiresUserActionForMediaPlayback false) — identical to today's viewDidLoad output for the same defaults
  - Behavior 2: toggling any script setting then calling applyConfigurationInPlace() yields a webview whose configuration reflects the new defaults and which has reloaded (current URL or homepage) — the observable contract replacing exit(0)
  - Behavior 3: enablePersistence()/disablePersistence() manage UIApplication.shared.isIdleTimerDisabled directly — true on enable, false on disable, with no WKWebView reference remaining
  - Shell-verifiable assertions (test target arrives in Phase 3): after implementation, grep -c 'noSleepView' over Swift sources returns 0 and grep -c 'isIdleTimerDisabled' returns ≥ 2
  </behavior>
  <action>
  1. Extract lines 31-69's construction into a new method applyConfiguration() returning WKWebViewConfiguration: read the three script booleans from UserDefaults, build enabledScripts array (CustomLayout always appended), load each script via Bundle.main.path(forResource:ofType:) with the existing guard-else-return, add the zoom viewport user script (forMainFrameOnly: true), add(self, name: "keyboard"), set the four media flags. viewDidLoad then reads: webView = WKWebView(frame: view.frame, configuration: applyConfiguration()) plus the existing property assignments (allowsLinkPreview etc.).
  2. Add applyConfigurationInPlace(): tear down and rebuild the web view when settings change — remove webView from superview, nil its navigationDelegate/uiDelegate, construct the new WKWebView via applyConfiguration(), reassign the stored property, re-add as subview below keyboardView and screenOffLabel (use the existing z-order: insert below keyboardView if visible), restore frame (full bounds, or reduced height if keyboardView is visible — mirror toggleKeyboard's frame math), reload the last content: if the old webView's URL was non-nil load it, else goHome(). Because WKWebView cannot swap configuration after init, full re-creation is the mechanism — no process exit anywhere.
  3. Replace NoSleep: delete the noSleepView property, its construction block (lines 96-110), and all noSleepView.evaluateJavaScript calls in enable/disablePersistence. New bodies: enablePersistence sets UIApplication.shared.isIdleTimerDisabled = true; disablePersistence invalidates any timer then sets UIApplication.shared.isIdleTimerDisabled = false. Delete the timer property and the _hasSleepDisabler polling loop entirely — the idle timer needs no polling. (NoSleepEnable.js stays on disk as an unused resource; plan 02-04 does not remove it from the bundle — its strings presence is harmless and out of gate scope.) Note the fidelity gap honestly in the SUMMARY: the public idle timer keeps the PHONE screen awake but does not assert a CarPlay-scene wake lock the way the entitlement-backed path did; degradation is observable on the debug screen per the Key Decision.
  4. Delete the checkIfYouTubePlaying() call in viewDidLoad (line 27) — its definition died in Task 1.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -1 | grep -q 'BUILD SUCCEEDED' && [ "$(grep -rc 'noSleepView' --include='*.swift' CarTube/ PlayOnCarTube/ | awk -F: '{s+=$2} END {print s}')" = "0" ] && [ "$(grep -rc 'isIdleTimerDisabled' --include='*.swift' CarTube/ | awk -F: '{s+=$2} END {print s}')" -ge 2 ] && grep -q 'func applyConfiguration' CarTube/CarPlay/CarPlayViewController.swift && grep -q 'func applyConfigurationInPlace' CarTube/CarPlay/CarPlayViewController.swift && ! grep -q 'checkIfYouTubePlaying' CarTube/CarPlay/CarPlayViewController.swift</automated>
  </verify>
  <acceptance_criteria>
  - BUILD SUCCEEDED; no noSleepView reference remains; isIdleTimerDisabled set in both persistence methods; timer property gone
  - applyConfiguration and applyConfigurationInPlace exist with the behavior contracts above; viewDidLoad no longer contains the inline config block nor the checkIfYouTubePlaying call
  - applyConfigurationInPlace contains no exit/terminate/suspend call of any kind
  - Script loading still uses Bundle.main.path(forResource:ofType:) + .atDocumentEnd + forMainFrameOnly pattern (unchanged convention)
  </acceptance_criteria>
  <done>The webview configuration is a reusable method, settings can be applied by re-creating the webview in place, and screen persistence runs on the public idle timer with no second webview.</done>
</task>

<task type="auto">
  <name>Settings apply-in-place wiring + toggle cleanup + surviving-key audit</name>
  <files>CarTube/Views/Settings.swift</files>
  <read_first>
  - CarTube/Views/Settings.swift (full file — the Lock Screen Dimming section at 58-61, saveSettings at 17-26, footer at 31-33)
  - .planning/phases/02-severance-signing-modernization/02-PATTERNS.md — "Settings-key contract" shared pattern (3-file rule, surviving keys list) and the Settings.swift pattern section
  - CarTube/CarPlay/CarPlaySingleton.swift (as edited by Task 1 — where applyConfiguration passthrough must land, mirroring sendInput/goHome routing)
  </read_first>
  <action>
  1. In Settings.swift: delete the entire Lock Screen Dimming section (lines 58-64: the Section with the RECOMMENDED dim footer and the lockScreenDimmingOn toggle), the @State lockScreenDimmingOn property (line 12), and its UserDefaults.set line in saveSettings (line 25).
  2. In saveSettings(), replace exitGracefully() with CarPlaySingleton.shared.applyConfiguration() AFTER all UserDefaults.set calls. Add that facade method to CarPlaySingleton (single line routing to controller?.applyConfigurationInPlace(), mirroring goBack/goHome/toggleKeyboard shape). Reword the Save button footer from "The app will quit." to reflect in-place application (e.g. changes apply immediately to the CarPlay browser) — no quit language may remain.
  3. Delete exitGracefully() from Utilities.swift (lines 27-31) — Settings.swift was its only caller.
  4. Audit the surviving keys: for each of SponsorBlockOn, AgeRestrictBypassOn, AdBlockerOn, Zoom, ScreenPersistenceOn confirm the 3-file contract still holds (register in CarTubeApp.registerDefaults, state+write in Settings, consume in CarPlayViewController.applyConfiguration or CarPlaySingleton persistence pair). Record the grep counts in the SUMMARY.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -1 | grep -q 'BUILD SUCCEEDED' && ! grep -rn 'exitGracefully\|exit(0)' --include='*.swift' CarTube/ && ! grep -q 'Lock Screen Dimming' CarTube/Views/Settings.swift && ! grep -q 'will quit' CarTube/Views/Settings.swift && grep -q 'applyConfiguration' CarTube/Views/Settings.swift && grep -q 'func applyConfiguration' CarTube/CarPlay/CarPlaySingleton.swift && for k in SponsorBlockOn AgeRestrictBypassOn AdBlockerOn Zoom ScreenPersistenceOn; do n=$(grep -rl "$k" --include='*.swift' CarTube/ | wc -l); [ "$n" = "3" ] || { echo "KEY $k in $n files"; exit 1; }; done && for k in SponsorBlockOn AgeRestrictBypassOn AdBlockerOn Zoom ScreenPersistenceOn; do grep -q "\"$k\"" CarTube/CarTubeApp.swift || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
  - BUILD SUCCEEDED; grep -rn 'exitGracefully|exit(0)' over CarTube Swift sources returns 0
  - Settings.swift contains no Lock Screen Dimming section and no quit language; saveSettings calls CarPlaySingleton.shared.applyConfiguration() after all writes
  - CarPlaySingleton.applyConfiguration routes to controller?.applyConfigurationInPlace()
  - Each of the 5 surviving keys appears in exactly 3 Swift files, with registration present in CarTubeApp.swift
  - SUMMARY records per-key grep counts as audit evidence
  </acceptance_criteria>
  <done>Settings applies configuration in place with no dead toggles, exitGracefully is deleted, and every surviving settings key still honors its 3-file contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app process → private frameworks (dlsym/CFBundle) | every removed call crossed into SpringBoard/BackBoard/MediaRemote territory |
| app container → system preference domains | CFPreferencesCopyAppValue against com.apple.springboard / com.apple.backboardd read outside the container (guideline 2.5.2) |
| Settings UI → process lifecycle | exit(0) path crossed from UI action into forbidden process termination |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Information Disclosure | getNowPlaying MediaRemote read | high | mitigate | Delete feature + callers (Task 1); scan-gate markers catch regression (plan 02-02) |
| T-02-08 | Elevation of Privilege | brightness/dlsym unsafeBitCast NULL crash | high | mitigate | Delete functions and callers atomically in caller-first order; build gate proves no dangling reference |
| T-02-09 | Information Disclosure | CFPreferences reads outside container | high | mitigate | getSettingsBrightness/isAutoBrightnessEnabled deleted; SBBacklightLevel + BKEnableALS surface in gate via SBBacklightLevel marker family |
| T-02-10 | Denial of Service | exit(0) from shipping UI | high | mitigate | Replaced by in-place reconfiguration; grep gate on exitGracefully/exit(0) |
| T-02-11 | Tampering | WKWebView re-creation loses message handler/delegates | medium | mitigate | applyConfigurationInPlace reuses applyConfiguration() output (single construction path) and reassigns delegates; acceptance criteria pin handler + flags |
| T-02-SC | Tampering | package installs | low | accept | no package installs this plan |
</threat_model>

<verification>
- All three tasks' automated commands green; BUILD SUCCEEDED after each cut
- Binary-level: scripts/scan-private-apis.sh on the built app binary exits 0 (run it as the final cross-check — markers MRMediaRemote/BKSDisplayBrightness/... now absent because the Swift layer is severed, even though plan 02-04's pbxproj work is pending)
- Surviving-key audit counts recorded in SUMMARY
</verification>

<success_criteria>
- No MediaRemote/BackBoard/SpringBoard symbol or caller remains in Swift sources; warning-label path intact
- Settings applies in place; no dead toggles; surviving keys at 3 sites each
- NoSleep runs on the public idle timer; second webview gone
</success_criteria>

## Artifacts this plan produces

- CarPlayViewController.applyConfiguration() — WKWebViewConfiguration factory (script selection + zoom + keyboard handler + media flags)
- CarPlayViewController.applyConfigurationInPlace() — teardown/re-create/reload path replacing exit-to-apply
- CarPlaySingleton.applyConfiguration() — facade passthrough (Settings → controller)
- enablePersistence()/disablePersistence() rewritten on UIApplication.shared.isIdleTimerDisabled
- Deleted functions (16 symbols listed in must_haves.artifacts) across Utilities.swift / CarPlaySingleton.swift / CarPlayViewController.swift / CarPlaySceneDelegate.swift / CarTubeApp.swift / ContentView.swift / Settings.swift
- Settings key "LockScreenDimmingOn" retired from all 3 contract sites; 5 surviving keys audited at 3 sites each
- Settings.swift footer copy: apply-in-place wording

<output>
Create .planning/phases/02-severance-signing-modernization/02-03-SUMMARY.md when done
</output>
