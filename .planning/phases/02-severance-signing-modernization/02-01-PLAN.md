---
phase: 02-severance-signing-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CarTube/CarTube.entitlements
  - ipabuild.sh
  - README.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONVENTIONS.md
  - AGENTS.md
  - .planning/PROJECT.md
  - .planning/phases/02-severance-signing-modernization/NOTES.md
autonomous: true
requirements: [INFRA-02]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - CarTube/CarTube.entitlements contains zero dictionary keys (bare empty plist dict)
    - A simulator build of scheme CarTube succeeds with the emptied entitlements file attached via the existing CODE_SIGN_ENTITLEMENTS build settings
    - The ad-hoc signature of the built CarTube.app embeds none of the seven removed private entitlement keys (SBStarkCapable, com.apple.runningboard.assertions.webkit, com.apple.multitasking.systemappassertions, com.apple.backboard.displaybrightness, platform-application, com.apple.private.security.no-container, com.apple.private.security.container-manager)
    - ipabuild.sh exists neither in the working tree nor in the git index; no ldid/ipabuild reference remains in any tracked app/build file (regenerated AGENTS.md included in the gate and clean)
    - The refreshed .planning/codebase map (STACK.md, CONVENTIONS.md) and the regenerated AGENTS.md document the standard-signing/iOS-16 baseline with no TrollStore packaging guidance — Phases 3-6 executors read the new baseline, not the TrollStore-era one
    - PROJECT.md Key Decisions records the two resolved Phase 2 decision points and the phase NOTES.md file is deleted
  artifacts:
    - CarTube/CarTube.entitlements emptied to a bare plist dict (file retained, all 7 keys removed)
    - Deleted file ipabuild.sh (git rm, discarding its uncommitted local modifications — approved)
    - README.md with the TrollStore requirement line reworded for App Store distribution
    - .planning/codebase/STACK.md and CONVENTIONS.md with TrollStore/ldid/iOS-14-era lines rewritten to the standard-signing iOS-16 baseline
    - AGENTS.md regenerated from the refreshed codebase map (matches neither ldid nor ipabuild)
    - Two new Key Decisions rows in .planning/PROJECT.md
  key_links:
    - pbxproj CODE_SIGN_ENTITLEMENTS (lines 465/527/557/600, untouched) → emptied CarTube.entitlements → codesign entitlements dump of built product shows no private keys
    - refreshed .planning/codebase map (STACK.md + CONVENTIONS.md) → generate-claude-md → AGENTS.md baseline read by Phases 3-6 executors
---

<objective>
Strip every TrollStore signing artifact (INFRA-02): empty the private entitlements file, delete the ldid/ipabuild packaging pipeline, and prove end-to-end that the app builds and signs with standard Automatic team signing and zero private entitlement keys — the tracer slice for the phase.

Signing settings need NO change: CODE_SIGN_STYLE = Automatic and DEVELOPMENT_TEAM = U67AKNW8PW are already set on all four target configurations (verified pbxproj lines 558/561, 601/604, 639/641, 666/668). INFRA-02 is entitlements content + pipeline deletion only. Per resolved decision point (NOTES.md): ALL private keys are removed, including the SBStarkCapable / runningboard / multitasking keys that supported NoSleep — the public idle-timer replacement for NoSleep lands in plan 02-03, and its degradation is observable on the INFRA-04 debug screen.

Purpose: The binary must carry no instant-flag entitlement markers under standard signing, and the repo must carry no sideload packaging artifacts.
Output: Emptied entitlements file, deleted ipabuild.sh, cleaned README, encoded Key Decisions, committed tree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-severance-signing-modernization/02-PATTERNS.md
@.planning/research/PITFALLS.md
@CarTube/CarTube.entitlements
</context>

<tasks>

<task type="tracer">
  <name>End-to-end standard-signing build — entitlements stripped, ipabuild pipeline deleted</name>
  <files>CarTube/CarTube.entitlements, ipabuild.sh, README.md, .planning/codebase/STACK.md, .planning/codebase/CONVENTIONS.md, AGENTS.md</files>
  <read_first>
  - CarTube/CarTube.entitlements (full file — all 7 keys listed below are present and must all go)
  - .planning/phases/02-severance-signing-modernization/02-PATTERNS.md — section "CarTube/CarTube.entitlements" (keys to delete, CODE_SIGN_ENTITLEMENTS keep decision) and "CI scan gate" analog notes
  - .planning/phases/02-severance-signing-modernization/NOTES.md — decision (a): remove ALL private entitlement keys; NoSleep degraded deliberately
  - README.md lines 1-40 (the TrollStore claim at line 15 and its surrounding feature list)
  - CarTube.xcodeproj/project.pbxproj lines 460-470 and 555-565 only (confirm CODE_SIGN_ENTITLEMENTS and Automatic/team settings — read to orient, do NOT edit this file)
  </read_first>
  <precondition>Phase 1 plans are executed: test -f Secrets.xcconfig && test -f Config/Secrets.xcconfig.example (the project-level base configuration references root Secrets.xcconfig; the build fails without it — that is Phase 1's designed behavior). Halt and report if missing.</precondition>
  <reversibility rating="costly">Emptied entitlements are recoverable from git history, but git rm -f ipabuild.sh permanently discards its current uncommitted local modifications (stale-app removal fix) — user-approved deletion per INFRA-02.</reversibility>
  <action>
  Wire the single signing slice end-to-end, nothing else:
  1. Rewrite CarTube/CarTube.entitlements to contain only the XML declaration, DOCTYPE, plist element, and an empty dict element — keep the file itself and keep all four CODE_SIGN_ENTITLEMENTS build-setting lines in the pbxproj pointing at it (when Apple grants the CarPlay entitlement in Phase 1 wiring, the key lands in this file with no pbxproj change). Remove all seven keys: SBStarkCapable, com.apple.runningboard.assertions.webkit, com.apple.multitasking.systemappassertions, com.apple.backboard.displaybrightness, platform-application, com.apple.private.security.no-container, com.apple.private.security.container-manager.
  2. Delete ipabuild.sh with git rm -f (the -f discards its uncommitted local modification; deletion is required by INFRA-02 regardless). It is not referenced anywhere in project.pbxproj, so no project-file change is needed.
  3. Reword README.md line 15 ("Works on any device supporting TrollStore...") to state the app targets standard App Store / TestFlight distribution; remove any other TrollStore/ldid/permasign installation instructions if present in the install section. Do not rewrite the whole README.
  4. Commit the pending uncommitted project hygiene in the same commit: git add CarTube/Assets.xcassets (currently untracked but already referenced by the modified project.pbxproj asset-path fix) and git add CarTube.xcodeproj/project.pbxproj so the tracked project does not reference a missing catalog. Verify git status is clean of ipabuild.sh afterwards.
  5. Refresh the codebase map to the new baseline (this plan's severance makes the old docs misleading for Phases 3-6): in .planning/codebase/STACK.md rewrite the "TrollStore IPA build" block (lines 42-50) to a standard-signing description (Automatic team signing, emptied entitlements; delete the ./ipabuild.sh command, the ldid re-sign bullet, and the ldid/zip tool requirement), reword the entitlements bullet at line 58 to "Entitlements file retained for the future CarPlay entitlement key (emptied in Phase 2)", and reword lines 13-14 to the iOS 16 deployment-target baseline (plan 02-04 raises it from 14.0); in .planning/codebase/CONVENTIONS.md rewrite the "Build and Release" bullets at lines 103-105 to standard App Store/TestFlight signing (drop the TrollStore packaging and iOS 14-15.4 constraint language; plan 02-04 raises the deployment target) and reword the iOS 14-15.4 symbol assumption at line 86 to reflect that Phase 2 removed the private-API surface. Then regenerate AGENTS.md from the refreshed map: node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query generate-claude-md --output AGENTS.md. Confirm the regenerated AGENTS.md no longer matches ldid/ipabuild (the verify command greps it).
  Do NOT touch any Swift source, the pbxproj build settings, deployment target, or the Dynamic package — those belong to plans 02-03/02-04.
  </action>
  <verify>
    <automated>plutil -p CarTube/CarTube.entitlements && [ "$(plutil -convert json -o - CarTube/CarTube.entitlements | tr -d ' \n')" = "{}" ] && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -1 | grep -q 'BUILD SUCCEEDED' && BP=$(xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' -showBuildSettings 2>/dev/null | grep -m1 'BUILT_PRODUCTS_DIR = ' | awk '{ print $3 }') && ENT=$(codesign -d --entitlements :- "$BP/CarTube.app" 2>/dev/null) && ! grep -qE 'SBStarkCapable|runningboard|systemappassertions|displaybrightness|platform-application|no-container|container-manager' <<<"$ENT" && test ! -f ipabuild.sh && ! git grep -nI 'ldid\|ipabuild' -- ':!*.planning*' ':!*.md.plan*' ':!AGENTS.md' && git ls-files --error-unmatch CarTube/Assets.xcassets/Contents.json && ! git status --short | grep -q 'ipabuild' && ! grep -q 'ldid\|ipabuild\|TrollStore' AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
  - plutil -p CarTube/CarTube.entitlements prints an empty dictionary (zero keys)
  - The simulator build prints BUILD SUCCEEDED with Automatic signing and the emptied entitlements attached
  - codesign -d --entitlements on the built CarTube.app output matches none of the seven private key names
  - ipabuild.sh absent from working tree and git index; git grep for ldid/ipabuild over tracked files (planning artifacts and regenerated AGENTS.md excluded) exits 1; the regenerated AGENTS.md itself contains no ldid/ipabuild/TrollStore packaging text
  - CarTube/Assets.xcassets is tracked (ls-files --error-unmatch succeeds) so the pbxproj asset reference resolves on fresh clone
  - README.md contains no TrollStore requirement line (grep -c 'TrollStore' README.md may only match historical-attribution contexts if any; the requirement claim is gone)
  - Regenerated AGENTS.md describes standard signing and the iOS-16 baseline (no TrollStore/ldid/ipabuild packaging guidance); .planning/codebase/STACK.md and CONVENTIONS.md carry the same refreshed lines
  </acceptance_criteria>
  <done>The app builds and signs under the existing Automatic team signing with zero private entitlement keys embedded, ipabuild.sh and ldid references are gone from the repo, and the tracer slice is committed.</done>
</task>

<task type="auto">
  <name>Encode the two resolved decision points into PROJECT.md Key Decisions and retire NOTES.md</name>
  <files>.planning/PROJECT.md, .planning/phases/02-severance-signing-modernization/NOTES.md</files>
  <read_first>
  - .planning/PROJECT.md (Key Decisions table — match its existing row format exactly)
  - .planning/phases/02-severance-signing-modernization/NOTES.md (full file — the two decisions to encode, then delete)
  - .planning/phases/02-severance-signing-modernization/02-PATTERNS.md — "Verified Caller Map" heading note on the surviving warning-label notify path (needed to state the gate-scope decision precisely)
  </read_first>
  <action>
  Make the two user-declined-to-choose defaults durable as project decisions:
  1. Append two rows to the PROJECT.md Key Decisions table in its existing format. Row one: Phase 2 removes ALL private entitlement keys including SBStarkCapable, com.apple.runningboard.assertions.webkit, and com.apple.multitasking.systemappassertions — INFRA-02 (no private entitlement keys) wins over keeping NoSleep on entitlements; the NoSleep hidden webview is replaced by the public UIApplication idle timer in plan 02-03, and any residual wake-lock degradation is observable on the INFRA-04 hook-verification debug screen rather than treated as a blocker. Row two: the Phase 2 strings scan gate (INFRA-05) fails only on symbols this phase REMOVES — the MediaRemote, BackBoardServices brightness, SpringBoard lock/port, and TrollStore entitlement markers — while the surviving private WebKit calls (_simulateTextEntered) and remaining AutoHook swizzling stay out of the gate per the accepted webview-video risk; the surviving com.apple.springboard.hasBlankedScreen notify key powering the screen-off warning label is likewise excluded.
  2. git rm .planning/phases/02-severance-signing-modernization/NOTES.md — its content is now encoded in PROJECT.md and in plans 02-02/02-03.
  </action>
  <verify>
    <automated>grep -q 'SBStarkCapable' .planning/PROJECT.md && grep -q 'idle timer' .planning/PROJECT.md && grep -q '_simulateTextEntered' .planning/PROJECT.md && grep -q 'hasBlankedScreen' .planning/PROJECT.md && test ! -f .planning/phases/02-severance-signing-modernization/NOTES.md && git status --short | grep -q 'D  .planning/phases/02-severance-signing-modernization/NOTES.md'</automated>
  </verify>
  <acceptance_criteria>
  - PROJECT.md Key Decisions contains rows covering both decision points (entitlements-all-removed with NoSleep consequence; gate scoped to removed symbols only with surviving WebKit/notify exclusions)
  - NOTES.md is deleted from the working tree and recorded as deleted in the index
  - The Key Decisions rows match the table's existing column format
  </acceptance_criteria>
  <done>Both resolved Phase 2 decision defaults are durable project decisions and the temporary NOTES.md is retired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entitlements file → code signature | private keys here become instant App Review flags embedded in every signed build |
| repo → public fork | any tracked TrollStore/ldid artifact is visible to reviewers and forks |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | CarTube.entitlements | high | mitigate | Empty all 7 private keys (this plan); acceptance gates assert empty dict + clean codesign entitlements dump |
| T-02-02 | Tampering | ipabuild.sh deletion | medium | mitigate | git rm -f per INFRA-02; git grep gate asserts no ldid/ipabuild in tracked files |
| T-02-03 | Repudiation | removed keys re-added later | medium | mitigate | entitlement keys become scan-gate markers in plan 02-02 (scripts/scan-private-apis.sh marker list) so regression fails CI |
| T-02-SC | Tampering | package installs | low | accept | no package installs this plan — entitlements/README/planning-doc edits only |
</threat_model>

<verification>
- Both tasks' automated commands green; simulator BUILD SUCCEEDED with emptied entitlements
- git diff scope check: no Swift source, no pbxproj build-setting lines changed in this plan's commits (Assets path fix commit aside)
</verification>

<success_criteria>
- Zero private entitlement keys in file and in the built product's signature
- Zero TrollStore packaging artifacts in the repo
- Decision defaults durable in PROJECT.md; tree clean of uncommitted ipabuild/asset-path drift
</success_criteria>

## Artifacts this plan produces

- CarTube/CarTube.entitlements — empty plist dict (file retained for the future CarPlay entitlement key)
- Deleted: ipabuild.sh (with its uncommitted modifications), .planning/phases/02-severance-signing-modernization/NOTES.md
- README.md — TrollStore requirement line reworded for App Store distribution
- .planning/codebase/STACK.md + CONVENTIONS.md — TrollStore/ldid/iOS-14 lines rewritten to standard-signing/iOS-16 baseline
- AGENTS.md — regenerated from the refreshed codebase map
- .planning/PROJECT.md — 2 new Key Decisions rows (entitlements-all-removed + gate-scope)
- Newly tracked: CarTube/Assets.xcassets (fixes dangling pbxproj reference)

<output>
Create .planning/phases/02-severance-signing-modernization/02-01-SUMMARY.md when done
</output>
