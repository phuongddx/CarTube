---
phase: 03-search-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CarTube.xcodeproj/project.pbxproj
  - CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme
  - CarTubeTests/UtilitiesTests.swift
  - CarTube/Util/Utilities.swift
autonomous: true
requirements: [SRCH-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - xcodebuild test -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' executes the CarTubeTests target and reports TEST SUCCEEDED — the project's first passing test run
    - The scheme is SHARED (committed at CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme) and its TestAction names CarTubeTests, so a fresh clone can run tests with no local Xcode scheme setup
    - extractYouTubeVideoID passes a fixture-class test matrix (watch, youtu.be, embed, shorts, query-param, non-YouTube, plain text, empty string, IDs containing - and _) with the regex constructed once and no force-try
  artifacts:
    - CarTubeTests PBXNativeTarget (productType com.apple.product-type.bundle.unit-test) in project.pbxproj with Debug+Release configurations and a dependency on the CarTube target
    - CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme with a TestAction including CarTubeTests
    - CarTubeTests/UtilitiesTests.swift with one test method per input class
  key_links:
    - shared scheme TestAction → CarTubeTests target → @testable import CarTube → extractYouTubeVideoID in CarTube/Util/Utilities.swift — the pipeline every later plan in this phase builds on
---

<objective>
Prove the project's test pipeline end-to-end as the Phase 3 tracer: a committed shared scheme runs a brand-new CarTubeTests unit-test target against real app source on the iOS simulator, seeded with the shared URL-parsing test matrix that SRCH-04 names as one of its two test subjects.

This is the riskiest scaffold item of the phase (verified: no test target, no xcshareddata scheme exists — only user-local auto-schemes), so it is proven alone, before any search code lands. Everything in plans 03-02 and 03-03 executes inside this proven pipeline.

Purpose: SRCH-04 requires an XCTest target that passes fixture tests for the search client AND shared URL parsing; the parsing half plus the runnable target land here (SRCH-04).
Output: CarTubeTests target, shared CarTube scheme with TestAction, UtilitiesTests parser matrix, hardened parser.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-core/03-PATTERNS.md
@.planning/codebase/TESTING.md
@CarTube/Util/Utilities.swift
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "xcodebuild test runs a real test against app source" — one path only</name>
  <files>CarTube.xcodeproj/project.pbxproj, CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme, CarTubeTests/UtilitiesTests.swift</files>
  <read_first>
  - .planning/phases/03-search-core/03-PATTERNS.md — "CarTubeTests/* (test, first in repo)" section: the full PBXNativeTarget scaffold recipe (unit-test product type, three build phases, PBXContainerItemProxy/PBXTargetDependency, Debug+Release configurations, no TEST_HOST), the scheme-gap warning, the verify command shape, and the fixture-loading convention
  - .planning/phases/01-external-dependencies-project-setup/01-01-PLAN.md — the house ruby-xcodeproj recipe: throwaway script using the installed xcodeproj gem (1.28.1), mutate, save, delete the script, never commit it
  - CarTube/Util/Utilities.swift — extractYouTubeVideoID (the function under test; survives Phase 2 severance — brightness/MediaRemote code around it is deleted, the parser is kept)
  - CarTube.xcodeproj/project.pbxproj — the two existing PBXNativeTarget blocks for shape; re-grep before editing because Phase 2 shifts line numbers
  </read_first>
  <precondition>Phase 2 executed: grep -c 'IPHONEOS_DEPLOYMENT_TARGET = 16.0' CarTube.xcodeproj/project.pbxproj returns 6 and test -f Secrets.xcconfig succeeds (the Phase 1 base configuration must resolve or every build fails).</precondition>
  <reversibility rating="reversible">pbxproj and scheme mutations are additive config, git-revertible; no source behavior changes in this task.</reversibility>
  <action>
  Wire ONE path: a single test file, in a single new test target, run by a single shared scheme — nothing else.
  1. Create the test target via a throwaway ruby script with the installed xcodeproj gem (delete the script after saving; never commit it): new PBXNativeTarget named CarTubeTests with productType com.apple.product-type.bundle.unit-test, its own PBXSourcesBuildPhase, PBXFrameworksBuildPhase, and PBXResourcesBuildPhase, Debug and Release XCBuildConfiguration objects with a buildConfigurationList (deployment target 16.0, PRODUCT_BUNDLE_IDENTIFIER com.avangelista.CarTubeTests, GENERATE_INFOPLIST_FILE = YES), a PBXContainerItemProxy + PBXTargetDependency making CarTubeTests depend on the CarTube target, a CarTubeTests group beside PlayOnCarTube in the root group children, and a CarTubeTests.xctest entry in the Products group.
  2. Link the app module for @testable import WITHOUT a test host: set BUNDLE_LOADER = $(BUILT_PRODUCTS_DIR)/CarTube.app/CarTube on the test target and do NOT set TEST_HOST (pure parser/decoder tests need the app binary linked, not the app process running — faster, no simulator app boot, and the Phase 3 service tests never need UIApplication). If the test bundle fails to load at runtime, the sanctioned in-task correction is to add the standard pair TEST_HOST = $(BUILT_PRODUCTS_DIR)/CarTube.app/CarTube app-side path plus keeping BUNDLE_LOADER = $(TEST_HOST) — the acceptance command is the arbiter either way.
  3. Create the shared scheme: using the gem's Xcodeproj::XCScheme API (or hand-authoring the equivalent XML at CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme), build/launch the CarTube target and add CarTubeTests to the TestAction. The scheme must live under xcshareddata (committed) — the existing user-local auto-schemes under xcuserdata are not.
  4. Create CarTubeTests/UtilitiesTests.swift: final class UtilitiesTests: XCTestCase with @testable import CarTube and exactly ONE test method for this tracer — testExtractVideoIDFromWatchURL asserting a standard watch URL with ?v= yields its 11-character ID. Add the file to the CarTubeTests target's sources phase via the same gem script.
  5. Run the verify command; commit only project.pbxproj, the scheme file, and the test file.
  Do not create any Search/ file, fixture, or service code in this task — those belong to plans 03-02/03-03 building out from this proven slice.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && test -f CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme && grep -q 'CarTubeTests' CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme && grep -q 'bundle.unit-test' CarTube.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test on scheme CarTube exits with TEST SUCCEEDED and reports Executed 1 test, 0 failures for UtilitiesTests
  - CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme exists, is untracked-to-be-committed or committed, contains the literal CarTubeTests in its TestAction, and lives outside xcuserdata
  - project.pbxproj contains a PBXNativeTarget with productType com.apple.product-type.bundle.unit-test named CarTubeTests, with Debug and Release configurations and a target dependency on CarTube
  - CarTubeTests/UtilitiesTests.swift contains @testable import CarTube and exactly one test method at this point
  - The throwaway ruby script no longer exists on disk (ls *.rb or the script's temp path returns nothing)
  </acceptance_criteria>
  <done>The command xcodebuild test runs one real XCTest against real app source through a committed shared scheme, and the scaffold artifacts are committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Parser test matrix — every URL class, shorts support, regex built once</name>
  <files>CarTubeTests/UtilitiesTests.swift, CarTube/Util/Utilities.swift</files>
  <read_first>
  - .planning/phases/03-search-core/03-PATTERNS.md — "CarTube/Util/Utilities.swift (utility, transform — parser test coverage)" section: the function source, TESTING.md's candidate fixture classes, and the two recommended (not yet applied) hardenings: build the regex once, replace try! with a non-failing construction
  - CarTube/Util/Utilities.swift — extractYouTubeVideoID and isYouTubeURL (verify current line numbers post-Phase-2; only these two functions change)
  - .planning/codebase/CONVENTIONS.md — "Error Handling" section: the guard-optional guidance being applied
  </read_first>
  <reversibility rating="reversible">pure-function change in one file, covered by the new tests; revertible in one commit.</reversibility>
  <behavior>
  - Test: https://m.youtube.com/watch?v=dQw4w9WgXcQ yields dQw4w9WgXcQ
  - Test: https://youtu.be/dQw4w9WgXcQ yields dQw4w9WgXcQ
  - Test: https://www.youtube.com/embed/dQw4w9WgXcQ yields dQw4w9WgXcQ
  - Test: https://www.youtube.com/shorts/dQw4w9WgXcQ yields dQw4w9WgXcQ (RED on the current regex — shorts is not in the path alternation; turns GREEN after the minimal pattern extension below)
  - Test: a youtube.com URL where the ID appears as a non-leading query parameter (&v=... after other params) yields the ID
  - Test: https://www.youtube-nocookie.com/embed/dQw4w9WgXcQ yields the ID
  - Test: an ID containing - and _ characters is extracted intact
  - Test: https://vimeo.com/12345 returns nil; plain text "hello world" returns nil; an empty string returns nil
  - Test: a watch URL with a 10-character or 12-character ID-like token returns nil (the {11} constraint holds)
  </behavior>
  <action>
  Add the behavior-block tests to CarTubeTests/UtilitiesTests.swift, run them RED first (the shorts case fails against the current pattern), then make the surgical change in CarTube/Util/Utilities.swift: extend the path-form alternation (?:v|e(?:mbed)?)\/ to also accept shorts\/ as a path form — adding shorts to that one alternation group, nothing else — and apply the two CONVENTIONS-recommended hardenings in the same edit: hoist the NSRegularExpression into a single lazily-built static constant (regex construction happens once per process, not once per call) and construct it without try! (a non-failing pattern literal means the static can be built with a single guard-and-fatal-style-free form such as NSRegularExpression(pattern:options:) inside an implicitly unwrapped static initialized from a do/catch, or a static optional consulted by the function returning nil when absent — pick the form that keeps the public signature extractYouTubeVideoID(_:) -> String? unchanged). Do not touch isYouTubeURL's signature, any other function in the file, or the duplicated parser in PlayOnCarTube/ShareViewController.swift — the extension dedupe is a recorded planner decision to defer (tech debt, noted again in plan 03-03's Key Decisions). All tests green, then commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && [ "$(grep -c 'func test' CarTubeTests/UtilitiesTests.swift)" -ge 10 ] && grep -q 'shorts' CarTube/Util/Utilities.swift && ! grep -q 'try!' CarTube/Util/Utilities.swift</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED with Executed 10 tests (or more), 0 failures for UtilitiesTests
  - grep -c 'func test' CarTubeTests/UtilitiesTests.swift returns at least 10
  - The regex pattern in Utilities.swift contains the shorts path form and extractYouTubeVideoID still returns String? with unchanged signature
  - grep -c 'try!' CarTube/Util/Utilities.swift returns 0
  - PlayOnCarTube/ShareViewController.swift is untouched (git diff --name-only does not list it)
  </acceptance_criteria>
  <done>The shared parser passes the full input-class matrix including shorts URLs, with a once-built, non-force-tried regex, inside the committed shared-scheme test run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test bundle → app binary | the test target links the app module via BUNDLE_LOADER; no untrusted input crosses — fixtures are repo-owned test inputs |
| ruby gem → pbxproj | local xcodeproj gem 1.28.1 (already installed, Phase 1 precedent) mutates the project file; no network fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | project.pbxproj hand-edited instead of gem-mutated | medium | mitigate | gem-only mutation per house recipe; throwaway script deleted after run; verify command greps for the unit-test product type and shared scheme |
| T-03-02 | Denial of Service | tests hitting the network | low | mitigate | this plan's tests are pure-function tests on extractYouTubeVideoID — zero network; the URLProtocol-stub discipline for the service lands in plan 03-02 |
| T-03-SC | Tampering | package installs | low | accept | no package installs; xcodeproj gem 1.28.1 already installed locally, no registry fetch |
</threat_model>

<verification>
- Task 1 automated command green: first TEST SUCCEEDED of the project, shared scheme committed outside xcuserdata
- Task 2 automated command green: 10+ parser tests pass, shorts supported, no force-try, extension untouched
- git diff scope check: only the four files in files_modified changed
</verification>

<success_criteria>
- xcodebuild test is a working, reproducible verification command for the repo (SRCH-04 target half)
- The shared URL parser is regression-locked by its input-class matrix (SRCH-04 parsing half)
- The scaffold (target + scheme + linking) is proven before any search code exists, so plans 03-02/03-03 only ADD files to a working pipeline
</success_criteria>

## Artifacts this phase produces (plan 01)

- CarTubeTests unit-test target in project.pbxproj (unit-test product type, Debug+Release configs, BUNDLE_LOADER link to the app binary, no TEST_HOST)
- CarTube.xcodeproj/xcshareddata/xcschemes/CarTube.xcscheme — shared scheme whose TestAction runs CarTubeTests (consumed by plans 03-02, 03-03 and every later phase's verification)
- CarTubeTests/UtilitiesTests.swift — parser input-class matrix (consumed as the SRCH-04 "shared URL parsing" evidence)
- Hardened extractYouTubeVideoID: shorts path form, once-built static regex, no try! (signature unchanged; used by the app's cartube:// flow and by Phase 4's coordinator)
- The working verify command shape xcodebuild test -scheme CarTube ... (house convention from here on)

<output>
Create .planning/phases/03-search-core/03-01-SUMMARY.md when done
</output>
