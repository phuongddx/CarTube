---
context: phase
phase: 03-search-core
task: null
total_tasks: null
status: in_progress
last_updated: 2026-08-18T16:09:26.019Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| SourceKit false-positive collateral | Live diagnostics claiming "No such module 'UIKit'/'XCTest'" or "Cannot find X in scope" appeared repeatedly after every new file/edit this session, even for symbols confirmed present by direct grep and by a real successful `xcodebuild build`/`test` run. Root cause: this session's Xcode 26.3/simulator-SDK-mismatch history (now fixed) plus SourceKit's live-index lag. | advisory | Never chase these as regressions before running the actual `xcodebuild build` / `xcodebuild test` and checking its real exit status. Confirmed pattern across 4+ occurrences this session (CarPlaySceneDelegate.swift, Debug.swift, LastQueryCache.swift, SearchFallback.swift, etc.) — the real build always succeeded regardless. |
| xcodeproj Ruby gem save quirks | Every `project.save` call via the ruby xcodeproj gem in this session dropped `dstSubfolder = PlugIns;` from the "Embed Foundation Extensions" PBXCopyFilesBuildPhase, and sometimes added a spurious empty `dependencies = ();` array to a target that had none. | advisory | Diff `project.pbxproj` immediately after every gem invocation; restore the dropped attribute and remove the spurious array before committing. Plans 02-04, 03-01, and 03-02 all hit this independently. |
| Secrets.xcconfig can vanish mid-session | The gitignored root `Secrets.xcconfig` (holding the sentinel `YOUTUBE_API_KEY = BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY`) disappeared from disk mid-Phase-2 with no clear culprit (likely an earlier agent's environment troubleshooting, e.g. a `git clean`). A build failed with "Unable to open base configuration reference file" until it was recreated from the documented sentinel value (see `.planning/phases/01-external-dependencies-project-setup/01-01-PLAN.md` line ~75). | advisory | If a build suddenly fails with that exact error, recreate the file with the sentinel line — do not assume the real key was supposed to be there. |

**Do not proceed until the above is understood — no boxes require checking, these are advisory, not blocking.**

<current_state>
Phase 3 (Search Core) — all 3 plans executed, summarized, and committed (03-01, 03-02, 03-03). 38 unit tests passing (`TEST SUCCEEDED`). No `03-VERIFICATION.md` exists yet. This matches the execute-phase "#2868 all plans summarized but no VERIFICATION.md" resume path exactly — the next `/gsd-execute-phase 3` run should skip straight to the phase-close gates (aggregate_results → code_review_gate → regression_gate → verify_phase_goal → security → transition), the same sequence just completed for Phase 2.
</current_state>

<completed_work>

Completed Plans:
- 03-01: CarTubeTests unit-test target + shared scheme + hardened `extractYouTubeVideoID` (13 tests) — Done, commit `8a0117b`
- 03-02: `YouTubeSearchService` + `SearchResult`, fixture/MockURLProtocol-based TDD, zero real network calls (26 tests) — Done, commit `be4d1fc`
- 03-03: `LastQueryCache` + `SearchFallback` (12 tests) + dev-key runbook + 3 Key Decision rows. Task 4 checkpoint resolved: user chose "skip smoke this phase" — Done, commit `22165f8`
</completed_work>

<remaining_work>

Phase-close pipeline for Phase 3 (none started):
- Code review (deep depth) via `gsd-code-reviewer` — scope: files changed across 03-01/02/03 (Search/, CarTubeTests/, pbxproj, docs/runbooks/google-dev-key.md)
- Regression gate — Phase 2's test files (scan-private-apis TDD suite) should be checked for cross-phase breakage; also worth explicitly re-running Phase 2's `scripts/tests/test-scan-private-apis.sh` since Phase 3 didn't touch those files but the gate step should still run per its own discovery logic
- `verify_phase_goal` — spawn `gsd-verifier` against SRCH-01/02/03/04 and the phase's 5 roadmap success criteria (see `.planning/ROADMAP.md` Phase 3 section)
- Security threat audit via `/gsd-secure-phase 3` (all 3 plans have `<threat_model>` blocks — `register_authored_at_plan_time: true`; check for the short-circuit rule at ASVS L1)
- UAT via `/gsd-verify-work 3` if the verifier reports `human_needed`
- Transition to Phase 4 (Carplay Search Surface) — update `PROJECT.md` (validate SRCH-01..04, log the 3 new Key Decisions' outcomes), `ROADMAP.md`, `STATE.md`
</remaining_work>

<decisions_made>

- Fixed 2 of 3 critical Phase-2 code-review findings (CR-01 idle-timer resync, CR-03 bundle-ID/team consistency across all 4 build configs → K2TYLYAWMK); left CR-02 (HideScrollBar Debug-row may false-PASS) and all 7 warnings + 3 info findings unfixed by explicit user choice, tracked in `PROJECT.md` Active and `02-REVIEW.md`.
- Installed a matching iOS 26.3.1 simulator runtime to fix the Xcode 26.3/SDK-26.2-vs-only-26.5-runtime mismatch that had blocked real `BUILD SUCCEEDED` since plan 02-01. User approved after I proactively killed a riskier `xcodebuild -downloadPlatform` background command I'd started without asking first.
- Phase 3's live smoke search (Task 4 of plan 03-03) deferred to Phase 4 by explicit user decision — neither the dev key nor the shipping key exists yet, so any live path would've meant a fresh Google Cloud Console detour mid-phase.
</decisions_made>

<blockers>
- Apple CarPlay entitlement (Case-ID 21672656): still pending, no SLA — external, no action possible right now.
- Google shipping key: Phase 1 plan 01-03's Task 2 checkpoint (Google Cloud Console, human-only) is still open. Doesn't block Phase 3's own completion (all tests are fixture-based).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable mirror of this file
2. `.planning/STATE.md` — current position, blockers, decisions log
3. `.planning/phases/03-search-core/03-0{1,2,3}-SUMMARY.md` — what each plan actually built
4. `.planning/phases/02-severance-signing-modernization/02-VERIFICATION.md` and `02-SECURITY.md` — the exact phase-close pipeline shape to replicate for Phase 3 (verifier independently re-ran the build/tests rather than trusting SUMMARY claims — do the same for Phase 3)

## Infrastructure State
- Xcode/Simulator: healthy. iPhone 17e simulator, id `F14D9B48-EF6B-4ACD-BB09-2D5951BF5D0A`, iOS 26.3.1, is the working destination for `xcodebuild build`/`test`. CarTube app installed on it as of this session.
- Git: branch `gsd/v1.0-milestone`, local HEAD far ahead of unpushed `origin/HEAD` — worktree isolation auto-degrades to sequential (`none`) for every wave in this state; this is expected and does not need fixing before resuming.
- `.claude/.ckignore` has a project-local `!build` negation added this session to work around a scout-block hook false-positive on `xcodebuild ... build` invocations — keep it, it's needed for any future `xcodebuild` call from the shell.
- `Secrets.xcconfig` exists locally (gitignored) with the sentinel value — do not delete.

<context>
This was a single long orchestration session driving /gsd-manager → Phase 2 full close-out → Phase 3 plan execution, with two AskUserQuestion decision points (Phase 2 code-review fix scope; Phase 3 live-smoke checkpoint). Paused specifically because of a harness context-usage warning (~70%), not a technical blocker — recommend the resuming session start fresh with `/clear` then `/gsd-execute-phase 3` per next_action below.
</context>

<next_action>
Start with: `/gsd-execute-phase 3` — it detects all 3 plans summarized with no VERIFICATION.md and resumes directly at the phase-close gates.
</next_action>
