---
phase: 03-search-core
plan: 03
type: tdd
wave: 3
depends_on: ["03-02"]
files_modified:
  - CarTube/Search/LastQueryCache.swift
  - CarTube/Search/SearchFallback.swift
  - CarTubeTests/LastQueryCacheTests.swift
  - CarTubeTests/SearchFallbackTests.swift
  - docs/runbooks/google-dev-key.md
  - .planning/PROJECT.md
  - CarTube.xcodeproj/project.pbxproj
autonomous: false
requirements: [SRCH-02, SRCH-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - A repeat of the identical query returns the cached page with ZERO network requests issued; a new query overwrites the single slot and re-fetches (SRCH-02 last-query cache)
    - Search is explicit-submit only: the service surface exposes one search(query) with no auto/prefetch/retry path anywhere in the Search/ island — verified by grep gates on the service and cache (SRCH-02 explicit-submit)
    - Given (isConfigured, SearchResult-failure-kind), the fallback decision function returns the correct action — apiKeyMissing/apiKeyInvalid/quotaExceeded → degradeToWebviewSearch; success/empty/other → no fallback — as pure fixture-testable data (SRCH-03 fail-closed)
    - The degrade target is documented in the caller contract as CarPlaySingleton.shared.searchVideo(query) — the existing webview path — with the singleton itself untouched this phase (milestone boundary: coordinator wiring is Phase 4)
    - A dev-key Google project exists, separated from the shipping key's project, with its value delivered through the same gitignored Secrets.xcconfig path; a single live smoke search verifies the shipping-key end-to-end against the real API, and the repo still contains zero key-pattern strings after it
    - PROJECT.md Key Decisions record: no user-visible search toggle this phase; share-extension parser dedupe deferred; dev-key separation policy
  artifacts:
    - CarTube/Search/LastQueryCache.swift (single-slot (query, [SearchResult]) store)
    - CarTube/Search/SearchFallback.swift (pure decision function + documented caller contract)
    - CarTubeTests/LastQueryCacheTests.swift + SearchFallbackTests.swift
    - docs/runbooks/google-dev-key.md
    - PROJECT.md Key Decision rows (3)
  key_links:
    - caller (Phase 4 coordinator) → cache read-before-network → service.search → on SearchError → SearchFallback.decide → CarPlaySingleton.shared.searchVideo(query) — the fail-closed chain whose every link except the final singleton call is built and tested this phase
    - dev key → Secrets.xcconfig (dev branch usage) / shipping key → same pipe — the two-project separation guarding the 100/day bucket from development burn
---

<objective>
Close the phase: deliver the quota budget's runtime half (last-query cache, no auto-search surface) and the fail-closed degrade decision as pure tested components; provision the dev key Phase 1 deferred to this phase; run one live smoke search through the finished client; record the planner decisions as Key Decisions.

Implements SRCH-02 (cache + explicit-submit budget) and SRCH-03 (typed-failure → webview-search degradation design + its tested decision core), and consumes the Phase 1 Key Decision that deferred dev-key provisioning to Phase 3.

Purpose: Pitfall 7's quota cliff is survived by budget behavior, not intent — cache-hit-avoidance, submit-only surface, and dev/shipping key separation are what keep 100 calls/day from being burned by development and repeat queries.
Output: cache + fallback components with tests, dev-key runbook + provisioned key, live smoke evidence, Key Decision rows, phase close-out state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-search-core/03-PATTERNS.md
@.planning/phases/03-search-core/03-02-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/phases/01-external-dependencies-project-setup/01-03-PLAN.md
@docs/runbooks/google-youtube-api-key.md
</context>

<tasks>
Execution order (4 tasks — at the checker's accepted ceiling, do not split): tasks run 1 → 2 → 3 → 4 with ONE deliberate overlap: Task 3 step 2 (the live smoke search) must NOT complete before Task 4's blocking checkpoint resolves the key decision — author the runbook and the three Key Decision rows (Task 3 steps 1 and 3) in normal order, but execute the smoke only after the human's "dev key ready" or "use shipping key" resume-signal, and on "skip smoke" drop step 2 per Task 3's fallback criterion.

<task type="auto" tdd="true">
  <name>LastQueryCache — single-slot (query, results) with zero-request hit</name>
  <files>CarTube/Search/LastQueryCache.swift, CarTubeTests/LastQueryCacheTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - .planning/phases/03-search-core/03-PATTERNS.md — "Last-query cache (single-slot store)" section: the cachedVideo triad analog (CarPlaySingleton.swift state/fill/get-clear), the store-on-submit/read-before-network/clear-on-new-query shape, and the no-UserDefaults rule (throwaway results, query never persisted)
  - .planning/phases/03-search-core/03-02-SUMMARY.md — the SearchResult type and MockURLProtocol this cache's tests compose with
  - CarTube/CarPlay/CarPlaySingleton.swift — the cachedVideo precedent for the minimal surface shape (get/set/clear naming style)
  </read_first>
  <reversibility rating="reversible">standalone in-memory type; no persistence, no callers yet — worst case is deletion.</reversibility>
  <behavior>
  - Test: cachedResults(for:) on an empty cache returns nil
  - Test: after store(query:results:), cachedResults(for: sameQuery) returns the stored array, and a DIFFERENT query returns nil (slot is single, keyed by exact string equality)
  - Test: after store(queryB), cachedResults(for: queryA) returns nil — overwrite evicts
  - Test: clear() empties the slot
  - Test: actor-isolation — concurrent access from two tasks does not crash (basic Sendable/actor sanity on simulator)
  </behavior>
  <action>
  Create CarTube/Search/LastQueryCache.swift as a small actor (stateless-service boundary preserved: the cache is the stateful counterpart to the stateless service, per the pattern map's separate-holder rule) exposing exactly three methods: cachedResults(for query: String) async -> [SearchResult]?, store(query: String, results: [SearchResult]) async, clear() async. Exact-string matching, no normalization, no TTL, no UserDefaults. File header in repo style, import Foundation only. Wire file + tests into targets via the throwaway gem script (delete after). Write the behavior tests RED first, implement GREEN, commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'actor LastQueryCache' CarTube/Search/LastQueryCache.swift && ! grep -q 'UserDefaults' CarTube/Search/LastQueryCache.swift && ! grep -E 'import UIKit|import WebKit' CarTube/Search/LastQueryCache.swift && ! grep -rniE 'retry|backoff|prefetch|autosearch' CarTube/Search/</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED with the cache tests included, 0 failures
  - CarTube/Search/LastQueryCache.swift declares actor LastQueryCache with exactly the three-method surface; no UserDefaults; Foundation-only imports
  - No retry/backoff/prefetch/auto-search tokens anywhere in CarTube/Search/ (the must_haves truth-2 grep gate)
  - project.pbxproj lists LastQueryCache.swift in CarTube sources and LastQueryCacheTests.swift in CarTubeTests sources
  </acceptance_criteria>
  <done>A single-slot, actor-isolated, non-persistent cache exists whose hit path needs no network — the SRCH-02 repeat-call avoider, ready for Phase 4's coordinator to slot between submit and service.</done>
</task>

<task type="auto" tdd="true">
  <name>SearchFallback — pure fail-closed decision function with documented caller contract</name>
  <files>CarTube/Search/SearchFallback.swift, CarTubeTests/SearchFallbackTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - .planning/phases/03-search-core/03-PATTERNS.md — "Fail-closed degradation (SRCH-03)" section: the CarPlaySingleton.searchVideo target, the precondition-check-then-route shape, the branch order (key missing/invalid → 403/quota → fall through to searchVideo; empty-success is NOT a failure), and the fixture-testability rule that the branch live in a pure function with the singleton call as the thin edge
  - .planning/research/ARCHITECTURE.md — Pattern 3 "Fail-closed degradation chain" (the exact decision tree) and the Internal Boundaries table (Search → CarPlay one-direction-only; the singleton gains nothing this phase)
  - .planning/research/PITFALLS.md — Pitfall 7 "quota-aware UX" fallback requirement and the Performance Traps row on quota-retry loops (single attempt, hard-stop on quota, fallback — no retry)
  - CarTube/Search/YouTubeSearchService.swift — the SearchError cases the decision function switches over (from plan 03-02)
  </read_first>
  <reversibility rating="reversible">pure function + doc contract; the Phase 4 coordinator consumes it as-is.</reversibility>
  <behavior>
  - Test: decide(keyMissing, query) → .degradeToWebviewSearch
  - Test: decide(failure(.apiKeyInvalid), query) → .degradeToWebviewSearch
  - Test: decide(failure(.quotaExceeded), query) → .degradeToWebviewSearch
  - Test: decide(success([result]), query) → .showResults([result])
  - Test: decide(success([]), query) → .showNoResults — empty success is data, not a failure (Phase 4's inline no-results row)
  - Test: decide(failure(.other("timeout")), query) → .degradeToWebviewSearch — fail-closed: ANY failure degrades, the driver is never left with a dead search (SRCH-03's "never a dead screen" reads broader than 403; the webview path costs zero quota and always works)
  </behavior>
  <action>
  Create CarTube/Search/SearchFallback.swift: enum SearchOutcomeAction { case showResults([SearchResult]); case showNoResults; case degradeToWebviewSearch } plus a pure static func decide(_ outcome: Result<[SearchResult], SearchError>, query: String) -> SearchOutcomeAction implementing the exact tree above (single switch, no side effects, no singleton import — Foundation only). In the same file, a comment block stating the CALLER CONTRACT in plain language: the owner of this decision (Phase 4's SearchCoordinator) executes .degradeToWebviewSearch by calling the existing webview search method searchVideo(query) on the app's CarPlay facade singleton (defined in the CarPlay/ group) — cost zero quota, already works today; SearchFallback itself never imports anything CarPlay (the boundary stays one-directional; this phase delivers the decision, Phase 4 wires the edge). Write the six behavior tests RED → GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'degradeToWebviewSearch' CarTube/Search/SearchFallback.swift && grep -q 'searchVideo' CarTube/Search/SearchFallback.swift && { ! grep -v '^\s*\/\/' CarTube/Search/SearchFallback.swift | grep -q 'searchVideo'; } && ! grep -E 'import UIKit|import WebKit' CarTube/Search/SearchFallback.swift && ! grep -rq 'CarPlaySingleton' CarTube/Search/ && ! grep -rniE 'retry|backoff|prefetch|autosearch' CarTube/Search/</automated>
  </verify>
  <acceptance_criteria>
  - xcodebuild test reports TEST SUCCEEDED with the fallback tests included, 0 failures
  - SearchFallback.swift contains the three-action enum and the pure decide function; every occurrence of the string searchVideo sits on a // comment line (executable code references none of it — verified by stripping comment lines and grepping empty)
  - No UIKit/WebKit imports and no reference to the CarPlay facade singleton type name anywhere under CarTube/Search/
  - project.pbxproj lists SearchFallback.swift + SearchFallbackTests.swift in their targets
  </acceptance_criteria>
  <done>The fail-closed decision core is pure, tested, and documented — every failure kind degrades to the webview search path, and the actual singleton call is deferred to its Phase 4 owner by contract.</done>
</task>

<task type="auto">
  <name>Dev-key runbook + one live smoke search against the real API</name>
  <files>docs/runbooks/google-dev-key.md, .planning/PROJECT.md, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - docs/runbooks/google-youtube-api-key.md — the Phase 1 shipping-key runbook whose console steps this dev-key runbook mirrors (dedicated project, enable, restrict, deliver into Secrets.xcconfig)
  - .planning/research/PITFALLS.md — Pitfall 7 "keep dev testing on a separate Google Cloud project/key from the shipping key" and the Integration Gotches row "one API project per client per policy III.D.1.c"
  - .planning/phases/01-external-dependencies-project-setup/01-03-PLAN.md — Task 3's Key Decision row-writing procedure (baseline row count via git HEAD, append-only)
  - .planning/phases/03-search-core/03-02-SUMMARY.md — the finished service this smoke run exercises
  - .planning/PROJECT.md — the Key Decisions table
  </read_first>
  <reversibility rating="reversible">a runbook doc, a local xcconfig value, and Key Decision rows — all revertible; the smoke search spends exactly 2 units (1 search.list + 1 videos.list) once.</reversibility>
  <action>
  1. Author docs/runbooks/google-dev-key.md mirroring the shipping-key runbook's structure for a SECOND dedicated Google Cloud project (e.g. cartube-dev): create project → enable YouTube Data API v3 → create key → restrict to the API (application restriction optional for a dev key, but note the policy one-project-per-client guidance) → deliver: for local development, temporarily swap the value into root Secrets.xcconfig's YOUTUBE_API_KEY; for CI, pass the dev key via the xcodebuild command-line override documented in Config/Secrets.xcconfig.example. State the separation rule verbatim: the shipping project's 100/day bucket is never used by development builds or tests — fixture tests never call the network, and any live verification uses the dev key.
  2. Live smoke (agent-executed, uses the dev key after the checkpoint below provides it — or the shipping key if the user explicitly chooses to spend 2 units): run a throwaway swift/xcrun snippet or a tiny XCTest (skipped by default in CI, marked with a name prefix Live and excluded from the scheme's TestAction so normal test runs never execute it) that constructs YouTubeSearchService with the bundle key and awaits search(query: "lofi beats") — assert a non-empty result with a filled duration field. Record the query used, the result count, and the observed videos.list duration value in the runbook's verification section (values observed, not fabricated).
  3. Append THREE Key Decision rows to .planning/PROJECT.md (baseline-gated append-only, same procedure as Phase 1): (a) No user-visible search toggle shipped in Phase 3 — rationale: SRCH-01..04 name no toggle; quota budget is enforced in code (explicit-submit surface, one-page, cache); the UX-level toggle decision belongs to Phase 4's coordinator; (b) Share-extension parser dedupe deferred — rationale: dedupe requires a pbxproj target-membership change for zero Phase 3 value; tech debt recorded; revisit when voice/Siri handoff lands (Pitfall 4's parser-drift warning noted); (c) Dev key separated into a dedicated Google Cloud project — rationale: development and CI must not burn the shipping project's shared 100/day bucket (Pitfall 7); fixture tests make quota-spend-in-tests structurally impossible.
  </action>
  <verify>
    <automated>grep -q 'cartube-dev\|dedicated' docs/runbooks/google-dev-key.md && grep -q 'Secrets.xcconfig' docs/runbooks/google-dev-key.md && ! grep -nE 'AIza[0-9A-Za-z_-]{30,}' docs/runbooks/google-dev-key.md && BEFORE=$(git show HEAD:.planning/PROJECT.md | grep -cE '^\| ') && AFTER=$(grep -cE '^\| ' .planning/PROJECT.md) && [ $((AFTER-BEFORE)) -eq 3 ] && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}'</automated>
  </verify>
  <acceptance_criteria>
  - docs/runbooks/google-dev-key.md exists, mirrors the shipping runbook's steps, names the Secrets.xcconfig delivery path, contains the separation rule, and zero AIza-pattern strings
  - A live smoke search ran end-to-end through YouTubeSearchService with observed results (non-empty, duration filled) recorded in the runbook — OR, if the user declined any live spend at the checkpoint, the runbook records that decision explicitly and the smoke is marked deferred-to-first-Phase-4-run
  - PROJECT.md Key Decisions gains exactly 3 rows (AFTER−BEFORE == 3) covering: no-toggle-this-phase, parser-dedupe-deferred, dev-key-separation
  - git grep AIza gate returns zero matches across tracked files after all edits
  - If a Live-prefixed test was used: it is excluded from the scheme TestAction, and the normal xcodebuild test run still reports TEST SUCCEEDED without executing it
  </acceptance_criteria>
  <done>The dev/shipping key separation is runbooked and Key-Decisioned, one live search proves the client against the real API with a filled duration, and repo hygiene stays clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Provision dev key (optional) and approve the live smoke spend</name>
  <what-built>The agent built the full dev-key runbook, the quota-budget components, and the fail-closed fallback; the live smoke search needs a real key in Secrets.xcconfig and an explicit decision to spend 2 quota units. CI and unit tests never need this — they are fixture-only by construction.</what-built>
  <read_first>
  - docs/runbooks/google-dev-key.md (the steps to follow)
  - docs/runbooks/google-youtube-api-key.md (the Phase 1 precedent: same console flow)
  </read_first>
  <action>
  Human chooses ONE of three paths and reports it: (1) follow docs/runbooks/google-dev-key.md now — create the cartube-dev project, restrict the key, paste it into Secrets.xcconfig (replacing the shipping value locally; the shipping value stays recorded wherever the user keeps it, e.g. 1Password — chat never needs the key itself) and confirm "dev key ready"; (2) approve spending 2 units from the SHIPPING key now for the smoke — reply "use shipping key"; (3) skip live verification this phase — reply "skip smoke" and the agent records the deferral per Task 3's fallback criterion. Then confirm the three Key Decision rows read correctly.
  </action>
  <how-to-verify>
  1. Open docs/runbooks/google-dev-key.md and follow it if choosing option 1.
  2. The chosen key value lives only in the gitignored Secrets.xcconfig — verify with git status (must NOT appear) and git check-ignore -q Secrets.xcconfig.
  3. Report the chosen option; if option 1, also report the dev project ID so the agent can note it in the runbook.
  4. Read the three new PROJECT.md Key Decision rows and flag any wording to change.
  </how-to-verify>
  <resume-signal>Type "dev key ready" (+ project ID), "use shipping key", or "skip smoke", plus any Key Decision wording fixes</resume-signal>
  <acceptance_criteria>
  - The user's chosen path is recorded in the runbook (or its SUMMARY) — dev key ID, shipping-spend approval, or deferral
  - If any live path was chosen: the smoke evidence exists in the runbook's verification section
  - Secrets.xcconfig remains gitignored and untracked regardless of path
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Cloud Console ↔ repo | dev-key creation is human-only behind Google auth; the repo owns only the runbook and the gitignored delivery slot |
| dev/shipping key projects | the 100/day shipping bucket must never be touched by development usage — separation is the control |
| live smoke → production quota | the one deliberate spend (2 units) crosses the boundary explicitly, at a blocking checkpoint, never silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Information Disclosure | dev key committed to the public fork | high | mitigate | dev key lands only in gitignored Secrets.xcconfig (same slot, swapped locally); AIza grep gate re-run in Task 3 and the checkpoint; runbook carries no key material |
| T-03-08 | Denial of Service (quota) | development/CI burns the shipping 100/day bucket | high | mitigate | dedicated dev project per Pitfall 7; fixture-only unit tests (structurally network-free via MockURLProtocol); the ONLY live spend is the checkpoint-approved 2-unit smoke |
| T-03-09 | Repudiation | live smoke result recorded without evidence | low | mitigate | runbook verification section records observed values (result count, duration string, query) from the actual run; fabrication is a verification failure |
| T-03-10 | Denial of Service | quota-retry loop on 403 | medium | mitigate | service has no retry path (single attempt, one page); SearchFallback hard-stops to webview degrade — grep gate: no retry/backoff tokens in Search/ |
| T-03-SC | Tampering | package installs | low | accept | no package installs; xcodeproj gem already local |
</threat_model>

<verification>
- Task 1 automated: cache tests pass; actor + three-method surface; no UserDefaults; Foundation-only
- Task 2 automated: fallback decision tests pass; searchVideo appears only in the contract comment; Search/ has zero executable CarPlay references
- Task 3 automated: runbook greps, 3-row Key Decision delta vs git HEAD baseline, AIza gate zero
- Checkpoint: chosen path recorded; gitignore holds; smoke evidence or explicit deferral
- Phase gate: full suite TEST SUCCEEDED; every SRCH requirement's success criterion traceable to a green test or a recorded artifact
</verification>

<success_criteria>
- SRCH-02: repeat identical query → zero network (test-proven); one page per search (surface-proven); explicit-submit only (grep-proven: no auto-search path)
- SRCH-03: every failure kind → webview-search degrade decision (test-proven); the existing path documented as the caller contract, singleton untouched
- Dev/shipping key separation runbooked, Key-Decisioned, and (if chosen) exercised by one live smoke
- Repo hygiene: zero key-pattern strings in tracked files at phase close
</success_criteria>

## Artifacts this phase produces (plan 03)

- CarTube/Search/LastQueryCache.swift — actor, single-slot exact-match (query, [SearchResult]) cache with cachedResults(for:)/store/clear (consumed by Phase 4's coordinator between submit and service call)
- CarTube/Search/SearchFallback.swift — pure decide(Result<[SearchResult], SearchError>) -> SearchOutcomeAction {showResults, showNoResults, degradeToWebviewSearch} + the caller contract naming CarPlaySingleton.shared.searchVideo(query) as the degrade edge (wired in Phase 4)
- CarTubeTests/LastQueryCacheTests.swift + CarTubeTests/SearchFallbackTests.swift
- docs/runbooks/google-dev-key.md — dev-project console steps, Secrets.xcconfig delivery, separation rule, live-smoke verification evidence
- .planning/PROJECT.md — three Key Decision rows: no Phase 3 search toggle, share-extension parser dedupe deferred, dev-key project separation
- Phase 3 close-out: Search/ island complete (model, service, cache, fallback — Foundation-only, fixture-tested), quota budget structural, degrade chain decided

<output>
Create .planning/phases/03-search-core/03-03-SUMMARY.md when done
</output>
