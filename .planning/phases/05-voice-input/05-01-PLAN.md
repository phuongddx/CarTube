---
phase: 05-voice-input
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CarTube/Info.plist
  - CarTube/Speech/VoiceSearchAvailability.swift
  - CarTube/Speech/SpeechRecognizerService.swift
  - CarTube/CarPlay/MicButton.swift
  - CarTube/CarPlay/CarPlayViewController.swift
  - CarTube/Views/Debug.swift
  - CarTubeTests/SpeechRecognizerServiceTests.swift
  - CarTubeTests/SpeechAvailabilityGateTests.swift
  - CarTubeTests/SilenceTimerTests.swift
  - CarTube.xcodeproj/project.pbxproj
autonomous: true
requirements: [VOX-01, VOX-02]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Holding the mic button starts listening, releasing submits the final transcript through the existing funnel — SearchCoordinator.search via the existing CarPlaySingleton.submitSearchQuery passthrough — and the Phase 4 overlay appears with its 'Searching…' loading row; one tap plays (phase goal; VOX-01; UI-SPEC loading transcript→results handoff row)"
    - "The mic button — 56pt circular UIButton, mic.fill 24pt white icon, #1C1C1E idle fill, 28pt corner radius, pinned 16pt below the top safe-area edge and 16pt from the trailing edge — is visible iff speech status is .authorized AND mic permission granted AND on-device recognition supported; it is hidden while the Phase 4 results overlay is visible and reappears on overlay dismissal if still authorized (VOX-01; UI-SPEC zero-one-many mic-button row: exactly one button or none)"
    - "The listening state is visible at a glance: fill flips to systemRed #FF3B30, a 0.8s autoreverse pulse (scale 1.0↔1.1) runs, and the 'Listening…' pill appears below the button from touch-down until session end (VOX-01; UI-SPEC loading mic-button row; 2.5.14)"
    - "The recognition service exists only when the gate passes — a nil SFSpeechRecognizer(locale:) or supportsOnDeviceRecognition == false never constructs a request; every constructed request sets requiresOnDeviceRecognition = true; no code path anywhere falls back to server-based recognition (05-RESEARCH.md Pitfall 5; ARCHITECTURE.md Anti-Pattern 5; VOX-01)"
    - "Auto-stop is app-side because buffer recognition never finalizes on its own: ≥1.8s without a partial-transcript change (with at least one recognized word) finalizes and submits exactly as touch-up would; a 10.0s hard cap bounds every session; one press = one session, no re-arm while held (VOX-01; 05-RESEARCH.md Pitfall 2 — corrected from the UI-SPEC's original Q3 assumption)"
    - "Touch-up — .touchUpInside, .touchUpOutside, .touchCancel, all identical — stops listening and submits the final transcript; an empty transcript shows the 'Didn't catch that' hint pill for 1.5s with a 0.2s fade-out, never shows the results overlay, and spends no API call (VOX-01; UI-SPEC error mic-button row)"
    - "Recognition failures map to spec copy: kAFAssistantErrorDomain 1110 → 'Didn't catch that'; 1700/1101/1107/1100 and kLSRErrorDomain 102/201 → 'Voice search unavailable' hint pill plus a visibility re-check that hides the button if on-device support is gone (05-RESEARCH.md Pitfall 6; UI-SPEC error mic-button availability row)"
    - "The audio session coexists with webview playback: category .playAndRecord with options [.mixWithOthers, .defaultToSpeaker], mode .default, active only while listening, torn down with removeTap → engine stop → endAudio → setActive(false, options: .notifyOthersOnDeactivation); an interruption .began event stops listening without auto-resume (05-RESEARCH.md Pitfall 4; research fact 3)"
    - "Both purpose strings — NSMicrophoneUsageDescription 'CarTube uses the microphone so you can search YouTube by voice from CarPlay.' and NSSpeechRecognitionUsageDescription 'CarTube converts your speech to text on your device to search YouTube from CarPlay.' — are committed in the same commit as the first speech code (VOX-02; research fact 6: the app crashes otherwise)"
    - "CarPlaySingleton gains zero new methods and the keyboard/Siri paths are untouched — the transcript rides the existing submitSearchQuery passthrough from 04-01 (UI-02 cap; milestone hard rule)"
    - "The mic surface is drivable on the phone simulator regardless of the pending CarPlay entitlement: a Debug 'Voice Search Preview' section embeds the production MicButton and drives the real service (entitlement contingency from the roadmap note)"
  artifacts:
    - CarTube/Info.plist — both purpose strings (VOX-02)
    - CarTube/Speech/VoiceSearchAvailability.swift — VoiceSearchState enum (needsOnboarding/ready/limited/denied) + pure evaluate(speechStatus:micStatus:onDeviceSupported:) + onDevice support probe; no UIKit imports
    - CarTube/Speech/SpeechRecognizerService.swift — construction-gated push-to-talk service: AVAudioEngine input tap → SFSpeechAudioBufferRecognitionRequest, silence timer with injectable clock, error mapping, injectable engine/task/onSubmit seams
    - CarTube/CarPlay/MicButton.swift — 56pt mic button + 'Listening…' pill + hint pills + pulse animation as one reusable UIView
    - CarPlayViewController wiring — micButton property, insertSubview below screenOffLabel, visibility gate refresh, touch-down/touch-up → service, overlay-interplay hiding
    - CarTube/Views/Debug.swift — 'Voice Search Preview' section (phone-simulator drivable)
    - CarTubeTests/SpeechRecognizerServiceTests.swift + SpeechAvailabilityGateTests.swift + SilenceTimerTests.swift
  key_links:
    - "MicButton touch-down → SpeechRecognizerService.startListening (gate asserts on-device support again at start) → session activate + engine tap + buffer request"
    - "Touch-up OR silence timer (1.8s/10.0s) → finalize: removeTap → engine stop → endAudio → session deactivate → final transcript → onSubmit → CarPlaySingleton.shared.submitSearchQuery(transcript) → SearchCoordinator.search → Phase 4 overlay 'Searching…' row → tap plays via loadUrl(YT_EMBED + videoId)"
    - "VoiceSearchAvailability.evaluate(speechStatus, micStatus, onDeviceSupported) → CarPlayViewController.refreshMicButtonVisibility → micButton.isHidden (consumed by 05-02's onboarding screen for the same verdict)"
    - "Recognition error → SpeechRecognizerService.mapError → VoiceHint (.noSpeech / .unavailable) → MicButton.showHint (1.5s pill, 0.2s fade) → visibility re-check"
---

<objective>
Tracer-first push-to-talk: a driver holds the mic button on the CarPlay screen, speaks, releases — and the spoken transcript lands on the finished Phase 4 funnel (SearchCoordinator.search → results overlay → one-tap play). This plan delivers VOX-01 (on-device recognition, gated visibility, visible listening state, app-side silence auto-stop) and the purpose-string half of VOX-02 (both keys committed with the first speech code).

Research corrections honored: silence auto-stop is an app-side timer (buffer recognition NEVER self-finalizes — 05-RESEARCH.md Pitfall 2), and the service is construction-gated on supportsOnDeviceRecognition because requiresOnDeviceRecognition alone fails silently to server recognition, which is forbidden (Pitfall 5).

Purpose: This is the phase's architectural slice — Speech/ group, MicButton component, visibility gate, and error taxonomy are decided here; 05-02 (onboarding) and 05-03 (Siri) build on VoiceSearchAvailability and the proven transcript path.
Output: SpeechRecognizerService + VoiceSearchAvailability + MicButton + CarPlay wiring + purpose strings + three test files, all target-wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-input/05-UI-SPEC.md
@.planning/phases/05-voice-input/05-RESEARCH.md
@.planning/phases/05-voice-input/05-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/04-carplay-search-surface/04-01-PLAN.md
@CarTube/CarPlay/CarPlayViewController.swift
@CarTube/CarPlay/CarPlaySingleton.swift
@CarTube/Views/Debug.swift
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end push-to-talk — one path: hold mic → transcript → SearchCoordinator → Phase 4 overlay; purpose strings in the same commit</name>
  <files>CarTube/Info.plist, CarTube/Speech/VoiceSearchAvailability.swift, CarTube/Speech/SpeechRecognizerService.swift, CarTube/CarPlay/MicButton.swift, CarTube/CarPlay/CarPlayViewController.swift, CarTubeTests/SpeechRecognizerServiceTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>Phase 2–4 artifacts landed and compiling: CarTube/Search/SearchCoordinator.swift with the search(_:) funnel and injectable seams, CarPlaySingleton.submitSearchQuery passthrough, SearchResultsViewController overlay wired below screenOffLabel, CarTubeTests target with the shared CarTube scheme, deployment target 16.0 (grep IPHONEOS_DEPLOYMENT_TARGET = 16.0 in project.pbxproj), and CarTube/Views/Debug.swift exists. Halt and report if any are missing — this plan never rebuilds them.</precondition>
  <read_first>
  - .planning/phases/05-voice-input/05-UI-SPEC.md — IN THIS ORDER: Copywriting Contract (exact strings: 'Listening…', 'Didn't catch that', 'Voice search unavailable', both purpose strings, accessibilityLabel 'Voice search' / hint 'Hold to search by voice'); Spacing exceptions (56pt button, 28pt radius, 16pt insets, ≥44pt targets); Color table (#1C1C1E idle fill, systemRed #FF3B30 listening fill only, black pill backgrounds); Typography (17pt semibold 'Listening…'); Component Inventory rows 1–3; Interaction Contract (touch-down/up rows, auto-stop row, empty-transcript row, overlay-visibility row, denied/unsupported row); Z-order block (micButton between resultsController.view and screenOffLabel); hard constraints (no prompts from the CarPlay scene, webview untouched)
  - .planning/phases/05-voice-input/05-RESEARCH.md — Pattern 1 (verified push-to-talk lifecycle with every API call annotated); Pattern 3 (VoiceSearchAvailability pure gating — the exact enum shape); Pitfall 2 (no system end-of-speech — app-side timer), Pitfall 4 (audio session coexistence), Pitfall 5 (construction gate), Pitfall 6 (nil recognizer, error taxonomy); Architecture Map (Availability gating must be a pure function outside UIKit); User Constraints block (binding spec decisions)
  - .planning/phases/04-carplay-search-surface/04-01-PLAN.md — the funnel contract being landed on: submitSearchQuery passthrough, SearchCoordinator.search(_:) blank-query guard, overlay insertion below screenOffLabel
  - CarTube/CarPlay/CarPlayViewController.swift — viewDidLoad wiring region (results controller insertion after screenOffLabel creation, before splash), showSearchResults/dismissSearchResults methods to extend with mic visibility
  - CarTube/CarPlay/CarPlaySingleton.swift — the submitSearchQuery passthrough (reuse; add nothing)
  </read_first>
  <reversibility rating="costly">the Speech/ group boundary, MicButton component API, and VoiceSearchAvailability verdicts become the contract 05-02's onboarding screen and 05-03's intent build against — reworking them after those land touches three plans.</reversibility>
  <behavior>
  - Test (funnel routing, written RED first): a SpeechRecognizerService built with a stubbed recognition-task seam emits a final result 'lofi beats'; driving startListening() then stopListening() delivers exactly one onSubmit callback with the transcript 'lofi beats', and a SearchCoordinator instance with spy seams records the presenter sequence [loading, results] from that transcript — proving hold→transcript→funnel→overlay end to end with zero live audio
  - Test (construction gate): building the service with the seam reporting on-device support false yields a nil/unavailable service — startListening() is a no-op and no request is ever constructed
  - Test (empty transcript): a session that ends with no recognized text delivers the empty outcome (not a transcript) — onSubmit is never called, so no search is issued and no API call is spent
  </behavior>
  <action>
  Write the three funnel-routing tests RED in CarTubeTests/SpeechRecognizerServiceTests.swift against a service stub, then build the one path production-quality:

  1. CarTube/Info.plist FIRST — add both purpose strings with the exact UI-SPEC copy (NSMicrophoneUsageDescription, NSSpeechRecognitionUsageDescription). These ship in this same commit as all first speech code; the task's automated verify fails the commit if either key is absent (research fact 6: requesting authorization without a key crashes the app).

  2. CarTube/Speech/VoiceSearchAvailability.swift — enum VoiceSearchState { case needsOnboarding, ready, limited, denied } and static func evaluate(speechStatus: SFSpeechRecognizerAuthorizationStatus, micStatus: AVAudioSession.RecordPermission, onDeviceSupported: Bool) -> VoiceSearchState implementing the Pattern 3 switch verbatim: any notDetermined/undetermined input → needsOnboarding; authorized+granted with support → ready; authorized+granted without support → limited; everything else (denied, restricted, partial grants) → denied. Plus a static on-device probe helper returning SFSpeechRecognizer(locale:)?.supportsOnDeviceRecognition ?? false (nil recognizer → false). Imports Speech and AVFAudio only — no UIKit; this file is the single source of truth both the mic button (this plan) and the phone onboarding screen (05-02) consume.

  3. CarTube/Speech/SpeechRecognizerService.swift — final class with an internal init taking injectable seams (an audio-engine protocol exposing inputNode tap install/remove, prepare/start/stop; a recognition-task factory protocol; an onSubmit: (String) -> Void closure; a clock closure () -> Date defaulting to Date.init). The initializer enforces the construction gate: when the recognizer is nil or its supportsOnDeviceRecognition is false, the service is created in an unavailable state and startListening() is a no-op that reports the unavailable outcome — a request object must never be constructed past a failed gate (Pitfall 5). startListening(): assert the gate again, configure AVAudioSession.sharedInstance() with setCategory(.playAndRecord, mode: .default, options: [.mixWithOthers, .defaultToSpeaker]) then setActive(true), create SFSpeechAudioBufferRecognitionRequest with shouldReportPartialResults = true and requiresOnDeviceRecognition = true, install the input tap (bufferSize 1024) appending buffers, prepare and start the engine, and launch recognitionTask with a result handler that tracks the latest result and surfaces isFinal. stopListening() finalizes: removeTap, engine stop, request.endAudio(), try? session.setActive(false, options: .notifyOthersOnDeactivation), then deliver the final transcript via onSubmit — or the empty outcome when nothing was recognized. Main-queue marshaling before any UI-adjacent callback, matching the repo's dispatch discipline. Repo header comment, module CarTube.

  4. CarTube/CarPlay/MicButton.swift — final class MicButton: UIView owning a UIButton (type .custom) sized 56×56pt — width/height as the 56.0 CGFloat literal, matching the verify gate — corner radius 28, white mic.fill UIImage(systemName:) pointSize 24 weight .semibold, idle fill UIColor(red: 28/255.0, green: 28/255.0, blue: 30/255.0, alpha: 1); closures onTouchDown/onTouchUp wired to touch-down and all three touch-up events identically; setListening(true) flips the fill to .systemRed, starts a CABasicAnimation transform scale 1.0↔1.1 duration 0.8 autoreverse repeat, and shows the 'Listening…' pill — a UILabel 17pt semibold white on a black pill (corner radius 16, 8pt vertical / 16pt horizontal padding) trailing-aligned 8pt below the button; setListening(false) restores idle fill, removes the pulse, hides the pill. Accessibility label 'Voice search', hint 'Hold to search by voice'. Repo header comment.

  5. CarPlayViewController.swift — add a private micButton property and a private optional SpeechRecognizerService. In viewDidLoad, after the results controller insertion and before the splash block: construct MicButton, frame 56×56 pinned 16pt below the top safe-area edge and 16pt from the trailing edge (top-anchoring per the UI-SPEC; CarPlay scenes have no notch — use the safe-area inset with a 16pt offset), insertSubview(belowSubview: screenOffLabel) exactly as the Phase 4 reservation note specifies, initial isHidden = true. Wire micButton.onTouchDown → service.startListening + micButton.setListening(true); onTouchUp → service.stopListening + micButton.setListening(false). The service is created only when the availability verdict is .ready, with onSubmit routed through CarPlaySingleton.shared.submitSearchQuery — zero new singleton methods. Add refreshMicButtonVisibility() evaluating VoiceSearchAvailability with live system statuses and setting micButton.isHidden accordingly; call it from viewDidLoad end, viewWillAppear, and from both showSearchResults (hide — overlay/mic never co-visible, blocks a second in-flight voice search and double-submits) and dismissSearchResults (re-show if still authorized). No webview interaction of any kind in the new code.

  6. Target-wire the four source files and the test file via the Phase 3/4 throwaway ruby-gem pbxproj procedure, then delete the script. GREEN → single commit containing the purpose strings and all first speech code.
  </action>
  <verify>
    <automated>plutil -extract NSMicrophoneUsageDescription raw CarTube/Info.plist | grep -q 'search YouTube by voice from CarPlay' && plutil -extract NSSpeechRecognitionUsageDescription raw CarTube/Info.plist | grep -q 'converts your speech to text on your device' && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test -only-testing:CarTubeTests/SpeechRecognizerServiceTests 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'requiresOnDeviceRecognition = true' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'mixWithOthers' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'defaultToSpeaker' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'notifyOthersOnDeactivation' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'endAudio' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'submitSearchQuery' CarTube/CarPlay/CarPlayViewController.swift && grep -q 'belowSubview: screenOffLabel' CarTube/CarPlay/CarPlayViewController.swift && grep -F 'mic.fill' CarTube/CarPlay/MicButton.swift | grep -q . && grep -q '56.0' CarTube/CarPlay/MicButton.swift && grep -q 'CABasicAnimation' CarTube/CarPlay/MicButton.swift && grep -q '0.8' CarTube/CarPlay/MicButton.swift && grep -F 'Listening…' CarTube/CarPlay/MicButton.swift | grep -q . && grep -q 'Voice search' CarTube/CarPlay/MicButton.swift && [ "$(git diff HEAD~1 -- CarTube/CarPlay/CarPlaySingleton.swift | wc -l)" = "0" ] && ! grep -q 'import UIKit' CarTube/Speech/VoiceSearchAvailability.swift</automated>
    <human-check>Entitlement contingency: the hold→transcript→overlay path is drivable on the phone via the Debug 'Voice Search Preview' section added in Task 3; the on-CarPlay visual pass rides the Phase 1 dated blocker and the 05-03 checkpoint.</human-check>
  </verify>
  <acceptance_criteria>
  - Both purpose strings extract non-empty from CarTube/Info.plist with the exact UI-SPEC copy (plutil gates) and are part of the same commit as the first speech code
  - SpeechRecognizerServiceTests green: funnel routing [loading, results] from a stubbed final transcript, construction-gate no-op when support is false, empty-transcript outcome issues no search
  - requiresOnDeviceRecognition = true present exactly where the request is built; .mixWithOthers, .defaultToSpeaker, notifyOthersOnDeactivation, endAudio all present; session activated only on startListening and deactivated on stopListening
  - MicButton: 56pt / radius 28 / mic.fill 24pt semibold / #1C1C1E idle fill / systemRed listening fill / 0.8s CABasicAnimation pulse / 'Listening…' 17pt semibold pill / accessibilityLabel 'Voice search' + hint 'Hold to search by voice' (grep gates)
  - CarPlayViewController: micButton inserted belowSubview screenOffLabel, 16pt insets, visibility gated via VoiceSearchAvailability.evaluate, hidden while overlay visible and restored on dismissal, transcript routed through the existing submitSearchQuery passthrough
  - CarPlaySingleton.swift has zero diff this task (3-method cap intact)
  - VoiceSearchAvailability.swift imports no UIKit (pure verdict function)
  </acceptance_criteria>
  <done>One press of the mic button runs the entire voice path — gate, session, tap, recognition, finalization, funnel — and lands the spoken query on the Phase 4 overlay, with both purpose strings shipped in the same commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Availability matrix + error taxonomy + hint pills — every state and failure covered</name>
  <files>CarTube/Speech/VoiceSearchAvailability.swift, CarTube/Speech/SpeechRecognizerService.swift, CarTube/CarPlay/MicButton.swift, CarTubeTests/SpeechAvailabilityGateTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>Task 1's service, availability function, and MicButton exist and its tests pass.</precondition>
  <read_first>
  - .planning/phases/05-voice-input/05-RESEARCH.md — Pitfall 6 (complete error-code table: kAFAssistantErrorDomain 1110/1700/1101/1107/1100, kLSRErrorDomain 102/201; nil recognizer → limited; legacy SFSpeechRecognizerErrorDomain constants do not compile in the modern SDK — match domains/codes as strings); Pattern 3 switch (the full matrix); Validation Architecture VOX-01 rows
  - .planning/phases/05-voice-input/05-UI-SPEC.md — Copywriting Contract hint-pill rows ('Didn't catch that', 'Voice search unavailable' — 1.5s pills, 0.2s fade); Interaction Contract rows for empty transcript and recognition failure (button visibility re-evaluated on failure; button hides on next visibility check)
  - CarTube/Speech/VoiceSearchAvailability.swift — the enum and evaluate switch from Task 1 (extend, do not reshape)
  - CarTube/Speech/SpeechRecognizerService.swift — the result-handler region where error mapping attaches
  </read_first>
  <reversibility rating="reversible">additive matrix and mapping inside files Task 1 created; worst case is revert of this task's diff.</reversibility>
  <behavior>
  - Tests (gating matrix — every combination): (authorized, granted, true) → ready; (authorized, granted, false) → limited; (notDetermined, *) and (*, undetermined) → needsOnboarding; (restricted, granted, true) → denied; (denied, granted, true) → denied; (authorized, denied, true) → denied; (authorized, denied, false) → denied — partial grants are denied per spec
  - Tests (error mapping): NSError domain kAFAssistantErrorDomain code 1110 → .noSpeech; codes 1700, 1101, 1107, 1100 → .unavailable; NSError domain kLSRErrorDomain codes 102 and 201 → .unavailable; nil-recognizer construction → unavailable/limited verdict, never a server request
  - Test (empty session): a session ending with no recognized text produces the noSpeech outcome — the hint path, not the search path
  </behavior>
  <action>
  Write the matrix and mapping tests RED in CarTubeTests/SpeechAvailabilityGateTests.swift, then complete the two production halves:

  1. VoiceSearchAvailability: verify the full switch against the behavior matrix (Task 1 built the shape; this task pins every cell with tests and fixes any divergence).

  2. SpeechRecognizerService: add an internal static error-mapping function from Error to a VoiceSearchOutcome (transcript / noSpeech / unavailable) implementing the Pitfall 6 table exactly — kAFAssistantErrorDomain 1110 → noSpeech; 1700, 1101, 1107, 1100 → unavailable; kLSRErrorDomain 102 (assets not installed) and 201 (Siri/Dictation disabled) → unavailable — matching on error domain strings and codes, never on legacy SFSpeechRecognizerErrorDomain constants (they do not compile in this SDK). The result handler routes: isFinal → transcript path; mapped noSpeech → empty outcome; mapped unavailable → unavailable outcome plus an onAvailabilityChanged callback so the scene re-checks visibility. No mapping branch ever re-issues a request without the on-device gate.

  3. MicButton: add showHint(_ hint:, onDismiss:) rendering the hint pill (same pill style as 'Listening…') with the exact copy — noSpeech → 'Didn't catch that', unavailable → 'Voice search unavailable' — auto-dismissing after 1.5s with a 0.2s fade-out, then invoking onDismiss so CarPlayViewController can call refreshMicButtonVisibility() (the button hides on the next check when support was withdrawn). GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test -only-testing:CarTubeTests/SpeechAvailabilityGateTests -only-testing:CarTubeTests/SpeechRecognizerServiceTests 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -F "Didn't catch that" CarTube/CarPlay/MicButton.swift | grep -q . && grep -F 'Voice search unavailable' CarTube/CarPlay/MicButton.swift | grep -q . && grep -q '1.5' CarTube/CarPlay/MicButton.swift && grep -q '0.2' CarTube/CarPlay/MicButton.swift && grep -q 'kAFAssistantErrorDomain' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'kLSRErrorDomain' CarTube/Speech/SpeechRecognizerService.swift && grep -q '1110' CarTube/Speech/SpeechRecognizerService.swift && ! grep -rq 'SFSpeechRecognizerErrorDomain' CarTube/Speech/ && [ "$(git diff HEAD~1 -- CarTube/CarPlay/CarPlaySingleton.swift | wc -l)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
  - SpeechAvailabilityGateTests green over the full combination matrix (all seven distinct verdict rows, partial-grant = denied)
  - Error mapping covers every code in the Pitfall 6 table; no legacy SFSpeechRecognizerErrorDomain constant anywhere under CarTube/Speech/ (negative grep)
  - Hint pills render both exact copy strings with 1.5s display and 0.2s fade; dismissal triggers the visibility re-check callback
  - CarPlaySingleton.swift again has zero diff
  </acceptance_criteria>
  <done>Every permission combination and every recognition failure has a defined, unit-proven outcome — and each failure surfaces as the exact UI-SPEC hint copy with a visibility re-check.</done>
</task>

<task type="auto" tdd="true">
  <name>Silence timer + lifecycle hardening + phone-simulator voice preview harness</name>
  <files>CarTube/Speech/SpeechRecognizerService.swift, CarTube/Views/Debug.swift, CarTubeTests/SilenceTimerTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>Task 1's service compiles with the clock seam injected and Task 2's error mapping is in place.</precondition>
  <read_first>
  - .planning/phases/05-voice-input/05-RESEARCH.md — Pitfall 2 (silence finalization algorithm: track last-changed partial timestamp, repeating 0.3s check, ≥1.8s with non-empty transcript → finalize, 10.0s hard cap) and the 'Silence-timed finalize' code example; Pitfall 4 (interruption notification .began → stop listening, never auto-resume unless shouldResume); Validation Architecture VOX-01 silence-timer row (injected clock)
  - .planning/phases/05-voice-input/05-UI-SPEC.md — Interaction Contract auto-stop row (finalization submits exactly as touch-up; one press = one session; no re-arm while held)
  - CarTube/Views/Debug.swift — the existing debug-section button pattern and the Phase 4 'Search Overlay Preview' section this extends
  - CarTube/Speech/SpeechRecognizerService.swift — the result handler and seams from Tasks 1–2
  </read_first>
  <reversibility rating="reversible">timer logic behind an injectable clock inside an existing file; the harness is a DEBUG-surface debug-screen section — both revertible in isolation.</reversibility>
  <behavior>
  - Test (silence finalize): with an injected fake clock, a session holding a non-empty unchanged partial transcript for 1.9s fires finalize exactly once and submits that transcript identically to touch-up
  - Test (no early fire): 1.7s of silence after a transcript change does not finalize
  - Test (hard cap): a session reaching 10.0s total finalizes even with an empty transcript — producing the noSpeech hint outcome, not a stuck listening state
  - Test (no re-arm): after finalization, further timer ticks submit nothing more — exactly one outcome per press; a subsequent startListening begins a fresh session
  - Test (teardown ordering): finalize drives removeTap → engine stop → endAudio → session deactivate in that order, asserted via the protocol spies
  </behavior>
  <action>
  Write the timer tests RED in CarTubeTests/SilenceTimerTests.swift driving the service through its seams with a fake clock, then:

  1. Silence timer: inside startListening, seed lastTranscriptChange from the injected clock and start a repeating 0.3s Timer whose tick calls an internal evaluateSilence(now:) — finalize when now - lastTranscriptChange ≥ 1.8 AND the latest partial transcript is non-empty, and when elapsed ≥ 10.0 regardless (hard cap). The result handler updates lastTranscript/lastTranscriptChange only when formattedString actually changed (the research example's guard). Finalization routes through the exact same finalize path as touch-up (removeTap → stop → endAudio → deactivate → transcript or noSpeech outcome), invalidates the timer, and suppresses re-arming until the next press. Constants 1.8 and 10.0 live as named private constants so the device pass can tune them (research A5).

  2. Interruption hardening: observe AVAudioSession.interruptionNotification; on .began, stop listening through the same finalize path (submitting whatever transcript exists) and never auto-resume on .ended unless shouldResume is true (Pitfall 4). Observer removed on finalize.

  3. Phone-simulator harness: extend CarTube/Views/Debug.swift with a 'Voice Search Preview' section following the existing debug style — a UIViewRepresentable embedding the production MicButton (so the 56pt button, red pulse, and pills render on the phone), wired to a real SpeechRecognizerService whose onSubmit prints the transcript to the console and routes through CarPlaySingleton.shared.submitSearchQuery (which no-ops visually without a CarPlay controller — the console + the funnel tests carry the proof; the harness carries the visuals). When the availability verdict is not .ready (e.g. simulator lacks the on-device model), the section shows the limited-state note instead of the button — the gate itself is the demo. Zero production-surface changes outside Debug.swift. GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test -only-testing:CarTubeTests/SilenceTimerTests -only-testing:CarTubeTests/SpeechRecognizerServiceTests 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q '1.8' CarTube/Speech/SpeechRecognizerService.swift && grep -q '10.0' CarTube/Speech/SpeechRecognizerService.swift && grep -q '0.3' CarTube/Speech/SpeechRecognizerService.swift && grep -q 'interruptionNotification' CarTube/Speech/SpeechRecognizerService.swift && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'Voice Search Preview' CarTube/Views/Debug.swift && grep -q 'MicButton' CarTube/Views/Debug.swift</automated>
  </verify>
  <acceptance_criteria>
  - SilenceTimerTests green: 1.9s finalize, 1.7s no-fire, 10.0s hard cap with empty transcript → noSpeech, single-outcome-per-press, teardown ordering
  - Constants 1.8 / 10.0 / 0.3 present as named constants; interruption .began stops listening without unconditional auto-resume
  - Debug screen builds with the 'Voice Search Preview' section embedding the production MicButton and routing through the real service + existing funnel passthrough
  - CarPlaySingleton.swift zero diff again; webview code untouched
  </acceptance_criteria>
  <done>Auto-stop works without any system end-of-speech service — silence and the hard cap both finalize through the proven path — and the whole mic surface is exercisable on the phone simulator today, whatever the entitlement status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| driver's voice → Speech framework | microphone audio captured in-process; the only trust-critical question is whether it ever leaves the device |
| recognition errors → UI state | raw framework errors cross into user-facing copy; error detail must not leak, only mapped outcomes |
| audio session → WebKit playback | the app's single AVAudioSession is shared with webview media; category changes take effect immediately |
| Speech/ island → CarPlay core | service terminates at a transcript string; no CarPlay references below the wiring layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | driver voice audio sent to Apple servers | critical | mitigate | requiresOnDeviceRecognition = true on every request + construction gate (service never built past supportsOnDeviceRecognition == false, re-asserted at startListening); error paths degrade to hints, never to a server-based retry; grep gate pins the flag where the request is built |
| T-05-02 | Denial of Service | app crash on first permission request (missing purpose string) | high | mitigate | both keys committed in Task 1's single commit; plutil extraction gates the task's automated verify |
| T-05-03 | Denial of Service | stuck listening state (timer never fires) | medium | mitigate | 10.0s hard cap + touch-up path + interruption observer, all fixture-tested; one outcome per press |
| T-05-04 | Tampering | audio-session change destabilizes webview playback | medium | mitigate | .mixWithOthers + .defaultToSpeaker held only while listening; notifyOthersOnDeactivation teardown; interruption .began stops listening (Pitfall 4); real head-unit behavior recorded at the 05-03 device checkpoint (research A2) |
| T-05-05 | Elevation of Privilege | permission prompt originating from the CarPlay scene at drive time | medium | mitigate | visibility gate requires .authorized + granted before the button exists — a .notDetermined state renders no button, so no prompt can originate from the car (phone-first onboarding lands in 05-02) |
| T-05-SC | Tampering | package installs | low | accept | system frameworks only — Speech/AVFAudio are SDK frameworks; no packages introduced (Registry Safety) |
</threat_model>

<verification>
- Task 1: plutil purpose-string gates + funnel-routing tests + construction-gate/empty-transcript tests + wiring and copy grep gates + zero CarPlaySingleton diff
- Task 2: full availability matrix + error-taxonomy tests + hint-copy greps + legacy-constant negative grep
- Task 3: silence-timer tests (1.8/10.0/0.3 constants) + interruption observer + Debug harness build gate
- VALIDATION.md alignment: Task 1 covers rows 05-xx-T1 (service lifecycle) and T4 (purpose strings); Task 2 covers the availability/error-mapping half of T1 and T5's state-mapping basis; Task 3 covers T2 (silence timer) and the entitlement-contingency harness. Test file names match the 05-VALIDATION.md Wave 0 list (SpeechAvailabilityGateTests, SilenceTimerTests) plus SpeechRecognizerServiceTests from the 05-RESEARCH.md map.
</verification>

<success_criteria>
- VOX-01 core delivered: push-to-talk with on-device recognition, gated button visibility, visible listening state, and app-side silence auto-stop — all landing on the Phase 4 funnel unchanged
- VOX-02 purpose-string half delivered: both keys in the first speech commit, plutil-gated
- No server-recognition path exists anywhere (construction gate + grep proof); no new CarPlaySingleton surface; webview untouched
- The mic surface is drivable on the phone simulator via the harness (entitlement contingency)
</success_criteria>

## Artifacts this phase produces (plan 01)

- CarTube/Speech/SpeechRecognizerService.swift — construction-gated push-to-talk service: AVAudioEngine tap → buffer request, silence timer (1.8s/10.0s) with injectable clock, Pitfall-6 error mapping, audio-session coexistence lifecycle (05-02 and 05-03 consume only its transcript output)
- CarTube/Speech/VoiceSearchAvailability.swift — VoiceSearchState enum + pure evaluate matrix: the single source of truth for mic-button visibility AND the 05-02 onboarding screen states
- CarTube/CarPlay/MicButton.swift — the 56pt mic button component: idle/listening/hint states, pulse, pills (embeddable on any surface — the Debug harness reuses it)
- CarPlayViewController mic wiring — visibility gating, overlay interplay, touch events, transcript routed through the existing submitSearchQuery passthrough
- CarTube/Info.plist — both purpose strings (VOX-02 gate artifact)
- CarTubeTests/SpeechRecognizerServiceTests.swift + SpeechAvailabilityGateTests.swift + SilenceTimerTests.swift — funnel routing, gating matrix, error taxonomy, timer state machine
- CarTube/Views/Debug.swift — 'Voice Search Preview' section (simulator-drivable mic surface pending the entitlement)

<output>
Create .planning/phases/05-voice-input/05-01-SUMMARY.md when done
</output>
