---
phase: 05-voice-input
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - CarTube/Views/VoiceSearchSetup.swift
  - CarTube/Views/ContentView.swift
  - CarTubeTests/VoiceOnboardingStateTests.swift
  - CarTube.xcodeproj/project.pbxproj
autonomous: true
requirements: [VOX-02]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On first run the VoiceSearchSetup sheet auto-presents from the phone's ContentView when speech status is .notDetermined — never from the CarPlay scene — and never auto-presents again once determined, either outcome; a persistent 'Voice Search' row (same Section as How to Use / Settings) provides later access (VOX-02; UI-SPEC first-run row; Pitfall 9)"
    - "The 'Enable Voice Search' CTA requests speech authorization first, then mic permission on completion — both callbacks marshaled to main before touching UI — and the state section updates from the results (VOX-02; UI-SPEC phone CTA row; research fact: both purpose strings already shipped in 05-01)"
    - "The denied state (either permission denied, or partial grant) renders 'Voice search is off' + 'To use voice search, CarTube needs microphone and speech recognition access. Allow both in Settings, then return to CarPlay.' + an 'Open Settings' button deep-linking via UIApplication.openSettingsURLString; status re-reads on scenePhase == .active (VOX-02; UI-SPEC denied + partial rows: no half-enabled state exists)"
    - "The authorized-but-unsupported variant renders 'Voice search is limited on this device' + 'Siri and typed search still work. On-device voice input needs a newer iOS version or downloaded language.' — the research-corrected copy variant; the mic button on CarPlay stays hidden, identical to denied (UI-SPEC ready-state row; research fact 1's authorized-but-unsupported handling)"
    - "The ready state renders 'Voice search is ready' + 'Hold the mic button on CarPlay and say what you want to watch, or say \"Hey Siri, search YouTube for …\"' ONLY when supportsOnDeviceRecognition == true; the screen's verdict comes from the same VoiceSearchAvailability.evaluate consumed by the mic button — one source of truth, two consumers (UI-SPEC ready row; ARCHITECTURE.md availability-gating tier)"
    - "Onboarding is completable before any CarPlay use, and after denial the typed keyboard and Siri paths remain fully usable — no dead ends (roadmap SC-4/VOX-01 interplay)"
    - "The explainer copy renders verbatim from the UI-SPEC Copywriting Contract (heading 'Voice Search', on-device privacy line 'Your voice is recognized on your device. Recordings are not sent to Apple or stored.'), wrapping freely in the Form on any phone width (UI-SPEC long-text row)"
  artifacts:
    - CarTube/Views/VoiceSearchSetup.swift — SwiftUI Form (HowTo style): explainer section + state section (CTA / ready / limited / denied + Open Settings) + Done dismiss
    - ContentView wiring — persistent 'Voice Search' row + first-run auto-present gate
    - CarTubeTests/VoiceOnboardingStateTests.swift — status-pair → screen-state mapping tests
  key_links:
    - "ContentView onAppear → speech status .notDetermined → sheet(VoiceSearchSetup()) — first-run gate, phone scene only"
    - "'Enable Voice Search' → SFSpeechRecognizer.requestAuthorization → (authorized ? AVAudioSession.sharedInstance().requestRecordPermission : denied) → VoiceSearchAvailability.evaluate → state section re-render"
    - "'Open Settings' → UIApplication.openSettingsURLString → app Settings pane → return → scenePhase .active → statuses re-read → state re-evaluated"
---

<objective>
Phone-first permission onboarding (VOX-02): the VoiceSearchSetup screen walks the driver through mic + speech authorization before CarPlay use, renders every state from the approved UI-SPEC contract — not-determined explainer with CTA, ready, the research-corrected limited variant, and denied with an Open Settings deep-link — and is reachable on first run automatically plus via a persistent row thereafter.

Purpose: 05-01 shipped the purpose strings and the availability verdict; this plan makes the permission acquisition path real so the mic button's visibility gate can ever become .ready, and closes the no-dead-ends contract on the phone surface.
Output: VoiceSearchSetup.swift + ContentView wiring + state-mapping tests, target-wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-input/05-UI-SPEC.md
@.planning/phases/05-voice-input/05-RESEARCH.md
@.planning/phases/05-voice-input/05-VALIDATION.md
@.planning/phases/05-voice-input/05-01-SUMMARY.md
@CarTube/Views/ContentView.swift
@CarTube/Views/HowTo.swift
@CarTube/Speech/VoiceSearchAvailability.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>VoiceSearchSetup Form — all four states, CTA permission orchestration, Settings deep-link</name>
  <files>CarTube/Views/VoiceSearchSetup.swift, CarTubeTests/VoiceOnboardingStateTests.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>05-01 artifacts exist: CarTube/Speech/VoiceSearchAvailability.swift with VoiceSearchState and evaluate(speechStatus:micStatus:onDeviceSupported:), both purpose strings in CarTube/Info.plist (plutil-verifiable), and the CarTubeTests target.</precondition>
  <read_first>
  - .planning/phases/05-voice-input/05-UI-SPEC.md — IN THIS ORDER: Copywriting Contract rows for the onboarding heading ('Voice Search'), the not-determined explainer (full multi-line string including the Siri mention and the on-device privacy line), ready state, the authorized-but-unsupported limited copy variant, denied state + 'Open Settings'; Typography (20pt semibold heading, 17pt regular body, 12pt footnote); Component Inventory rows 4 (VoiceSearchSetup Form structure: explainer section + state section + Done) ; Interaction Contract rows for phone CTA, denied state, and first-run presentation; Color phone-surface row (system semantic colors; the single #FF3B30 accent CTA 'Enable Voice Search' — NOT system blue)
  - .planning/phases/05-voice-input/05-RESEARCH.md — 'Availability + permission orchestration' code example (speech first, then mic; both callbacks arrive off-main — marshal to main); Pitfall 7 (first-run auto-present fires only from the phone scene's ContentView appear, never CarPlay activation); User Constraints block (onboarding contract)
  - .planning/phases/05-voice-input/05-01-SUMMARY.md — VoiceSearchAvailability's exact enum shape and the limited-verdict semantics the screen mirrors
  - CarTube/Views/HowTo.swift — the Form/Section style being mirrored
  - CarTube/Views/ContentView.swift — the Section where the 'Voice Search' row lands (Task 2) and the scenePhase onChange pattern the denied-state re-read reuses
  </read_first>
  <reversibility rating="reversible">a new standalone screen + tests; wiring in Task 2 is additive to ContentView's List; both revertible in isolation.</reversibility>
  <behavior>
  - Tests (status → screen state mapping, pure): (notDetermined, undetermined) → needsOnboarding renders the explainer + CTA; (authorized, granted, true) → ready renders the ready copy; (authorized, granted, false) → limited renders the limited copy variant; (denied, granted, true) and (authorized, denied, true) → denied renders the denied copy + Open Settings — driving VoiceSearchAvailability.evaluate directly (the mapping is proven against the 05-01 matrix, not a parallel implementation)
  - Test (partial = denied): (authorized, denied, false) → denied — no half-enabled state
  </behavior>
  <action>
  Write the mapping tests RED in CarTubeTests/VoiceOnboardingStateTests.swift, then build CarTube/Views/VoiceSearchSetup.swift as a SwiftUI Form mirroring the HowTo style:

  1. Structure: navigationTitle 'Voice Search' inline (20pt semibold heading weight class); an explainer Section rendering the exact UI-SPEC explainer string verbatim (17pt regular, wrapping freely); a state Section whose content switches on a VoiceSearchState; a 'Done' dismiss Button in the toolbar or a final section per the HowTo idiom. Internal @State for the current verdict, initialized by calling VoiceSearchAvailability.evaluate with live system statuses and the on-device probe from 05-01 — the screen never re-derives the verdict logic (one source of truth).

  2. State renderings, exact copy from the Copywriting Contract: needsOnboarding → the 'Enable Voice Search' CTA button styled with the single accent identity (Color(red: 1, green: 59/255.0, blue: 48/255.0) — #FF3B30, not system blue); ready → 'Voice search is ready' + the ready body including the Siri mention; limited → 'Voice search is limited on this device' + 'Siri and typed search still work. On-device voice input needs a newer iOS version or downloaded language.'; denied → 'Voice search is off' + the full denied body + an 'Open Settings' Button calling the openURL environment with UIApplication.openSettingsURLString. A refresh() helper re-reads all three inputs and re-evaluates.

  3. CTA orchestration per the research example: 'Enable Voice Search' calls SFSpeechRecognizer.requestAuthorization; in its completion, hop to DispatchQueue.main, and only when the result is .authorized call AVAudioSession.sharedInstance().requestRecordPermission, whose completion hops to main and refreshes the verdict (denied speech short-circuits straight to refresh — evaluate yields denied or needsOnboarding from system state). No prompt can ever fire from this screen while it is not on screen, and no prompt fires from CarPlay anywhere (that surface has no such code — 05-01's gate).

  4. On scenePhase == .active (via @Environment in this view), call refresh() so Settings changes made via the deep-link are picked up on return.

  5. Target-wire VoiceSearchSetup.swift + the test file via the throwaway gem script; delete the script. GREEN → commit.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' test -only-testing:CarTubeTests/VoiceOnboardingStateTests 2>&1 | tail -5 | tee /dev/stderr | grep -q 'TEST SUCCEEDED' && grep -q 'Enable Voice Search' CarTube/Views/VoiceSearchSetup.swift && grep -q 'Voice search is ready' CarTube/Views/VoiceSearchSetup.swift && grep -F 'Voice search is limited on this device' CarTube/Views/VoiceSearchSetup.swift && grep -F 'Siri and typed search still work. On-device voice input needs a newer iOS version or downloaded language.' CarTube/Views/VoiceSearchSetup.swift && grep -q 'Voice search is off' CarTube/Views/VoiceSearchSetup.swift && grep -q 'Open Settings' CarTube/Views/VoiceSearchSetup.swift && grep -q 'openSettingsURLString' CarTube/Views/VoiceSearchSetup.swift && grep -q 'recognized on your device' CarTube/Views/VoiceSearchSetup.swift && grep -q 'VoiceSearchAvailability' CarTube/Views/VoiceSearchSetup.swift && grep -q 'requestAuthorization' CarTube/Views/VoiceSearchSetup.swift && grep -q 'requestRecordPermission' CarTube/Views/VoiceSearchSetup.swift && ! grep -q 'exit(0)' CarTube/Views/VoiceSearchSetup.swift && [ "$(grep -c 'VoiceSearchAvailability.evaluate' CarTube/Views/VoiceSearchSetup.swift)" -ge "1" ]</automated>
  </verify>
  <acceptance_criteria>
  - VoiceOnboardingStateTests green: all four verdicts map to their copy variants via VoiceSearchAvailability.evaluate; partial grant = denied
  - Every UI-SPEC copy string present verbatim (grep gates above); CTA uses the #FF3B30 accent identity, not system blue
  - The screen consumes VoiceSearchAvailability.evaluate (no parallel status logic) and requests speech-then-mic with main-hop marshaling on both callbacks
  - Denied state deep-links via UIApplication.openSettingsURLString; verdict refreshes on scenePhase .active
  - project.pbxproj lists VoiceSearchSetup.swift in CarTube and VoiceOnboardingStateTests.swift in CarTubeTests
  </acceptance_criteria>
  <done>The phone carries a complete permission onboarding screen — explainer, CTA, ready, limited, denied with Settings deep-link — driven entirely by the same availability verdict the CarPlay mic button uses.</done>
</task>

<task type="auto">
  <name>ContentView wiring — first-run auto-present gate + persistent 'Voice Search' row</name>
  <files>CarTube/Views/ContentView.swift, CarTube.xcodeproj/project.pbxproj</files>
  <precondition>VoiceSearchSetup.swift exists from Task 1 and builds.</precondition>
  <read_first>
  - .planning/phases/05-voice-input/05-UI-SPEC.md — Component Inventory row 5 (ContentView entry: persistent row same Section as How to Use / Settings, sheet presentation, first-run auto-present when .notDetermined) and the Interaction Contract first-run row (once determined — either outcome — never auto-present again)
  - .planning/phases/05-voice-input/05-RESEARCH.md — Pitfall 7 (auto-present from the PHONE scene's ContentView appear only, never CarPlay scene activation)
  - CarTube/Views/ContentView.swift — the existing Section with the How to Use / Settings / Debug NavigationLinks, the scenePhase onChange block, and the @State conventions
  </read_first>
  <reversibility rating="reversible">two small additions to one view; revertible in isolation.</reversibility>
  <action>
  Add to ContentView: a private @State showVoiceSetup Bool; a 'Voice Search' Button row in the existing Section (between 'How to Use' and 'Settings', matching the List row idiom) setting showVoiceSetup = true and presenting VoiceSearchSetup via .sheet(isPresented:). First-run gate: onAppear, when SFSpeechRecognizer.authorizationStatus() == .notDetermined, set showVoiceSetup = true once — the system status IS the gate (once determined, either outcome, the condition is permanently false; no UserDefaults flag needed, matching the research's don't-hand-roll table). This is the phone scene's ContentView only; the CarPlay scene gains nothing. Do not modify the existing clipboard scenePhase block or the URL-play path — surgical additions only.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && grep -q 'Voice Search' CarTube/Views/ContentView.swift && grep -q 'VoiceSearchSetup' CarTube/Views/ContentView.swift && grep -q 'isPresented' CarTube/Views/ContentView.swift && grep -q 'notDetermined' CarTube/Views/ContentView.swift && grep -q 'onAppear' CarTube/Views/ContentView.swift && ! grep -rn 'requestAuthorization\|requestRecordPermission' CarTube/CarPlay/ --include='*.swift' | grep -q .</automated>
  </verify>
  <acceptance_criteria>
  - App builds; 'Voice Search' row present in the HowTo/Settings Section; sheet presents VoiceSearchSetup
  - First-run auto-present gated on speech status .notDetermined from ContentView onAppear (phone scene); no UserDefaults flag added; never auto-presents once determined
  - Negative grep: no requestAuthorization/requestRecordPermission call anywhere under CarTube/CarPlay/ — prompts can only originate from the phone surface (Pitfall 9 proof)
  - Clipboard/scenePhase/playVideo code paths untouched
  </acceptance_criteria>
  <done>Voice onboarding auto-surfaces on first phone run, stays reachable forever through a persistent row, and no permission prompt can ever originate from the car screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system permission state → onboarding UI | the screen renders from system status APIs only; drift with Settings changes is the failure mode |
| phone CTA → system TCC prompts | the app initiates both permission requests; prompts are system-owned modal UI |
| Settings deep-link → return state | external Settings mutation must be reflected on return |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | medium | onboarding state vs system truth | mitigate | verdicts derived exclusively from VoiceSearchAvailability.evaluate over live system APIs; refresh on scenePhase .active; no cached permission state exists to go stale |
| T-05-07 | Spoofing | low | permission prompt context | mitigate | prompts fire only from the phone screen (ContentView/VoiceSearchSetup); negative grep proves zero requestAuthorization under CarTube/CarPlay/ — a prompt cannot originate at drive time |
| T-05-08 | Denial of Service | low | repeated first-run prompts | mitigate | the .notDetermined system status is the once-only gate; any determination (grant or deny) permanently disables auto-present; the persistent row is user-initiated only |
| T-05-SC | Tampering | low | package installs | accept | no packages — system SwiftUI + Speech/AVFAudio only |
</threat_model>

<verification>
- Task 1 automated: state-mapping tests green + verbatim copy greps + accent identity + evaluate-consumption gate + main-hop marshaling
- Task 2 automated: build + row/sheet/first-run greps + CarPlay-surface negative prompt grep
- VALIDATION.md alignment: covers rows 05-xx-T4 (purpose-string presence already shipped in 05-01; this plan exercises the request paths those keys gate) and 05-xx-T5 (onboarding states: state-mapping unit tests + copy greps)
</verification>

<success_criteria>
- VOX-02 complete: phone-first mic + speech onboarding with both purpose strings active, first-run auto-present, persistent access, denied recovery via Settings, and the limited variant — no dead ends for typed/Siri paths
- The onboarding screen and the CarPlay mic button share one availability verdict function
- Zero prompts possible from the CarPlay surface (grep-proven)
</success_criteria>

## Artifacts this phase produces (plan 02)

- CarTube/Views/VoiceSearchSetup.swift — the complete onboarding Form: explainer, CTA (speech-then-mic orchestration), ready/limited/denied states with exact UI-SPEC copy, Open Settings deep-link, scenePhase refresh
- ContentView wiring — persistent 'Voice Search' row + system-status-based first-run auto-present gate
- CarTubeTests/VoiceOnboardingStateTests.swift — verdict→copy-state mapping matrix (the 05-VALIDATION.md VoiceOnboardingStateTests Wave 0 item)
- The no-CarPlay-prompts boundary proof (negative grep convention later phases must preserve)

<output>
Create .planning/phases/05-voice-input/05-02-SUMMARY.md when done
</output>
