---
phase: 01-external-dependencies-project-setup
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - docs/runbooks/google-youtube-api-key.md
  - .planning/PROJECT.md
  - .planning/STATE.md
  - Secrets.xcconfig
  - .planning/phases/01-external-dependencies-project-setup/NOTES.md
autonomous: false
requirements: [INFRA-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A restricted YouTube Data API key exists in a dedicated Google Cloud project with both the API restriction (YouTube Data API v3) and the iOS application restriction naming the confirmed distribution bundle ID, and its value lives only in the gitignored Secrets.xcconfig
    - The built app Info.plist carries the real key at build time (sentinel replaced), while git-tracked files still contain zero key-pattern strings
    - The Key Decisions table records the audio-category rationale, the xcconfig delivery mechanism, and the dev-key deferral to Phase 3
    - STATE.md shows the branch taken on success criterion 3: entitlement granted (wired per runbook) or still pending (dated blocker retained)
  artifacts:
    - docs/runbooks/google-youtube-api-key.md
    - PROJECT.md Key Decision rows (audio-category rationale incl. residual risk, xcconfig mechanism, dev-key deferral)
    - STATE.md Phase 1 close-out entries
    - Secrets.xcconfig containing the real key (gitignored)
  key_links:
    - real key → root Secrets.xcconfig → $(YOUTUBE_API_KEY) → built Info.plist (pipe proven in 01-01; this plan drops the real value in)
    - confirmed bundle ID (from 01-02 checkpoint) → Google key iOS application restriction → matches future App Store build
---

<objective>
Provision the restricted YouTube Data API key in a dedicated Google Cloud project, drop the real value into the pipe plan 01-01 proved, verify both key restrictions and end-to-end delivery, and close the phase out: Key Decision rows, grant-status branch, and the dated record Phases 2–6 read.

Implements INFRA-01 success criterion 2 in full and criterion 3's branch resolution.

Purpose: Google key provisioning is the second external clock and the only remaining gate on Phase 3 search work.
Output: restricted key delivered at build time, three Key Decision rows, closed-out phase state.
</objective>

<execution_context>
@/Users/ddphuong/.codex/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md
@docs/runbooks/apple-carplay-entitlement-request.md
@docs/runbooks/carplay-entitlement-grant-wiring.md
</context>

<tasks>

<task type="auto">
  <name>Author Google key runbook referencing the confirmed bundle ID</name>
  <files>docs/runbooks/google-youtube-api-key.md</files>
  <read_first>
  - .planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md — Pitfall 4 (key still ships; restrictions are abuse-surface reduction), Pitfall 5 (enable API before restricting), Pitfall 6 (one application-restriction type), Assumption A1 (service name youtube.googleapis.com), Open Question 3 (dev-key deferral), and the quota quote in State of the Art
  - .planning/STATE.md — the confirmed bundle ID string recorded by plan 01-02 Task 3
  - Config/Secrets.xcconfig.example (the delivery path this runbook feeds)
  </read_first>
  <reversibility rating="reversible">documentation only.</reversibility>
  <action>
  Write docs/runbooks/google-youtube-api-key.md as the exact console procedure, in this order: create a NEW dedicated Google Cloud project (e.g. named cartube-shipping) → APIs & Services → Library → enable YouTube Data API v3 (verify status ON on the Enabled APIs page) → Credentials → Create credentials → API key (the Console mandates at least one API restriction at creation) → open the key → API restrictions: select YouTube Data API v3 only → Application restrictions: iOS apps, and list the confirmed distribution bundle ID exactly as recorded in STATE.md plus the share-extension ID formed by appending .PlayOnCarTube to it → note that exactly one application-restriction type is allowed (iOS apps only, no website restriction) → save.

  Then the quota-guard section: enable billing-free quota alerting on the project — a usage alert near the 100 search.list calls/day bucket so a leak is noticed (a leaked key burns the whole fleet's quota; every request even invalid costs at least 1 unit). Then the delivery section: put the created key into root Secrets.xcconfig as the YOUTUBE_API_KEY value, replacing the sentinel — never anywhere else, and never in the tracked example.

  Then the verification section with the exact commands: the gcloud check (gcloud services api-keys list with the appropriate project flag, or equivalent REST call) confirming the key's API target is youtube.googleapis.com (per assumption A1) and its bundle-ID restriction; the xcodebuild + plutil extraction proving the built Info.plist carries the real key; the git grep hygiene check that still must return zero.

  Then the honesty section, stated plainly: the key ships inside the IPA's Info.plist and is readable with plutil — client-embedded keys are public-by-design; the restrictions reduce the abuse surface but Google documents that iOS bundle-ID restriction bypass is straightforward; rotation via a new key value in Secrets.xcconfig (or the CI command-line override) is the remediation path if quota alerts fire.
  </action>
  <verify>
    <automated>grep -q 'YouTube Data API v3' docs/runbooks/google-youtube-api-key.md && grep -q '.PlayOnCarTube' docs/runbooks/google-youtube-api-key.md && grep -q 'gcloud services api-keys' docs/runbooks/google-youtube-api-key.md && grep -qi 'Secrets.xcconfig' docs/runbooks/google-youtube-api-key.md && ! grep -nE 'AIza[0-9A-Za-z_-]{30,}' docs/runbooks/google-youtube-api-key.md</automated>
  </verify>
  <acceptance_criteria>
  - docs/runbooks/google-youtube-api-key.md exists and contains the literals YouTube Data API v3, .PlayOnCarTube, gcloud services api-keys, Secrets.xcconfig, and references the bundle ID from STATE.md (a bundle-id placeholder is acceptable only if 01-02 recorded a BLOCKER — in that case the plan halts at the checkpoint instead)
  - The file contains zero AIza-pattern strings
  - The enable-before-restrict order and the one-restriction-type rule are each stated in the document (verifiable by grepping the concepts API restrictions and iOS)
  </acceptance_criteria>
  <done>A complete click-path runbook exists: dedicated project, enable, create, both restrictions naming the confirmed bundle IDs, quota alerting, delivery into Secrets.xcconfig, verification commands, and the public-by-design honesty note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Create the restricted key and inject it into Secrets.xcconfig</name>
  <what-built>Agent authored the full Google Cloud console runbook; the human performs the login-walled console actions and hands the created key value to the agent for build-time verification. The agent has gcloud CLI 579.0.0 available and will verify restrictions against the project once the key exists.</what-built>
  <read_first>
  - docs/runbooks/google-youtube-api-key.md (the script to follow, in order)
  - Secrets.xcconfig (the single file the key may land in)
  </read_first>
  <action>
  Human follows the runbook top to bottom: dedicated project → enable API → create key → set both restrictions naming the confirmed bundle IDs → enable quota alerting → paste the key value into root Secrets.xcconfig replacing BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY. Then report the Google Cloud project ID so the agent can run the gcloud verification. If the user prefers not to paste the key into chat: they edit Secrets.xcconfig themselves and simply confirm completion plus the project ID — the key value never needs to transit chat.
  </action>
  <how-to-verify>
  1. Follow docs/runbooks/google-youtube-api-key.md exactly — dedicated project, API enabled, key created, BOTH restrictions set.
  2. Put the key into the root Secrets.xcconfig (replacing the sentinel). Do not paste it anywhere else.
  3. Report the project ID and whether restrictions were both set (screenshots optional).
  4. The agent then runs: plutil extraction on a fresh simulator build (must print the real key, which starts with AIza), the gcloud restrictions check against the reported project, and the git grep hygiene gate (must stay empty).
  </how-to-verify>
  <resume-signal>Type "key created" plus the Google Cloud project ID, or describe what blocked you</resume-signal>
  <acceptance_criteria>
  - A fresh xcodebuild simulator build's Info.plist yields a YOUTUBE_API_KEY value matching pattern ^AIza[0-9A-Za-z_-]{30,}$ via plutil
  - gcloud (or the user's restriction screenshots if gcloud auth is unavailable) confirms the key has both the YouTube Data API v3 API restriction and the iOS bundle-ID application restriction
  - git grep -nE 'AIza[0-9A-Za-z_-]{30,}' returns zero matches across tracked files
  - git check-ignore -q Secrets.xcconfig still succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Record Key Decisions, resolve the grant-status branch, close out Phase 1 state</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
  - .planning/PROJECT.md — the Key Decisions table (append rows; do not alter existing rows)
  - .planning/STATE.md — the dated entitlement blocker from 01-02 and the Session Continuity section
  - docs/runbooks/carplay-entitlement-grant-wiring.md — the procedure referenced by the granted branch
  - .planning/phases/01-external-dependencies-project-setup/NOTES.md — dev-key deferral rationale to be made a Key Decision
  </read_first>
  <action>
  1. Before appending anything, capture the baseline table-row count: BEFORE=$(grep -cE '^\| ' .planning/PROJECT.md) (at execution time this equals the committed HEAD state — verify it with git show HEAD:.planning/PROJECT.md | grep -cE '^\| ' and halt if they differ). Then append three rows to the PROJECT.md Key Decisions table, each with Outcome Pending: (a) CarPlay entitlement requested under the audio category — rationale: the app genuinely plays YouTube audio through the vehicle's system; the video category requires AirPlay video and parked-only use which contradict the milestone's core value; residual risk: an on-screen video surface under an audio entitlement may still draw review scrutiny; (b) API key delivered via gitignored root Secrets.xcconfig with $(YOUTUBE_API_KEY) Info.plist injection — rationale: Apple's documented substitution, zero moving parts, CI rotates via xcodebuild command-line override; known limit: the key ships in the IPA and is public-by-design, guarded by both restrictions and quota alerting; (c) Google dev-key project deferred to Phase 3 — rationale: researcher recommendation; Phase 1 provisions only the shipping key so search work never blocks.
  2. Check whether Apple has already granted the entitlement (expected answer: no). If granted: follow docs/runbooks/carplay-entitlement-grant-wiring.md including its entitlements-file sequencing constraint, and record the wired state in STATE.md. If still pending: keep the dated blocker and add a Phase 1 close-out line recording that the branch resolved as pending, that Phases 3–5 proceed on phone-side mocks, and that on-device CarPlay verification happens when the grant lands.
  3. Update STATE.md: Current Position (Phase 1 complete pending external clocks, next Phase 2), Session Continuity, and Blockers/Concerns so the entry states submission date, still-pending status (or grant date and wiring result), and the runbook pointer. Delete nothing else.
  4. Delete the file .planning/phases/01-external-dependencies-project-setup/NOTES.md (its two defaults — bundle-ID re-prefix and dev-key deferral — are now consumed decisions: the bundle ID is recorded in STATE.md, the deferral is a Key Decision row).
  </action>
  <verify>
    <automated>grep -q 'audio category' .planning/PROJECT.md && grep -q 'Secrets.xcconfig' .planning/PROJECT.md && grep -q 'Phase 3' .planning/PROJECT.md && BEFORE=$(git show HEAD:.planning/PROJECT.md | grep -cE '^\| ') && AFTER=$(grep -cE '^\| ' .planning/PROJECT.md) && [ $((AFTER-BEFORE)) -eq 3 ] && grep -qE '^\| [^|]*audio category' .planning/PROJECT.md && grep -qE '^\| [^|]*Secrets\.xcconfig' .planning/PROJECT.md && grep -qE '^\| [^|]*Phase 3' .planning/PROJECT.md && grep -q 'pending\|granted' .planning/STATE.md && ! test -f .planning/phases/01-external-dependencies-project-setup/NOTES.md && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}'</automated>
  </verify>
  <acceptance_criteria>
  - PROJECT.md Key Decisions table gains exactly three new rows (asserted as AFTER−BEFORE row-count delta of 3 against git HEAD baseline) containing the literals audio category, Secrets.xcconfig, and Phase 3; no pre-existing row's text is modified
  - STATE.md contains the word pending or granted describing the entitlement status alongside the submission date
  - NOTES.md no longer exists at .planning/phases/01-external-dependencies-project-setup/NOTES.md
  - git grep -nE 'AIza[0-9A-Za-z_-]{30,}' returns zero matches (final phase-gate hygiene check)
  </acceptance_criteria>
  <done>All three Key Decisions durably recorded, the success-criterion-3 branch explicitly resolved in state, working notes consumed, and the repo still carries no key material.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Cloud Console ↔ repo | key creation is human-only behind Google auth; repo owns delivery + verification |
| Secrets.xcconfig → IPA | intentional crossing; key is public-by-design and treated as such |
| key → Google quota bucket | 100 search.list calls/day shared across all installs; a leak starves every user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Information Disclosure | real key committed to public fork during injection step | high | mitigate | key lands only in gitignored root Secrets.xcconfig; git grep AIza gate re-run in both Task 2 and Task 3 acceptance criteria; example file negative-gated at authoring time |
| T-01-06 | DoS (quota abuse) | extracted key burns fleet-wide 100/day bucket | high | mitigate | both restrictions (API: YouTube Data API v3 only; application: iOS bundle IDs matching the confirmed distribution ID + .PlayOnCarTube) + quota alerting + documented rotation path; accept residual bypassability per Google's own guidance |
| T-01-SC | Tampering | package installs | low | accept | no package installs or registry fetches in this plan |
</threat_model>

<verification>
- Task 1 runbook greps green and contains no key material
- Task 2 checkpoint: plutil extraction prints the real AIza key; gcloud/screenshots confirm both restrictions; hygiene greps stay empty
- Task 3: three Key Decision rows, branch resolved in STATE.md, NOTES.md removed, final AIza gate clean
</verification>

<success_criteria>
- Restricted key exists in a dedicated project, both restrictions naming the confirmed bundle IDs, delivered at build time into the IPA
- Zero key-pattern strings in tracked files across the entire phase
- Phase 1 state closes with both external clocks started and the entitlement branch explicitly recorded
</success_criteria>

## Artifacts this phase produces (plan 03)

- File docs/runbooks/google-youtube-api-key.md — dedicated-project creation, enable-before-restrict order, both restriction types, quota alerting, delivery into root Secrets.xcconfig, verification commands (gcloud + plutil + git grep), public-by-design honesty note
- .planning/PROJECT.md — three appended Key Decision rows: audio-category rationale incl. residual risk; xcconfig delivery mechanism incl. CI override and IPA-visibility limit; dev-key project deferral to Phase 3
- .planning/STATE.md — Phase 1 close-out (grant-status branch resolved, submission date retained, runbook pointer)
- .planning/phases/01-external-dependencies-project-setup/NOTES.md — deleted (defaults consumed)
- Secrets.xcconfig — real key value replacing BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY (local file, never committed)
- Deletion of sentinel literal BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY from Secrets.xcconfig

<output>
Create .planning/phases/01-external-dependencies-project-setup/01-03-SUMMARY.md when done
</output>
