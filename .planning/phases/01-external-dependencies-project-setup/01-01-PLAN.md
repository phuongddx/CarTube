---
phase: 01-external-dependencies-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Config/Secrets.xcconfig.example
  - Secrets.xcconfig
  - .gitignore
  - CarTube/Info.plist
  - CarTube.xcodeproj/project.pbxproj
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A simulator build of scheme CarTube bakes the value from root Secrets.xcconfig into the built app Info.plist under key YOUTUBE_API_KEY
    - Secrets.xcconfig is gitignored; no AIza-prefixed key material exists in any git-tracked file
    - A fresh clone that skips the documented copy step fails to build with a base-configuration error, proving the copy step is mandatory
  artifacts:
    - Config/Secrets.xcconfig.example (committed placeholder template)
    - Secrets.xcconfig at repo root (local, gitignored, sentinel value)
    - .gitignore entry for Secrets.xcconfig
    - CarTube/Info.plist key YOUTUBE_API_KEY with value $(YOUTUBE_API_KEY)
    - CarTube.xcodeproj/project.pbxproj file reference + base_configuration_reference on project Debug and Release configurations
  key_links:
    - root Secrets.xcconfig → pbxproj project-level base configuration (Debug + Release) → $(YOUTUBE_API_KEY) substitution in CarTube/Info.plist → BUILT_PRODUCTS_DIR/CarTube.app/Info.plist read via plutil
---

<objective>
Wire the one repo-side path this phase owns, end-to-end: a build-time YouTube API key defined in a gitignored xcconfig reaches the built app's Info.plist via Xcode's documented $(YOUTUBE_API_KEY) substitution, with repo-hygiene gates proving no key material can enter git.

Implements the build-time delivery half of INFRA-01 success criterion 2 ("never committed to the repo, with a build-time delivery mechanism"), using the researcher-verified Pattern 1 (xcconfig → Info.plist injection). The Google key itself is provisioned in plan 01-03; this plan proves the delivery pipe with a sentinel value.

Purpose: Phase 3 search work must never block on key delivery, and the public fork (origin phuongddx/CarTube, upstream Avangelista/CarTube) must never receive key material — git history is forever.
Output: committed Config/Secrets.xcconfig.example, gitignored root Secrets.xcconfig, pbxproj xcconfig attachment, Info.plist injection entry, hygiene gates.
</objective>

<execution_context>
@/Users/ddphuong/.codex/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md
@.gitignore
@CarTube/Info.plist
</context>

<tasks>

<task type="tracer">
  <name>End-to-end build-time secret injection — xcconfig to built Info.plist</name>
  <files>Config/Secrets.xcconfig.example, Secrets.xcconfig, .gitignore, CarTube/Info.plist, CarTube.xcodeproj/project.pbxproj</files>
  <read_first>
  - .planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md — sections "Pattern 1: xcconfig secret injection" and "Code Examples" (setting format, $(Name) substitution, Configurations attachment, layering, deselect-targets guidance, CI variant)
  - .gitignore (full file — confirm no secrets entry exists yet)
  - CarTube/Info.plist (hand-authored plist already substitutes $(PRODUCT_MODULE_NAME) at line 28 — same mechanism)
  - CarTube.xcodeproj/project.pbxproj (objectVersion/build-configuration layout; note existing CODE_SIGN_ENTITLEMENTS and PRODUCT_BUNDLE_IDENTIFIER lines for orientation only — do not touch them)
  </read_first>
  <reversibility rating="reversible">pbxproj xcconfig attachment and Info.plist key are pure additive config; reverting is a file-reference removal.</reversibility>
  <action>
  Wire the single path, nothing else:
  1. Create Config/Secrets.xcconfig.example (committed) containing one setting line, YOUTUBE_API_KEY = REPLACE_ME, preceded by comment lines stating: copy this file to ../Secrets.xcconfig before building (the build fails without it — verified in Task 2); the real file is gitignored and must never be committed; CI recipe — copy the example to Secrets.xcconfig, then pass xcodebuild a YOUTUBE_API_KEY=... command-line override so the real key never touches disk in CI.
  2. Create root Secrets.xcconfig (NOT committed) with the single line YOUTUBE_API_KEY = BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY — a deliberately non-AIza sentinel so this task's build check cannot trip the AIza hygiene grep; the real key replaces it in plan 01-03.
  3. Append a .gitignore section with heading comment "Build-time secrets — never commit (public fork)" and entry line Secrets.xcconfig.
  4. Add to CarTube/Info.plist a top-level key YOUTUBE_API_KEY of type string with value $(YOUTUBE_API_KEY) — same substitution the file already uses for $(PRODUCT_MODULE_NAME).
  5. Attach the xcconfig via a throwaway ruby script using the installed xcodeproj gem (1.28.1): Xcodeproj::Project.open on CarTube.xcodeproj, create a file reference for Secrets.xcconfig in the main group, and for each configuration in the project-level build_configuration_list whose name is Debug or Release set its base_configuration_reference to that file reference; save the project. Do NOT add the file reference to any target's build phase (it is configuration input, not a resource). Do NOT set base_configuration_reference at target level — project level layers first and target-level settings would win, and no target defines YOUTUBE_API_KEY so layering is safe. Delete the throwaway script after running it (it is not committed).
  Do not touch entitlements, signing settings, bundle identifiers, or any Swift file — those belong to later phases.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' -showBuildSettings 2>/dev/null | grep -m1 '[[:space:]]YOUTUBE_API_KEY = ' && BP=$(xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' -showBuildSettings 2>/dev/null | grep -m1 'BUILT_PRODUCTS_DIR = ' | awk '{ print $3 }') && plutil -extract YOUTUBE_API_KEY raw "$BP/CarTube.app/Info.plist" && git check-ignore -q Secrets.xcconfig && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}'</automated>
  </verify>
  <acceptance_criteria>
  - The xcodebuild command exits 0 and its final lines contain BUILD SUCCEEDED
  - -showBuildSettings prints a YOUTUBE_API_KEY line whose value is BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY
  - plutil -extract YOUTUBE_API_KEY raw on the built CarTube.app/Info.plist prints BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY exactly (proves substitution reached the product)
  - git check-ignore -q Secrets.xcconfig exits 0
  - git grep -nE 'AIza[0-9A-Za-z_-]{30,}' exits 1 (zero matches across tracked files)
  - CarTube/Info.plist contains the literal pair YOUTUBE_API_KEY / $(YOUTUBE_API_KEY)
  - git status does not list Secrets.xcconfig as untracked (it is ignored)
  </acceptance_criteria>
  <done>The xcconfig value flows end-to-end into the built Info.plist, Secrets.xcconfig is ignored, and no key-pattern string exists in tracked files.</done>
</task>

<task type="auto">
  <name>Verify fresh-clone behavior and lock the copy-step requirement into the template</name>
  <files>Config/Secrets.xcconfig.example</files>
  <read_first>
  - Config/Secrets.xcconfig.example (as created in Task 1)
  - .planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md — "Known limitation" paragraph of Pattern 1 and the CI variant
  </read_first>
  <action>
  Prove the documented trade-off rather than assume it, then record it:
  1. Move the root Secrets.xcconfig aside (mv to a temp path outside the repo), run the same simulator build, and capture the failure — with the base-configuration reference pointing at a missing file the build must fail before compiling. Note the exact error wording for the SUMMARY.
  2. Move the file back and rebuild — must succeed.
  3. Update the Config/Secrets.xcconfig.example header comment so it states the observed behavior verbatim-in-meaning: a checkout without a root Secrets.xcconfig fails the build until the example is copied there, and this is intentional (the attached file is the gitignored one so no real key can ship in git). Keep the CI recipe from Task 1 unchanged.
  </action>
  <verify>
    <automated>mv Secrets.xcconfig /tmp/Secrets.xcconfig.phase01 && OUT=$(xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3) && ! grep -q 'BUILD SUCCEEDED' <<<"$OUT" && mv /tmp/Secrets.xcconfig.phase01 Secrets.xcconfig && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -1 | grep -q 'BUILD SUCCEEDED' && grep -q 'Secrets.xcconfig' Config/Secrets.xcconfig.example && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}'</automated>
  </verify>
  <acceptance_criteria>
  - With Secrets.xcconfig absent, the xcodebuild invocation does NOT print BUILD SUCCEEDED (non-zero build result; error text recorded in the plan SUMMARY)
  - After restoring the file, the same invocation prints BUILD SUCCEEDED
  - Config/Secrets.xcconfig.example contains the word Secrets.xcconfig and instructions to copy it to the repo root
  - git grep -nE 'AIza[0-9A-Za-z_-]{30,}' still exits 1 after all edits
  </acceptance_criteria>
  <done>Missing-file build failure is demonstrated, the template documents the mandatory copy step and CI override recipe, and hygiene gates remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local secrets file → git index | the only place key material could leak into the public fork |
| xcconfig → build product | value intentionally crosses into the IPA; documented as public-by-design |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Secrets.xcconfig committed to public fork | high | mitigate | .gitignore entry + committed example template only + git grep AIza gate in both tasks' acceptance criteria |
| T-01-02 | Information Disclosure | key baked into IPA Info.plist | medium | accept | inherent to any client-side key; documented in example header; paired in 01-03 with API + bundle-ID restrictions and quota alerting |
| T-01-SC | Tampering | package installs | low | accept | no package installs this phase — ruby xcodeproj gem 1.28.1 already installed locally, no registry fetch |
</threat_model>

<verification>
- Both task automated commands green; build/plutil/check-ignore/grep assertions pass
- No Swift source modified; entitlements and signing settings untouched (git diff scope check)
</verification>

<success_criteria>
- $(YOUTUBE_API_KEY) substitution proven into the built product with the sentinel value
- Repo hygiene gates in place and passing on a public-fork repo
- Fresh-clone requirement documented from observed behavior, not assumption
</success_criteria>

## Artifacts this phase produces (plan 01)

- File Config/Secrets.xcconfig.example (committed) — build setting YOUTUBE_API_KEY = REPLACE_ME, copy/CI instructions in header comments
- File Secrets.xcconfig at repo root (local, gitignored) — setting YOUTUBE_API_KEY
- .gitignore section "Build-time secrets — never commit (public fork)" with entry Secrets.xcconfig
- CarTube/Info.plist — top-level key YOUTUBE_API_KEY, string value $(YOUTUBE_API_KEY)
- CarTube.xcodeproj/project.pbxproj — file reference Secrets.xcconfig + base_configuration_reference on project-level Debug and Release build configurations
- Build setting name YOUTUBE_API_KEY (consumed by Phase 3 via Bundle.main.object(forInfoDictionaryKey:))

<output>
Create .planning/phases/01-external-dependencies-project-setup/01-01-SUMMARY.md when done
</output>
