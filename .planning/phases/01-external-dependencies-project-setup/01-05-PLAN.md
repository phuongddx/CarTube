---
phase: 01-external-dependencies-project-setup
plan: 05
type: execute
wave: 2
depends_on: ["01-04"]
files_modified:
  - CarTube.xcodeproj/project.pbxproj
  - CarTube/CarTube.entitlements
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements: [INFRA-01]

user_setup:
  - service: apple-developer-portal
    why: "Apple granted the CarPlay Audio App entitlement 2026-08-18 (Case-ID 21672656) — only the entitlements-file edit (runbook step 8) is done; the remaining Apple-portal and Xcode-signing steps require a human with Apple Developer Program access and local Xcode UI"
    env_vars: []
    dashboard_config:
      - task: "Follow docs/runbooks/carplay-entitlement-grant-wiring.md steps 1-7 (enable CarPlay on App ID com.cartube.carplay at developer.apple.com/account, create + import a new CarPlay-capable provisioning profile) and 9-11 (Xcode: turn off Automatically manage signing for the CarTube target, select the imported profile, verify the CarPlay scene connects via the CarPlay Simulator)"
        location: "developer.apple.com/account and local Xcode Signing & Capabilities"

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The remaining CarPlay entitlement grant-wiring steps are complete: the App ID com.cartube.carplay has the CarPlay entitlement enabled in the Apple portal, a new CarPlay-capable provisioning profile is imported and selected in Xcode with automatic signing turned off for the CarTube target, and the human has observed whether the CarPlay scene connects in the CarPlay Simulator — closing the outstanding steps of 01-VERIFICATION.md's partial gap on 'the new provisioning profile is wired into the project and the CarPlay scene connection is verified'"
    - "The signing-configuration change did not corrupt the project: CODE_SIGN_ENTITLEMENTS still points at CarTube/CarTube.entitlements, the CarTube target's bundle ID is still com.cartube.carplay, the entitlements file still carries com.apple.developer.carplay-audio = true, and a simulator build still succeeds"
  artifacts:
    - CarTube.xcodeproj/project.pbxproj (CarTube target Debug+Release: CODE_SIGN_STYLE = Manual, non-empty PROVISIONING_PROFILE_SPECIFIER, CODE_SIGN_ENTITLEMENTS unchanged)
    - CarTube/CarTube.entitlements (com.apple.developer.carplay-audio = true, unchanged)
    - .planning/STATE.md (CarPlay-wiring blocker bullet rewritten as resolved, dated, with the human-reported scene-connection observation)
  key_links:
    - "Apple portal App ID com.cartube.carplay CarPlay capability -> new provisioning profile -> imported into Xcode -> CarTube target Signing & Capabilities (Automatic off, profile selected) -> CODE_SIGN_ENTITLEMENTS=CarTube/CarTube.entitlements (unchanged) -> CarPlay scene connects in the CarPlay Simulator (human-observed, no CLI proxy)"
---

<objective>
Close the second of two open Phase 1 gaps recorded in 01-VERIFICATION.md: Apple granted the CarPlay Audio App entitlement on 2026-08-18 (Case-ID 21672656), but only runbook step 8 (adding `com.apple.developer.carplay-audio = true` to `CarTube/CarTube.entitlements`) has been executed. Steps 1-7 (Apple-portal App ID capability + provisioning profile creation/import) and 9-11 (Xcode signing-style toggle + CarPlay Simulator scene verification) remain — all Apple-portal-login, Xcode-UI, and simulator-observed actions this agent cannot perform.

This plan gates those remaining steps behind a `checkpoint:human-action`, then runs the concrete, agent-executable verification that IS possible afterward: confirming the project's signing settings actually switched to manual signing against an imported profile, and that nothing else in the signing/entitlements configuration regressed. It deliberately does NOT invent an automated check for the CarPlay Simulator scene connection itself — that observation has no CLI proxy and is recorded from the human's report.

Purpose: closes INFRA-01 success criterion 3's remaining branch — "the new provisioning profile is wired into the project and the CarPlay scene connection is verified."
Output: manual signing against the imported CarPlay-capable profile confirmed at the project-settings level, entitlements/bundle-ID regression-checked, STATE.md reflecting the true resolved (or partially-resolved) state.

Note on sequencing: this plan is independent of plan 01-04 (Google key) — there is no logical dependency between the two external clocks. It is assigned wave 2 / `depends_on: ["01-04"]` purely because both plans' close-out tasks touch `.planning/STATE.md`, and the file-overlap rule requires sequential waves for same-wave file safety. The human can perform the Apple-portal/Xcode steps whenever convenient regardless of plan 01-04's status.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-external-dependencies-project-setup/01-VERIFICATION.md
@docs/runbooks/carplay-entitlement-grant-wiring.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Complete the remaining CarPlay entitlement grant-wiring steps in the Apple Developer Portal and Xcode</name>
  <read_first>
  - docs/runbooks/carplay-entitlement-grant-wiring.md (the exact 11-step sequence; step 8 is already done and committed)
  - CarTube/CarTube.entitlements (currently carries only `com.apple.developer.carplay-audio = true` — step 8's result)
  </read_first>
  <action>
  Step 8 is already done: `CarTube/CarTube.entitlements` carries `com.apple.developer.carplay-audio = true`, committed. The remaining steps require a human with Apple Developer Program access and local Xcode: (1-5) log in to developer.apple.com/account -> Certificates, IDs & Profiles -> Identifiers -> select App ID `com.cartube.carplay` -> enable the CarPlay entitlement -> Save; (6-7) Provisioning Profiles -> create a new provisioning profile for `com.cartube.carplay` that supports CarPlay -> import it into Xcode; (9-10) in Xcode, select the CarTube target -> Signing & Capabilities -> turn OFF "Automatically manage signing" -> select the newly imported profile (confirm Build Settings -> Code Signing Entitlements still points at `CarTube/CarTube.entitlements` — it already does); (11) verify the CarPlay scene connects using the CarPlay Simulator from Additional Tools for Xcode (download from developer.apple.com/download/all if not already installed).
  </action>
  <instructions>
  1. developer.apple.com/account -> Certificates, IDs & Profiles -> Identifiers -> `com.cartube.carplay` -> enable CarPlay entitlement -> Save.
  2. Provisioning Profiles -> create a new profile for `com.cartube.carplay` that supports CarPlay -> download -> double-click (or drag into Xcode) to import.
  3. Xcode -> CarTube target -> Signing & Capabilities -> uncheck "Automatically manage signing" -> select the imported profile from the Provisioning Profile dropdown.
  4. Confirm Build Settings -> Code Signing Entitlements still reads `CarTube/CarTube.entitlements` (it should not have changed).
  5. Open the CarPlay Simulator (Xcode -> Open Developer Tool -> Simulator, or the standalone CarPlay Simulator from Additional Tools for Xcode) and launch CarTube — observe whether the CarPlay scene connects.
  6. Report back: whether steps 1-10 completed without error, and what you observed in step 5 (scene connected / did not connect / no CarPlay Simulator available to test).
  </instructions>
  <verification>
  Task 2 in this plan re-checks the project's signing settings via `xcodebuild -showBuildSettings` (CODE_SIGN_STYLE should read Manual with a non-empty PROVISIONING_PROFILE_SPECIFIER for the CarTube target) and re-runs a simulator build regression. The CarPlay Simulator scene-connection observation itself has no automated proxy — it is recorded exactly as you report it, including if you report you could not test it.
  </verification>
  <resume-signal>Type "wired" plus your CarPlay Simulator observation (scene connected / did not connect / could not test — describe why), or describe what blocked you</resume-signal>
  <acceptance_criteria>
  - The App ID `com.cartube.carplay` has the CarPlay entitlement enabled in the Apple portal
  - A new CarPlay-capable provisioning profile is imported into Xcode and selected for the CarTube target with automatic signing OFF
  - Code Signing Entitlements still points at `CarTube/CarTube.entitlements`
  - The CarPlay Simulator scene-connection outcome (or the reason it couldn't be tested) is explicitly reported, not silently skipped
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify signing configuration switched to manual with the imported profile; entitlements and bundle ID unchanged</name>
  <files>CarTube.xcodeproj/project.pbxproj, CarTube/CarTube.entitlements</files>
  <precondition>Task 1's human checkpoint reported "wired" (not a blocker description) — if the human reported being blocked (e.g. no paid Apple Developer account, no CarPlay Simulator available), halt this task and record the blocker instead of fabricating a passing result.</precondition>
  <read_first>
  - docs/runbooks/carplay-entitlement-grant-wiring.md steps 9-11
  - CarTube/CarTube.entitlements — must still carry only `com.apple.developer.carplay-audio = true`
  </read_first>
  <action>
  Run `xcodebuild -showBuildSettings -project CarTube.xcodeproj -target CarTube -configuration Debug` and the same for `-configuration Release`. For both configurations, assert: `CODE_SIGN_STYLE` reads `Manual` (not `Automatic`), `PROVISIONING_PROFILE_SPECIFIER` is present and non-empty (the human's imported profile name — do not hardcode an expected value since that name is chosen when the profile is created), and `CODE_SIGN_ENTITLEMENTS` still reads `CarTube/CarTube.entitlements`, and `PRODUCT_BUNDLE_IDENTIFIER` still reads `com.cartube.carplay` (guards against the profile having been created for the wrong bundle ID or team). Then read `CarTube/CarTube.entitlements` directly and assert it still contains the exact key/value pair `com.apple.developer.carplay-audio` / `true` and nothing else was silently added or removed. Finally, re-run a plain simulator build (`xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build`) as a regression check that the manual-signing change for device builds did not break simulator builds, which don't require the real profile. Do NOT attempt to build or sign for a physical device or `generic/platform=iOS` in this task, and do NOT attempt to verify the CarPlay Simulator scene connection itself — that remains the human-reported observation from Task 1, carried into the SUMMARY verbatim.
  </action>
  <verify>
    <automated>DEBUG_SETTINGS="$(xcodebuild -showBuildSettings -project CarTube.xcodeproj -target CarTube -configuration Debug 2>/dev/null)" && RELEASE_SETTINGS="$(xcodebuild -showBuildSettings -project CarTube.xcodeproj -target CarTube -configuration Release 2>/dev/null)" && echo "$DEBUG_SETTINGS" | grep -qE 'CODE_SIGN_STYLE = Manual' && echo "$RELEASE_SETTINGS" | grep -qE 'CODE_SIGN_STYLE = Manual' && echo "$DEBUG_SETTINGS" | grep -qE 'PROVISIONING_PROFILE_SPECIFIER = [^ ]+' && echo "$RELEASE_SETTINGS" | grep -qE 'PROVISIONING_PROFILE_SPECIFIER = [^ ]+' && echo "$DEBUG_SETTINGS" | grep -q 'CODE_SIGN_ENTITLEMENTS = CarTube/CarTube.entitlements' && echo "$DEBUG_SETTINGS" | grep -q 'PRODUCT_BUNDLE_IDENTIFIER = com.cartube.carplay' && grep -q 'com.apple.developer.carplay-audio' CarTube/CarTube.entitlements && grep -A1 'com.apple.developer.carplay-audio' CarTube/CarTube.entitlements | grep -q '<true/>' && xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED'</automated>
  </verify>
  <acceptance_criteria>
  - Both Debug and Release CarTube-target build settings show `CODE_SIGN_STYLE = Manual` and a non-empty `PROVISIONING_PROFILE_SPECIFIER`
  - `CODE_SIGN_ENTITLEMENTS` still resolves to `CarTube/CarTube.entitlements` and `PRODUCT_BUNDLE_IDENTIFIER` still resolves to `com.cartube.carplay`
  - `CarTube/CarTube.entitlements` still contains exactly `com.apple.developer.carplay-audio = true`
  - A plain simulator build still succeeds (manual signing for device did not break simulator builds)
  - No automated check was invented for the CarPlay Simulator scene connection — that observation is carried from Task 1's human report only
  </acceptance_criteria>
  <done>The project-settings half of the grant-wiring runbook (steps 9-10) is proven at the build-settings level, the entitlement and bundle ID are unchanged, and the human's scene-connection observation (step 11) is on record — nothing about the on-device behavior is fabricated.</done>
</task>

<task type="auto">
  <name>Close out the CarPlay-wiring blocker in STATE.md</name>
  <files>.planning/STATE.md</files>
  <precondition>Task 2's automated verify passed (manual signing confirmed, entitlements/bundle ID unchanged, simulator build green) — if Task 2 halted, record the blocker as still-open with the specific reason instead of marking it resolved.</precondition>
  <reversibility rating="reversible">documentation only — STATE.md text update.</reversibility>
  <read_first>
  - .planning/STATE.md — the exact bullet beginning "**CarPlay entitlement GRANTED (2026-08-18)**" under Blockers/Concerns
  </read_first>
  <action>
  Replace the bullet starting "**CarPlay entitlement GRANTED (2026-08-18)**" with a bullet reflecting the true state as of today. If Task 2 passed and the human reported the CarPlay Simulator scene connected: write a bullet starting "**CarPlay entitlement wiring RESOLVED (today's date)**" recording that steps 1-11 are complete, the CarTube target now signs manually against the imported profile (project-settings confirmed via xcodebuild -showBuildSettings), the entitlements file and bundle ID are unchanged, and the human observed the CarPlay scene connect in the CarPlay Simulator. If Task 2 passed but the human reported they could not test the CarPlay Simulator (e.g. tool not installed, no compatible hardware): write the same resolved bullet but explicitly note the scene-connection observation as an open residual item with the human's stated reason, rather than claiming it verified. If Task 1 or Task 2 did not pass: leave the bullet describing the specific remaining blocker (which numbered steps are still outstanding and why), do not claim resolution. Do not delete the rest of the Blockers/Concerns section.
  </action>
  <verify>
    <automated>! grep -q 'CarPlay entitlement GRANTED (2026-08-18)' .planning/STATE.md && grep -q 'com.apple.developer.carplay-audio' CarTube/CarTube.entitlements</automated>
  </verify>
  <acceptance_criteria>
  - STATE.md no longer contains the original pre-closure bullet text verbatim (it has been replaced, not duplicated)
  - The new bullet accurately states either full resolution with the human's scene-connection observation, resolution-with-residual-item, or the specific remaining blocker — never a false "resolved" claim when Task 1/2 did not both pass
  - CarTube/CarTube.entitlements is confirmed unchanged (still carries the CarPlay key)
  </acceptance_criteria>
  <done>STATE.md accurately reflects the true wiring state — the CarPlay-wiring gap from 01-VERIFICATION.md is either closed and evidenced, or explicitly tracked with its real remaining blocker, never silently marked done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple Developer Portal ↔ repo | entitlement/profile grant is human-only behind Apple ID auth; repo owns the resulting signing-config verification |
| Human Xcode-UI action ↔ project.pbxproj | a manual signing-config change made outside version-controlled automation; must be regression-checked, not trusted blindly |
| CarPlay Simulator scene observation | inherently human-observed; no CLI proxy exists — must never be fabricated as an automated pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | CarTube target signing config (project.pbxproj) | medium | mitigate | Task 2 re-verifies CODE_SIGN_ENTITLEMENTS and PRODUCT_BUNDLE_IDENTIFIER are unchanged after the human's Xcode edits, guarding against a wrong-profile or wrong-team selection silently altering the signed identity |
| T-01-10 | Denial of Service | simulator/CI build pipeline | medium | mitigate | Task 2 re-runs a full simulator build after the signing-style change to prove Debug/simulator builds still succeed regardless of the new Manual signing style, catching any accidental project corruption immediately |
| T-01-SC | Tampering | package installs | low | accept | no package installs or registry fetches in this plan |
</threat_model>

<verification>
- Task 1 checkpoint: human confirms the Apple-portal + Xcode-signing steps and reports the CarPlay Simulator scene-connection observation (or explicit inability to test it)
- Task 2 automated: xcodebuild -showBuildSettings confirms Manual signing + non-empty profile specifier + unchanged entitlements/bundle ID; simulator build regression stays green
- Task 3 automated: STATE.md accurately reflects resolution, partial resolution, or the specific remaining blocker
</verification>

<success_criteria>
- The CarTube target signs manually against an imported CarPlay-capable provisioning profile, confirmed at the project-settings level
- CODE_SIGN_ENTITLEMENTS, the entitlements file contents, and the bundle ID are all unchanged by the signing-config switch
- The CarPlay Simulator scene-connection observation is recorded honestly (verified, or explicitly untested) — never fabricated
- STATE.md closes the 01-VERIFICATION.md CarPlay-wiring gap for a future verifier, or precisely documents what remains
</success_criteria>

## Artifacts this phase produces (plan 05)

- CarTube.xcodeproj/project.pbxproj — CarTube target Debug+Release signing settings switched to CODE_SIGN_STYLE=Manual with a non-empty PROVISIONING_PROFILE_SPECIFIER (human-performed via Xcode UI, agent-verified via xcodebuild -showBuildSettings)
- CarTube/CarTube.entitlements — confirmed unchanged, still carrying `com.apple.developer.carplay-audio = true`
- .planning/STATE.md — CarPlay-wiring blocker bullet rewritten to reflect the true state: resolved with the human's scene-connection observation, resolved-with-residual-item, or the specific remaining blocker

<output>
Create .planning/phases/01-external-dependencies-project-setup/01-05-SUMMARY.md when done
</output>
