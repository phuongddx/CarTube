---
phase: 01-external-dependencies-project-setup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - Secrets.xcconfig
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: false
gap_closure: true
requirements: [INFRA-01]

user_setup:
  - service: google-cloud
    why: "Create the dedicated Google Cloud project, enable YouTube Data API v3, and create the restricted API key CarTube's search feature calls — the runbook and build-time delivery pipe are already built and proven, only the real key is missing"
    env_vars: []
    dashboard_config:
      - task: "Follow docs/runbooks/google-youtube-api-key.md end to end: new project cartube-shipping -> enable YouTube Data API v3 -> create key -> API restriction (YouTube Data API v3 only) -> iOS application restriction (com.cartube.carplay, com.cartube.carplay.playon) -> quota alert -> paste the key into root Secrets.xcconfig replacing the sentinel"
        location: "console.cloud.google.com"

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A restricted YouTube Data API key exists in a dedicated Google Cloud project (API restriction: YouTube Data API v3 only; iOS application restriction: com.cartube.carplay + com.cartube.carplay.playon), delivered only via the gitignored root Secrets.xcconfig — the sentinel BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY is no longer present anywhere in the delivery path (closes 01-VERIFICATION.md gap: 'A restricted YouTube Data API key exists in a dedicated Google Cloud project...')"
    - "A fresh simulator build's Info.plist carries the real key at build time — plutil extraction matches ^AIza[0-9A-Za-z_-]{30,}$ — while git-tracked files still contain zero AIza-pattern strings (closes 01-VERIFICATION.md gap: 'Built app Info.plist carries the real YouTube API key at build time')"
  artifacts:
    - Secrets.xcconfig (gitignored, real AIza-prefixed key value replacing the sentinel)
    - .planning/STATE.md (Google-key blocker bullet rewritten as resolved, dated, with evidence)
    - .planning/PROJECT.md (xcconfig-delivery Key Decision row Outcome cell updated from "— Pending")
  key_links:
    - "real key -> root Secrets.xcconfig -> pbxproj baseConfigurationReference -> $(YOUTUBE_API_KEY) -> built Info.plist (the pipe plan 01-01 proved with the sentinel; this plan supplies the real value and re-proves the pipe end to end with it)"
---

<objective>
Close the first of two open Phase 1 gaps recorded in 01-VERIFICATION.md: the Google Cloud YouTube Data API key was never actually created, so root `Secrets.xcconfig` still holds the sentinel `BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY` instead of a real `AIza...` key. The runbook (`docs/runbooks/google-youtube-api-key.md`) and the entire build-time delivery pipe (xcconfig -> pbxproj -> Info.plist substitution) were already built and proven in plans 01-01/01-03 — the only missing piece is the login-walled Google Cloud Console action itself, which this agent cannot perform.

This plan gates the one genuinely human step behind a `checkpoint:human-action`, then runs the exact automated verification the original 01-03 plan already specified (plutil extraction against the real-key regex, git-grep hygiene, git-ignore confirmation) once the human confirms, and closes out the tracking state so a future verifier can confirm the gap is shut.

Purpose: Google key provisioning is the last missing piece of INFRA-01's success criterion 2 and the thing already blocking Phase 3 search functionality (REQUIREMENTS.md shows SRCH-01/SRCH-04 as "Gaps Found").
Output: real restricted key delivered into the built binary, repo hygiene re-proven, STATE.md/PROJECT.md reflecting the true resolved state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-project-setup/01-VERIFICATION.md
@.planning/phases/01-external-dependencies-project-setup/01-03-SUMMARY.md
@docs/runbooks/google-youtube-api-key.md
@Config/Secrets.xcconfig.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Create the restricted Google Cloud API key and paste it into Secrets.xcconfig</name>
  <read_first>
  - docs/runbooks/google-youtube-api-key.md (the exact script to follow, top to bottom)
  - Secrets.xcconfig (currently `YOUTUBE_API_KEY = BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY` — the single file the real key value may land in)
  </read_first>
  <action>
  Everything automatable is already done: the runbook exists, the xcconfig -> pbxproj -> Info.plist delivery pipe is proven end-to-end with the sentinel value, and `.gitignore` already excludes `Secrets.xcconfig`. The one remaining thing requires a human with Google Cloud Console access: create a new dedicated project (`cartube-shipping`), enable YouTube Data API v3, create an API key, set both restrictions (API: YouTube Data API v3 only; iOS application: `com.cartube.carplay` and `com.cartube.carplay.playon`), enable quota alerting, then paste the created key into root `Secrets.xcconfig` replacing the sentinel string `BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY`. The key value never needs to be pasted into chat — editing the file directly and reporting completion is the preferred path.
  </action>
  <instructions>
  1. Open `docs/runbooks/google-youtube-api-key.md` and follow it exactly, in order: new project `cartube-shipping` -> APIs & Services -> Library -> enable YouTube Data API v3 (verify status ON before continuing) -> Credentials -> Create credentials -> API key -> set the API restriction (YouTube Data API v3 only) -> set the application restriction (iOS apps: `com.cartube.carplay`, `com.cartube.carplay.playon`) -> Save -> set up quota alerting (~80% of the daily bucket).
  2. Edit root `Secrets.xcconfig` yourself, replacing `BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY` with the real key value on the `YOUTUBE_API_KEY = ...` line. Do not touch `Config/Secrets.xcconfig.example` (it must keep `REPLACE_ME`).
  3. Report back: the Google Cloud project ID, and confirmation that both restrictions were set (or "skip — fixture only" if you'd rather defer this and leave the sentinel in place for now).
  </instructions>
  <verification>
  Task 2 in this plan runs immediately after you confirm: a fresh simulator build, `plutil -extract YOUTUBE_API_KEY raw` on the built `Info.plist` (must match `^AIza[0-9A-Za-z_-]{30,}$`), `git grep -nE 'AIza[0-9A-Za-z_-]{30,}'` (must stay at zero matches), and `git check-ignore -q Secrets.xcconfig` (must still succeed). A `gcloud services api-keys describe` restriction check runs best-effort if the reported project ID is reachable, but is not required to pass this gate — Console-side restriction screenshots are an acceptable substitute if `gcloud` auth isn't set up for that account.
  </verification>
  <resume-signal>Type "key created" plus the Google Cloud project ID, or "skip — fixture only" to defer and leave the sentinel in place</resume-signal>
  <acceptance_criteria>
  - `Secrets.xcconfig` contains a `YOUTUBE_API_KEY` value matching `^AIza[0-9A-Za-z_-]{30,}$`, not the sentinel
  - Both restrictions (API: YouTube Data API v3; iOS application: the two `com.cartube.*` bundle IDs) were set in the Console per the runbook
  - The key was never pasted into any git-tracked file, document, or chat transcript that gets committed
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Verify real key delivery end-to-end and repo hygiene</name>
  <files>Secrets.xcconfig, CarTube/Info.plist</files>
  <precondition>Task 1's human checkpoint reported "key created" (not "skip — fixture only") — if the human chose to skip, halt this task and leave the sentinel in place; do not fabricate a passing result.</precondition>
  <read_first>
  - Secrets.xcconfig — confirm the line now reads `YOUTUBE_API_KEY = AIza...` (not the sentinel)
  - docs/runbooks/google-youtube-api-key.md section 4 ("Verification") — the exact commands this task runs
  </read_first>
  <action>
  Run a fresh simulator build: `xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build`. Extract `BUILT_PRODUCTS_DIR` from `xcodebuild -showBuildSettings` for the same scheme/destination, then run `plutil -extract YOUTUBE_API_KEY raw "$BUILT_PRODUCTS_DIR/CarTube.app/Info.plist"` and assert the output matches the pattern `^AIza[0-9A-Za-z_-]{30,}$` — if it still prints `BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY`, the gap is not closed and this task must fail loudly rather than report success. Then run `git grep -nE 'AIza[0-9A-Za-z_-]{30,}'` across the tracked tree and assert zero matches, and `git check-ignore -q Secrets.xcconfig` and assert exit 0. As a best-effort, non-blocking addition: if Task 1 reported a Google Cloud project ID and `gcloud auth list` shows an active account, run `gcloud services api-keys list --project=<reported-project-id>` and `gcloud services api-keys describe <key-name> --project=<reported-project-id>` to confirm the API restriction targets `youtube.googleapis.com` and the application restriction lists both `com.cartube.*` bundle IDs; record whatever this prints (or its absence, if `gcloud` auth isn't available for that account) in the SUMMARY without gating pass/fail on it.
  </action>
  <verify>
    <automated>xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' build 2>&1 | tail -3 | grep -q 'BUILD SUCCEEDED' && BP="$(xcodebuild -project CarTube.xcodeproj -scheme CarTube -destination 'platform=iOS Simulator,name=iPhone Air' -showBuildSettings 2>/dev/null | awk '/ BUILT_PRODUCTS_DIR/{print $3; exit}')" && KEY="$(plutil -extract YOUTUBE_API_KEY raw "$BP/CarTube.app/Info.plist")" && echo "$KEY" | grep -qE '^AIza[0-9A-Za-z_-]{30,}$' && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}' && git check-ignore -q Secrets.xcconfig</automated>
  </verify>
  <acceptance_criteria>
  - `plutil -extract YOUTUBE_API_KEY raw` on the freshly built `Info.plist` prints a value matching `^AIza[0-9A-Za-z_-]{30,}$`, never the sentinel string
  - `git grep -nE 'AIza[0-9A-Za-z_-]{30,}'` returns zero matches across all tracked files
  - `git check-ignore -q Secrets.xcconfig` exits 0
  - The best-effort `gcloud` restriction check output (or its documented absence) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The real key is proven flowing end-to-end into the built binary via the existing delivery pipe, and zero key material exists in any git-tracked file.</done>
</task>

<task type="auto">
  <name>Close out the Google-key blocker in STATE.md and PROJECT.md</name>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <precondition>Task 2's automated verify passed (real key confirmed end-to-end) — do not mark the blocker resolved if Task 2 halted or Task 1 was skipped.</precondition>
  <reversibility rating="reversible">documentation only — STATE.md and PROJECT.md text updates.</reversibility>
  <read_first>
  - .planning/STATE.md — the exact bullet beginning "**Google shipping key not yet created (external, 2026-08-18)**" under Blockers/Concerns
  - .planning/PROJECT.md — the Key Decisions table row beginning "API key delivered via gitignored root Secrets.xcconfig with $(YOUTUBE_API_KEY) Info.plist injection" (Outcome cell currently "— Pending")
  </read_first>
  <action>
  In .planning/STATE.md, replace the bullet starting "**Google shipping key not yet created (external, 2026-08-18)**" with a resolved bullet starting "**Google shipping key RESOLVED (today's date)**" that records: the created Google Cloud project ID (from Task 1's report), that both restrictions (YouTube Data API v3; iOS bundle IDs com.cartube.carplay + com.cartube.carplay.playon) were set per the runbook, that Task 2's plutil extraction confirmed the real key reaches the built Info.plist, and that the git-hygiene grep stayed at zero matches. Do not delete the rest of the Blockers/Concerns section.
  In .planning/PROJECT.md, find the Key Decisions row whose Rationale/Decision text is "API key delivered via gitignored root Secrets.xcconfig with $(YOUTUBE_API_KEY) Info.plist injection" and change its Outcome cell from "— Pending" to "✓ Verified (today's date) — real key confirmed reaching the built Info.plist; git-hygiene clean; see STATE.md for the created project ID." Do not alter any other row's text.
  </action>
  <verify>
    <automated>! grep -q 'Google shipping key not yet created' .planning/STATE.md && grep -qi 'Google shipping key RESOLVED' .planning/STATE.md && grep -qE 'AIza\[0-9A-Za-z_-\]\{30,\}\$|plutil' .planning/STATE.md && ! grep -A2 'API key delivered via gitignored root Secrets.xcconfig' .planning/PROJECT.md | grep -q '— Pending' && ! git grep -nE 'AIza[0-9A-Za-z_-]{30,}'</automated>
  </verify>
  <acceptance_criteria>
  - STATE.md no longer contains the literal phrase "Google shipping key not yet created"
  - STATE.md contains a new bullet with the literal phrase "Google shipping key RESOLVED" plus today's date and the evidence summary
  - PROJECT.md's xcconfig-delivery Key Decision row Outcome cell no longer reads "— Pending"
  - Final `git grep -nE 'AIza[0-9A-Za-z_-]{30,}'` hygiene re-check still returns zero matches
  </acceptance_criteria>
  <done>STATE.md and PROJECT.md both reflect the true resolved state — the Google key gap from 01-VERIFICATION.md is closed and evidenced, nothing silently marked done that isn't.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Cloud Console ↔ repo | key creation is human-only behind Google auth; repo owns delivery + verification |
| Secrets.xcconfig → IPA | intentional crossing; key is public-by-design and treated as such (documented in the runbook's honesty section) |
| key → Google quota bucket | 100 search.list calls/day shared across all installs; a leak starves every user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | real key committed to the public fork during the paste step | high | mitigate | key lands only in gitignored root Secrets.xcconfig; git grep AIza gate re-run in both Task 2 and Task 3 acceptance criteria; example file stays REPLACE_ME |
| T-01-08 | DoS (quota abuse) | extracted key burns the fleet-wide 100/day search.list bucket | high | mitigate | both restrictions (API: YouTube Data API v3 only; application: iOS bundle IDs com.cartube.carplay/.playon) + quota alerting instructed in Task 1; best-effort gcloud confirmation in Task 2; documented rotation path in the runbook if a leak is suspected |
| T-01-SC | Tampering | package installs | low | accept | no package installs or registry fetches in this plan |
</threat_model>

<verification>
- Task 1 checkpoint: human confirms the key was created with both restrictions and pasted into Secrets.xcconfig (or explicitly skips)
- Task 2 automated: fresh simulator build succeeds; plutil extraction matches the real-key regex; git grep hygiene stays clean; git-ignore still holds
- Task 3 automated: STATE.md and PROJECT.md both show the resolved state; final hygiene re-check clean
</verification>

<success_criteria>
- Restricted key exists in a dedicated Google Cloud project with both restrictions naming the confirmed bundle IDs, delivered at build time into the built binary
- Zero key-pattern strings in tracked files
- STATE.md and PROJECT.md accurately reflect resolution, closing the 01-VERIFICATION.md Google-key gap for a future verifier
</success_criteria>

## Artifacts this phase produces (plan 04)

- Secrets.xcconfig — real `AIza...` key value replacing `BUILD_TIME_SENTINEL_REPLACE_WITH_REAL_KEY` (local file, gitignored, never committed)
- .planning/STATE.md — Google-key blocker bullet rewritten as resolved, dated, with project ID and verification evidence
- .planning/PROJECT.md — xcconfig-delivery Key Decision row Outcome cell updated from "— Pending" to a verified/dated outcome
- Re-proof of the existing delivery pipe (Secrets.xcconfig -> pbxproj baseConfigurationReference -> $(YOUTUBE_API_KEY) -> built Info.plist) with the real key value, not the sentinel

<output>
Create .planning/phases/01-external-dependencies-project-setup/01-04-SUMMARY.md when done
</output>