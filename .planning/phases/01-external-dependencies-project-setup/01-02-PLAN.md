---
phase: 01-external-dependencies-project-setup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runbooks/apple-carplay-entitlement-request.md
  - docs/runbooks/carplay-entitlement-grant-wiring.md
  - .planning/STATE.md
autonomous: false
requirements: [INFRA-01]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A paste-ready entitlement request exists whose text positions the app under the audio category and contains no on-screen-viewing or driver-context wording inside the paste block
    - The exact distribution bundle ID is consciously confirmed by the user before anything is submitted — never the upstream author's prefix by default
    - The entitlement application is submitted at developer.apple.com/carplay and the submission date is recorded as a dated STATE.md entry
    - A grant-wiring runbook exists that any later phase can execute the day Apple grants the entitlement, including the entitlements-file sequencing constraint
  artifacts:
    - docs/runbooks/apple-carplay-entitlement-request.md
    - docs/runbooks/carplay-entitlement-grant-wiring.md
    - .planning/STATE.md dated Blockers/Concerns entry (submission date, category, confirmed bundle ID, runbook pointer)
  key_links:
    - confirmed bundle ID → Apple portal App ID registration → future CarPlay provisioning profile (a mismatch here orphans the grant)
    - grant-wiring runbook step "add com.apple.developer.carplay-audio" ↔ Phase 2 entitlements cleanup (INFRA-02) — the current TrollStore-only entitlements file cannot sign against a real profile
---

<objective>
Start the Apple external clock: prepare both runbooks (request + grant wiring), gate the bundle-ID decision on the user, submit the CarPlay entitlement application under the audio category, and record it as a dated blocker so later phases never silently wait.

Implements INFRA-01 success criteria 1 (audio-category submission, documented rationale, dated record) and the pre-staged half of criterion 3 (grant-wiring runbook).

Purpose: entitlement review runs days–months with no SLA; starting it on day 1 with the correct App ID is the entire point of this phase.
Output: two committed runbooks, a submitted application, a dated STATE.md blocker entry.
</objective>

<execution_context>
@/Users/ddphuong/.codex/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md
@.planning/phases/01-external-dependencies-project-setup/NOTES.md
@CarTube/CarTube.entitlements
</context>

<tasks>

<task type="auto">
  <name>Author Apple entitlement request + grant-wiring runbooks with bundle-ID placeholder</name>
  <files>docs/runbooks/apple-carplay-entitlement-request.md, docs/runbooks/carplay-entitlement-grant-wiring.md</files>
  <read_first>
  - .planning/phases/01-external-dependencies-project-setup/01-RESEARCH.md — sections "Pattern 2: Entitlement grant-wiring runbook", "Pitfall 1" (bundle ID gates both clocks), "Pitfall 2" (entitlements file blocks standard provisioning), "Pitfall 3" (category framing), and Assumptions A2/A3
  - CarTube/CarTube.entitlements (verbatim list of TrollStore keys that make the current file ungrantable)
  - .planning/phases/01-external-dependencies-project-setup/NOTES.md (default decision: re-prefix to user's own reverse-DNS; exact string confirmed at the checkpoint)
  - CarTube.xcodeproj/project.pbxproj — the PRODUCT_BUNDLE_IDENTIFIER and DEVELOPMENT_TEAM lines only (orientation for the runbook's current-state section)
  </read_first>
  <reversibility rating="reversible">documentation only; no project configuration changes.</reversibility>
  <action>
  Create docs/runbooks/ and write two files.

  apple-carplay-entitlement-request.md contains:
  1. A prerequisites section: paid Apple Developer Program membership required (research assumption A2 — confirm the account tier at sign-in; a free-tier account cannot submit), team ID U67AKNW8PW, and the bundle-ID decision with the user-confirmed value `com.cartube.carplay` (chosen 2026-08-18; share-extension App ID registered as `com.cartube.carplay.playon`) plus the fallback note — option B: keep com.avangelista.CarTube only if the user consciously accepts upstream branding and it is registerable. State plainly that the pbxproj PRODUCT_BUNDLE_IDENTIFIER change lands in Phase 2, but the string named here must be the final distribution ID.
  2. The exact entry URL developer.apple.com/carplay and click path: Request CarPlay app entitlement (redirects to /contact/carplay/ behind Apple ID sign-in), register/select the App ID under Certificates, IDs & Profiles → Identifiers using the confirmed bundle ID, select category audio, agree to the CarPlay Entitlement Addendum, paste the request text.
  3. A paste-ready request text delimited by literal markers line REQUEST-TEXT-BEGIN and line REQUEST-TEXT-END. The text between the markers describes CarTube as an app whose primary purpose is audio playback of YouTube content through the vehicle's audio system, using the existing CarPlay scene integration, and it omits every mention of on-screen viewing, a video surface, or driver contexts — audio-category apps must be designed primarily for audio playback per the CarPlay Developer Guide criteria, and the residual category-fit risk is recorded as a Key Decision in plan 01-03. Outside the markers, a rationale section may explain why such wording is excluded.
  4. A post-submission section: record the submission date, expect no SLA (community-reported days–months), and re-check the developer account periodically.

  carplay-entitlement-grant-wiring.md contains the verified post-grant sequence as numbered steps: log in to developer.apple.com/account → Certificates, IDs & Profiles → Identifiers → select the App ID → enable the CarPlay entitlements → save → Provisioning Profiles → create a new profile for the App ID → import it into Xcode → add key com.apple.developer.carplay-audio with value true to the entitlements file → Signing & Capabilities → turn OFF Automatically manage signing → confirm Build Settings CODE_SIGN_ENTITLEMENTS still points at CarTube/CarTube.entitlements → verify the CarPlay scene connects using the CarPlay Simulator from Additional Tools for Xcode (download when the grant lands). Include a prominent sequencing-constraint section naming every current entitlements key (SBStarkCapable, com.apple.runningboard.assertions.webkit, com.apple.multitasking.systemappassertions, com.apple.backboard.displaybrightness, platform-application, com.apple.private.security.no-container, com.apple.private.security.container-manager) as ungrantable by a real profile: signing fails until the file holds only grantable keys, the key-stripping is Phase 2 (INFRA-02) scope, and if the grant lands before Phase 2 completes, either finish Phase 2's entitlements cleanup first or swap in a clean entitlements file as part of executing this runbook — then rebuild and verify. Close with the expected-branch note: while pending, Phases 3–5 proceed against phone-side mocks.
  </action>
  <verify>
    <automated>grep -q 'developer.apple.com/carplay' docs/runbooks/apple-carplay-entitlement-request.md && grep -q 'com.apple.developer.carplay-audio' docs/runbooks/carplay-entitlement-grant-wiring.md && grep -q 'CarPlay Entitlement Addendum' docs/runbooks/apple-carplay-entitlement-request.md && grep -q 'Automatically manage signing' docs/runbooks/carplay-entitlement-grant-wiring.md && grep -q 'REQUEST-TEXT-BEGIN' docs/runbooks/apple-carplay-entitlement-request.md && grep -q 'REQUEST-TEXT-END' docs/runbooks/apple-carplay-entitlement-request.md && [ "$(sed -n '/REQUEST-TEXT-BEGIN/,/REQUEST-TEXT-END/p' docs/runbooks/apple-carplay-entitlement-request.md | grep -icE 'watch|driving|video|driver')" = "0" ] && grep -q 'com.apple.private.security.no-container' docs/runbooks/carplay-entitlement-grant-wiring.md</automated>
  </verify>
  <acceptance_criteria>
  - docs/runbooks/apple-carplay-entitlement-request.md exists and contains the literal strings developer.apple.com/carplay, CarPlay Entitlement Addendum, com.cartube.carplay, and the category word audio
  - The sed-extracted REQUEST-TEXT-BEGIN/REQUEST-TEXT-END block exists (both marker lines present) and matches zero case-insensitive hits for the expressions watch, driving, video, or driver
  - docs/runbooks/carplay-entitlement-grant-wiring.md exists and contains com.apple.developer.carplay-audio, Automatically manage signing, CarTube/CarTube.entitlements, and all seven ungrantable entitlement key names
  - Neither runbook contains a section instructing any edit to the pbxproj this phase
  </acceptance_criteria>
  <done>Both runbooks committed: paste-ready audio-category request text with the bundle-ID placeholder, and the full post-grant wiring sequence including the entitlements-file constraint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Confirm distribution bundle ID and submit the CarPlay entitlement request</name>
  <what-built>Agent prepared the complete submission runbook with paste-ready audio-category request text; the distribution bundle ID is pre-confirmed as `com.cartube.carplay` (user choice recorded 2026-08-18), and the checkpoint validates that choice and the Apple-account state before anything is submitted, because the entitlement grant attaches to that App ID permanently.</what-built>
  <read_first>
  - docs/runbooks/apple-carplay-entitlement-request.md (full document — this is the script the user follows)
  - .planning/phases/01-external-dependencies-project-setup/NOTES.md (default applied: re-prefix under the user's own reverse-DNS)
  </read_first>
  <reversibility rating="costly">a submitted application names an App ID; a later bundle-ID change orphans the grant and forces re-application — guarded by this same checkpoint confirming the exact string first.</reversibility>
  <how-to-verify>
  1. Validate the pre-confirmed bundle ID `com.cartube.carplay` before opening any Apple page — confirm you still want the product-brand prefix (you chose option 5 on 2026-08-18) and that it is registerable under team U67AKNW8PW; fallback option B: keep com.avangelista.CarTube only if you accept upstream branding and confirm it is registerable.
  2. Sign in at developer.apple.com/carplay with the account owning team U67AKNW8PW; confirm the membership is a paid Apple Developer Program tier (a free tier cannot submit — report back if so, this becomes a blocker).
  3. Follow the runbook: Certificates, IDs & Profiles → Identifiers → register the App ID with your chosen bundle ID string → select the audio category → agree to the CarPlay Entitlement Addendum → paste the request text from between the REQUEST-TEXT markers unchanged.
  4. Submit. Record the submission date shown/known.
  5. Report back: the exact confirmed bundle ID string, the submission date, and any form fields that differed from the runbook (research assumption A3 expected minor surprises).
  </how-to-verify>
  <resume-signal>Type "submitted" plus the confirmed bundle ID and submission date, or describe what blocked you</resume-signal>
  <acceptance_criteria>
  - User-reported confirmed bundle ID string is recorded by the executor and written into STATE.md in Task 3
  - Submission date reported and non-ambiguous
  - If submission was impossible (e.g., free-tier account), the blocker is reported instead and Task 3 records it as the dated blocker
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Record dated entitlement blocker and rationale location in STATE.md</name>
  <files>.planning/STATE.md</files>
  <read_first>
  - .planning/STATE.md (Blockers/Concerns section — the generic seed line to be upgraded)
  - docs/runbooks/carplay-entitlement-grant-wiring.md (the runbook the entry points to)
  </read_first>
  <action>
  Replace the generic Phase 1 blocker seed line under Blockers/Concerns with a dated record containing: submission date as reported, category requested (audio), the exact confirmed bundle ID string, no SLA with periodic developer-account re-check, pointer to docs/runbooks/carplay-entitlement-grant-wiring.md as the procedure to execute the day the grant lands, and the standing note that Phases 3–5 are unblocked via phone-side mocks while pending. Update the Session Continuity lines (last activity, stopped at) to reflect the submission. If Task 2 reported a submission blocker instead of a submission, record that fact and date instead — the dated record exists either way. Do not edit PROJECT.md here; Key Decision rows are written once, in plan 01-03.
  </action>
  <verify>
    <automated>grep -q 'audio' .planning/STATE.md && grep -q 'carplay-entitlement-grant-wiring' .planning/STATE.md && grep -qE 'Submitted [0-9]{4}-[0-9]{2}-[0-9]{2}|BLOCKER' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
  - STATE.md Blockers/Concerns contains the submission date in YYYY-MM-DD form (or a BLOCKER record with its own date), the word audio, the confirmed bundle ID string, and the literal path docs/runbooks/carplay-entitlement-grant-wiring.md
  - The old generic seed line about the entitlement timeline no longer exists in its original undated form
  - Session Continuity reflects this submission as the last activity
  </acceptance_criteria>
  <done>The external Apple clock is visible in project state as a dated, actionable record that any later phase reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple developer portal ↔ repo | submission is human-only behind Apple ID auth; repo owns rationale, date, and runbook |
| bundle ID ↔ App ID ↔ future profile | permanent linkage; wrong string orphans the grant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | bundle-ID confirmation in entitlement submission | high | mitigate | blocking human checkpoint confirms the exact string before any submission; default re-prefix per NOTES.md; upstream prefix never silently reused |
| T-01-03 | Repudiation | category framing in request text | medium | mitigate | paste-ready text scoped between REQUEST-TEXT markers, negative-gated for prohibited wording; residual category-fit risk recorded as Key Decision in plan 01-03 |
| T-01-SC | Tampering | package installs | low | accept | no package installs or registry fetches in this plan |
</threat_model>

<verification>
- Task 1 automated greps green (URL, entitlement key, addendum, signing step, marker-scoped wording gate)
- Task 2 checkpoint completed with confirmed bundle ID and date (or blocker) reported
- Task 3 STATE.md greps green
</verification>

<success_criteria>
- Apple entitlement application submitted under the audio category with the consciously-chosen bundle ID, date recorded in STATE.md
- Grant-wiring runbook executable by any later phase, including the entitlements-file sequencing constraint
- No repo configuration changed by this plan (runbooks + state only)
</success_criteria>

## Artifacts this phase produces (plan 02)

- File docs/runbooks/apple-carplay-entitlement-request.md — prerequisites (paid-membership check, bundle-ID options), portal click path from developer.apple.com/carplay, REQUEST-TEXT-BEGIN/REQUEST-TEXT-END paste block, post-submission recording instructions
- File docs/runbooks/carplay-entitlement-grant-wiring.md — numbered post-grant sequence (App ID capability → profile → import → com.apple.developer.carplay-audio → automatic signing off → scene verification), ungrantable-key sequencing constraint, pending-branch note
- .planning/STATE.md — dated Blockers/Concerns record (submission date, audio category, confirmed bundle ID, runbook pointer)
- Marker literals REQUEST-TEXT-BEGIN / REQUEST-TEXT-END (used by the wording gate)

<output>
Create .planning/phases/01-external-dependencies-project-setup/01-02-SUMMARY.md when done
</output>
